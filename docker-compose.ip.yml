# Compatibility entrypoint. The IP deployment now imports the hardened
# production model so credentials, TLS settings and runtime limits have one
# source of truth. Prefer docker-compose.prod.yml for new installations.
include:
  - path: ./docker-compose.prod.yml
