volumes:
  repoguard_loki_data:
    external: true
    name: ${LOKI_DATA_VOLUME_NAME:-app_repoguard_loki_data}
  repoguard_grafana_data:
    external: true
    name: ${GRAFANA_DATA_VOLUME_NAME:-app_repoguard_grafana_data}
  repoguard_alloy_data:
    external: true
    name: ${ALLOY_DATA_VOLUME_NAME:-repoguard_observability_alloy_data_v1}
