# RepoGuard Agent 全量代码审查与优化报告

审查日期：2026-07-13
审查分支：PRAgent-test
审查范围：后端 Java/SQL/YAML、前端 Vue/TypeScript/CSS、Docker/Nginx/Caddy、GitHub Actions 与测试体系
文档格式说明：根据最新约定，本报告作为唯一经批准的非 README Markdown 优化进度文档提交；测试或临时脚本仍不得提交 Git。

## 一、结论摘要

项目已经具备较好的生产化基础，不属于需要推倒重构的状态。认证链路采用内存访问令牌、HttpOnly 刷新 Cookie、CSRF 校验和刷新令牌轮换；外部请求有主机/端口策略；Docker 镜像使用非 root 用户；RabbitMQ 发布确认、补偿、任务抢占、数据库归档、指标基数限制、API 契约快照和 MySQL/RabbitMQ 集成 CI 均已落地。

当前最应优先处理的不是代码风格，而是以下四类问题：

1. 通知手动重试在数据库事务提交前直接发布 RabbitMQ，且没有状态白名单和并发抢占，存在重复通知及“消息已发、事务回滚”的一致性风险。
2. 前端部分异步流程缺少请求序号或取消机制，慢响应可能覆盖新路由、新搜索条件；集成配置的四接口并发保存可能形成部分成功。
3. 大表迁移、逐行插入和首屏公共依赖仍有明确的扩容与性能成本。
4. 构建版本、覆盖率门禁、真实浏览器验收和依赖治理尚未形成完全可复现的闭环。

建议先完成 P0 和 P1，确认行为正确后再进行架构收敛。不要为了减少文件数而先做大规模重构。

## 二、规模与验证基线

代码规模：

- 后端生产 Java：653 个文件，约 35,774 行。
- 后端测试 Java：359 个文件，约 36,614 行。
- 前端 TypeScript：95 个文件，约 8,443 行。
- 前端 Vue：39 个文件，约 6,056 行。
- Flyway 迁移：54 个文件。
- 后端 service.impl：99 个文件，平均约 101.8 行；其中 13 个超过 200 行。

本次验证结果：

- mvn test：1,617 个测试，0 failure，0 error，2 skipped；跳过项为依赖真实 MySQL/RabbitMQ 环境的生产上下文集成测试。
- JaCoCo：line 89.9%，branch 72.1%，method 90.0%，class 96.0%。
- 前端 vue-tsc：通过。
- 前端 ESLint：通过。
- 前端 Vitest：17 个测试文件、61 个测试全部通过。
- 前端 Vite 生产构建：通过，3,327 个模块完成转换。
- Maven dependency:analyze：构建通过；编译器提示生产代码存在 deprecated API，测试代码另有 deprecated/unchecked 提示。Starter 项目会让 dependency:analyze 产生较多“间接依赖被直接使用”的噪声，不应机械地把所有传递依赖改成直接依赖。

验证限制：

- 本地 JDK 为 26，项目 POM、CI 和运行镜像目标为 Java 25。JaCoCo 0.8.13 对 JDK 26 系统类出现 major version 70 插桩错误，但 Maven 仍返回成功；应用类覆盖率报告仍生成。应在 CI 之外也强制 Java 25，或升级兼容的 JaCoCo 后再使用 JDK 26。
- 本次未连接真实 GitHub、LLM、生产 MySQL/RabbitMQ，也未执行真实浏览器和压力测试。
- 结论以静态审查、现有测试、构建结果和配置拓扑为依据；涉及容量的项需要用生产量级数据复测。

## 三、优先级定义

- P0：可能造成重复外部副作用、数据/消息不一致，优先阻断发布。
- P1：会产生错误展示、部分保存、竞态、明显性能或扩容风险，应在近期迭代处理。
- P2：工程治理、可维护性、可访问性和长期成本优化，可分阶段推进。

## 四、P0 优化项

### P0-1 通知手动重试需要改为“受控状态迁移 + 提交后发布”

证据：

- repoguard-backend/src/main/java/com/repoguard/agent/service/impl/NotificationEventQueryServiceImpl.java:68
- repoguard-backend/src/main/java/com/repoguard/agent/service/impl/NotificationEventQueryServiceImpl.java:76
- repoguard-backend/src/main/java/com/repoguard/agent/service/impl/NotificationEventQueryServiceImpl.java:77
- repoguard-backend/src/main/java/com/repoguard/agent/notification/NotificationDispatchServiceImpl.java:50
- repoguard-backend/src/main/java/com/repoguard/agent/notification/NotificationDispatchServiceImpl.java:60
- repoguard-backend/src/main/java/com/repoguard/agent/notification/NotificationEventPublishCoordinator.java:35

现状：retryEvent() 带 @Transactional，先无条件把事件改为 PENDING，再调用 publishExistingEvent()；后者直接执行 publish()，没有使用项目已经存在的 publishAfterCommit()。更新条件也没有限制原状态或声明抢占者。

风险：

- 两个并发重试请求都可能发布同一个事件。
- 已经 DELIVERED/PUBLISHED 的事件也能被 API 再次改为 PENDING 并发布。
- RabbitMQ 已确认后，如果后续 select/组装响应抛异常，数据库事务回滚，但消息无法回滚，补偿任务可能再次发布。
- 当前单测只验证“重置并调用发布”，没有覆盖并发、事务回滚和已完成状态。

建议：

1. 增加单条条件更新：仅允许 PUBLISH_FAILED/DELIVERY_FAILED 等可重试状态转为 PENDING，并通过版本号或 publish_claimed_at/publish_claimed_by 完成 CAS 抢占。
2. 事务内只保存重试意图；提交后调用 publishAfterCommit()，或完全交给现有补偿 worker 扫描 PENDING 事件。
3. API 返回 queued/accepted 语义，不要把 RabbitMQ 发布成功与最终通知送达混为一体。
4. 消费端继续以 eventKey/eventId 做幂等，防止消息系统的至少一次投递转化为重复外部通知。

验收标准：

- 同一事件 20 个并发重试请求最多一个获得发布权。
- DELIVERED/PUBLISHED 事件返回 409 或业务拒绝，不改变状态。
- 强制事务回滚时 RabbitMQ 不出现消息。
- after-commit 发布失败时事件保留为可补偿状态，并能被 recovery worker 恢复。

## 五、P1 优化项

### P1-1 集成配置“保存全部”存在部分成功

证据：

- repoguard-frontend/src/features/integrations/composables/useIntegrationConfigPersistence.ts:88
- repoguard-frontend/src/features/integrations/composables/useIntegrationConfigPersistence.ts:94
- repoguard-frontend/src/features/integrations/composables/useIntegrationConfigPersistence.ts:102

现状：GitHub、MySQL、RabbitMQ、Review Policy 四个独立 PUT 通过 Promise.all 并发执行。任意一个失败时，其余请求不会取消，已经成功的配置仍然持久化，但页面只显示整体失败且不应用成功响应。

建议二选一：

- 交互改为每张集成卡独立保存、独立反馈，这是改动最小且语义最清楚的方案。
- 如果业务必须“全部保存”，增加后端批量命令，在同一数据库事务内校验并更新全部配置；外部连接测试与持久化分离。

同时把 loadConfig() 的 Promise.all 改为 Promise.allSettled，允许成功配置先展示，并在具体卡片标记失败。保存失败后主动重新拉取服务端状态，避免页面与数据库不一致。

验收标准：构造四个请求中任一失败，用户能明确看到每项结果；页面最终状态与数据库一致，不出现静默部分保存。

### P1-2 任务详情主请求可能被旧路由响应覆盖

证据：

- repoguard-frontend/src/features/review-detail/composables/useReviewDetailLoader.ts:54
- repoguard-frontend/src/features/review-detail/composables/useReviewDetailLoader.ts:75
- repoguard-frontend/src/features/review-detail/composables/useReviewDetailLoader.ts:76
- repoguard-frontend/src/pages/ReviewDetailPage.vue:716

现状：loadDetail() 在请求前读取 id，但响应回来后没有确认当前路由 id 仍然相同，也没有 requestSeq/AbortController。快速从任务 A 导航到任务 B 时，A 的慢响应可能最后写入 selectedTask；旧请求的 finally 也可能提前关闭新请求的 loading 状态。

建议：复用 useReviewTasksList 已有的 requestSeq 模式，或在 API 层支持 AbortSignal。只有“请求序号仍是最新且 id 等于当前路由”时才允许写状态、错误和 loading。pollReviewStatus() 同样应用该规则。

验收标准：人为让 A 延迟、B 快速返回，最终 URL、详情、轮询对象和 loading 必须全部属于 B。

### P1-3 用户搜索存在请求风暴和乱序覆盖

证据：

- repoguard-frontend/src/pages/UserManagementPage.vue:262
- repoguard-frontend/src/pages/UserManagementPage.vue:270
- repoguard-frontend/src/pages/UserManagementPage.vue:454

现状：keyword 每次输入都会立即调用服务端查询，没有 debounce，也没有请求序号。快速输入可能产生多次数据库 LIKE 查询，旧关键字的慢响应还能覆盖新关键字结果。分页和过滤刷新期间 loading 也没有统一覆盖。

建议：增加 300~400ms debounce、requestSeq/AbortController，并把所有列表入口收敛为一个 loadUsers({ reason })。后端对 username/email 模糊查询结合真实数据量核对索引和执行计划；数据量较大时考虑前缀搜索或专用检索字段。

验收标准：连续输入 10 个字符只触发 1~2 次请求；乱序响应不改变最终列表；切页和筛选时有一致的加载状态。

### P1-4 用户统计卡片把“当前页”误当成“全量账号”

证据：

- repoguard-frontend/src/pages/UserManagementPage.vue:213
- repoguard-frontend/src/pages/UserManagementPage.vue:238
- repoguard-frontend/src/pages/UserManagementPage.vue:249
- repoguard-frontend/src/pages/UserManagementPage.vue:255
- repoguard-frontend/src/pages/UserManagementPage.vue:271

现状：接口已经返回 usersTotal，但“账号总数、启用账号、管理员、禁用账号”全部基于当前页 users 数组计算。页大小默认为 20，因此账号总数最多显示 20；其他分类数也只是当前页分布。filteredUsers 又对已经由服务端筛选过的当前页重复筛选。

建议：

- 账号总数至少使用 usersTotal。
- 增加后端聚合接口一次返回 total/active/admin/disabled，或明确把卡片文案改为“当前页”。推荐前者。
- 删除当前页重复过滤，保持服务端分页和筛选为单一事实来源。

验收标准：准备 50 个账号并使用 20 条分页时，账号总数显示 50；各分类与数据库聚合一致。

### P1-5 审查结果和变更文件使用逐行 INSERT

证据：

- repoguard-backend/src/main/java/com/repoguard/agent/worker/ReviewFindingReplacementService.java:29
- repoguard-backend/src/main/java/com/repoguard/agent/worker/ReviewFindingReplacementService.java:32
- repoguard-backend/src/main/java/com/repoguard/agent/worker/ChangedFileReplacementService.java:21
- repoguard-backend/src/main/java/com/repoguard/agent/worker/ChangedFileReplacementService.java:23

现状：删除旧记录后，在事务内对每个 finding/file 单独调用 mapper.insert()。大 PR 会产生 N 次数据库往返，并延长持锁时间；事务并发冲突时整个批次还可能重试。

建议：增加显式 batchInsert SQL（foreach values）或受控的 MyBatis batch executor，按 100~500 条分批；依据字段大小和 max_allowed_packet 调整批次。保留去重和整体事务语义，并记录 batch size、SQL 时长和失败批次。

验收标准：500 文件、1,000 findings 的持久化 SQL 次数从约 1,500 次降到个位数或低两位数；结果数量和事务回滚语义不变。

### P1-6 大表 DDL 不宜直接绑定应用启动

证据：

- repoguard-backend/src/main/resources/application-prod.yml:7
- repoguard-backend/src/main/resources/db/migration/V45__dashboard_normalized_generated_columns.sql:9
- repoguard-backend/src/main/resources/db/migration/V45__dashboard_normalized_generated_columns.sql:129
- repoguard-backend/src/main/resources/db/migration/V45__dashboard_normalized_generated_columns.sql:144
- repoguard-backend/src/main/resources/db/migration/V47__review_task_list_keyset_indexes.sql:9
- repoguard-backend/src/main/resources/db/migration/V54__admin_audit_and_operational_retention.sql:34

现状：生产 profile 默认启用 Flyway；V45 对 review_task/review_finding 连续增加多个 STORED 生成列和索引，V47/V51~V54 继续增加索引。随着审查数据增长，启动时 DDL 可能持有 metadata lock、重建表或超过健康检查窗口。

建议：

1. 发布流程增加独立 migrator 阶段，应用实例不并发争抢迁移。
2. 在生产量级副本上记录每条 DDL 的耗时、锁等待、临时空间和回滚方案。
3. 大表采用 expand/backfill/index/contract 分阶段迁移；必要时使用 MySQL online DDL 或在线变更工具。
4. 对 DDL 设置锁等待上限，发布前检查长事务；失败时停止发布而不是无限重启。

验收标准：使用生产量级数据完成迁移演练，明确最大锁等待和停机窗口；迁移失败不会让所有应用副本同时重启。

### P1-7 应用限流只在单 JVM 内有效

证据：

- repoguard-backend/src/main/java/com/repoguard/agent/security/AuthAttemptLimiter.java:20
- repoguard-backend/src/main/java/com/repoguard/agent/security/AuthAttemptLimiter.java:47
- repoguard-backend/src/main/java/com/repoguard/agent/github/webhook/GithubWebhookRateLimiter.java:20
- repoguard-backend/src/main/java/com/repoguard/agent/github/webhook/GithubWebhookRateLimiter.java:50

现状：认证和 webhook 限流均使用进程内 ConcurrentHashMap，并对整张 map 加 synchronized。多实例时限额按实例数放大；单实例高并发时所有 key 竞争同一把锁。当前 Nginx 还有边缘限流，可以降低风险，但应用层语义仍依赖单实例拓扑。

建议：生产多实例使用 Redis/Bucket4j 或网关集中限流；如果近期保持单实例，则在配置和运行检查中明确限制，并把同步 map 改为按 key 原子计算/有界缓存。统一可信代理解析，后端只信任来自受信代理的 X-Real-IP。

验收标准：两实例压测下总限额不随实例数翻倍；2 万离散 key 压测不出现明显全局锁排队或无界内存增长。

### P1-8 首屏公共依赖和全局 CSS 偏重

证据：

- repoguard-frontend/src/main.ts:3
- repoguard-frontend/src/main.ts:23
- repoguard-frontend/src/main.ts:25
- repoguard-frontend/src/main.ts:44
- repoguard-frontend/dist/index.html（本地构建产物，不提交）

本次构建中，index.html 首屏预加载的 JS/CSS 原始体积约 931KB，gzip 后约 239KB。Element Plus JS 约 287KB、CSS 约 357KB；main.ts 还全局导入所有功能域 CSS，使登录页也承担管理页样式成本。Dashboard 再按路由加载 ECharts core 约 392KB 和 zrender 约 176KB。

建议：

- Element Plus 改为组件级注册和组件样式导入，避免完整 index.css。
- 功能域 CSS 由对应路由页/组件导入，保留真正的全局 token/reset/layout CSS。
- 检查 vendor 手工分包是否造成不必要的首屏 modulepreload；以真实网络 waterfall 决定，而不是只按包名拆分。
- 为首屏 gzip JS/CSS、最大异步 chunk 设置 CI 预算。

验收标准：登录页不下载 review-detail、notification、message-queue 等功能样式；首屏 gzip 体积至少下降 25%，低端设备 LCP/INP 不回退。

### P1-9 消息队列自动刷新可能重叠

证据：

- repoguard-frontend/src/features/message-queue/composables/useMessageQueueHealth.ts:48
- repoguard-frontend/src/features/message-queue/composables/useMessageQueueHealth.ts:104
- repoguard-frontend/src/features/message-queue/composables/useMessageQueueHealth.ts:107

现状：setInterval 每 30 秒直接调用 loadHealth()，没有 loading 守卫、请求取消或 document.visibilityState 判断。请求超过 30 秒时会重叠，旧响应可能覆盖新响应，并重复弹出错误。

建议：改为“请求完成后再 setTimeout”的串行调度；页面隐藏时暂停，恢复时立即刷新；增加 requestSeq/AbortController，并将自动刷新失败展示为页面状态而非每轮 toast。

验收标准：慢接口场景任意时刻最多一个请求；切换后台 5 分钟不持续轮询；恢复页面后只触发一次刷新。

## 六、P2 优化项

### P2-1 图表缺少容器级自适应和可访问名称

证据：

- repoguard-frontend/src/components/EChartPanel.vue:2
- repoguard-frontend/src/components/EChartPanel.vue:28
- repoguard-frontend/src/components/EChartPanel.vue:32
- repoguard-frontend/src/layouts/RepoGuardLayout.vue:14
- repoguard-frontend/src/layouts/RepoGuardLayout.vue:22

现状：图表只监听 window.resize。侧栏收起、父容器尺寸改变不一定触发 window resize，Canvas 可能保持旧宽度。图表容器没有 role/aria-label，两个侧栏图标按钮也缺少稳定的可访问名称。

建议：EChartPanel 使用 ResizeObserver 监听容器，卸载时 disconnect；prop 增加 accessibleLabel/summary，并启用 ECharts aria 或提供数据表替代。图标按钮补 aria-label、type="button" 和 tooltip。

### P2-2 当前“E2E”实际是 JSDOM 路由冒烟

证据：

- repoguard-frontend/package.json:11
- repoguard-frontend/src/e2e/appRouteSmoke.test.ts:26
- repoguard-frontend/src/e2e/appRouteSmoke.test.ts:71

现状：测试验证路由解析和 fetch mock，不会挂载真实页面，也不覆盖浏览器布局、Cookie、CSP、Nginx 代理、图表 Canvas、键盘操作和主要管理流程。

建议：补充真实浏览器验收，至少覆盖登录/刷新 Cookie、任务列表到详情、人工复核、评论发布确认、集成保存失败、用户筛选和移动端布局。遵循仓库约束，本次不提交测试脚本；若要长期纳入 Git 门禁，需要先明确调整“测试脚本不得提交”的治理规则，或将脚本放到独立测试仓库/受控 CI 资产中。

### P2-3 API 契约维护链路过于定制

证据：

- repoguard-frontend/src/api/contracts.ts:142
- repoguard-frontend/src/api/contracts.ts:571
- repoguard-frontend/src/api/apiEnvelope.ts:14
- repoguard-backend/src/test/java/com/repoguard/agent/testsupport/FrontendApiContractCatalog.java:16
- repoguard-backend/src/test/java/com/repoguard/agent/testsupport/FrontendApiContractCatalog.java:37
- repoguard-backend/src/test/java/com/repoguard/agent/testsupport/FrontendApiContractCatalog.java:217

现状：项目已经有很强的 API 快照与前端签名校验，但后端测试通过正则解析 TypeScript 源码，并维护 Java/TypeScript 类型别名表；前端成功响应仍通过泛型断言直接信任 JSON。contracts.ts 格式变化、复杂类型或重命名会增加维护成本。

建议：逐步以标准 OpenAPI 文档作为单一事实来源，在 CI 临时生成 TypeScript client/type 后执行 typecheck 和差异检查；保留少量手写领域适配层。对认证、配置、审查详情等关键响应可增加轻量运行时 schema 校验，至少在开发/灰度环境开启。

### P2-4 构建版本与质量门禁需要收口

证据：

- repoguard-backend/pom.xml:21
- repoguard-backend/pom.xml:137
- repoguard-frontend/package.json:17
- repoguard-frontend/package.json:29
- .nvmrc:1
- .github/workflows/pr-quality.yml:23
- .github/workflows/pr-quality.yml:44

现状：

- Java 目标是 25，CI/Docker 也是 25，但本地可用 JDK 26 直接构建；仓库没有 Maven Wrapper/Enforcer。
- JaCoCo 只生成并上传报告，没有 check 阈值；前端也没有 coverage 门禁。
- Node CI/.nvmrc 为 22，但 @types/node 使用 25。
- 当前工作树中 README 的 Java/Spring Boot 版本描述与 POM/CI/Docker 存在漂移风险，应以构建文件为准自动生成或校验。

建议：增加 Maven Wrapper 和 Enforcer（Java 25、Maven 版本），对错误 JDK fail fast；升级/配置 JaCoCo 以兼容允许的 JDK；从当前基线开始设置渐进式 branch/line 门禁。前端对核心 composable/API 设置覆盖率门禁，并把 @types/node 对齐 Node 22。

### P2-5 容器内重复写文件日志

证据：

- repoguard-backend/src/main/resources/logback-spring.xml:17
- repoguard-backend/src/main/resources/logback-spring.xml:27
- repoguard-backend/src/main/resources/logback-spring.xml:31
- docker-compose.prod.yml:48
- docker-compose.observability.yml:24

现状：后端同时写 console 和 /app/logs/backend 滚动文件，文件上限 2GB；生产 Compose 没有挂载该目录，而 Promtail 通过 Docker socket 抓 stdout。文件日志因此重复消耗 I/O 和容器 writable layer，容器重建后也不可作为可靠存档。

建议：prod profile 只写结构化 console，由容器日志链路采集；本地 profile 才启用 rolling file。若必须保留文件日志，则挂载持久卷并明确轮转、容量告警和采集来源，避免双采集。

### P2-6 未处理异常日志缺少完整诊断链

证据：

- repoguard-backend/src/main/java/com/repoguard/agent/common/GlobalExceptionHandler.java:55
- repoguard-backend/src/main/java/com/repoguard/agent/common/GlobalExceptionHandler.java:57
- repoguard-backend/src/main/java/com/repoguard/agent/common/GlobalExceptionHandler.java:76

现状：统一异常处理只记录异常类型、脱敏消息和第一帧位置，没有把 exception 作为日志参数写入堆栈。安全性较好，但复杂异步/事务异常可能无法还原根因。

建议：响应继续使用安全固定文案；日志增加稳定 errorId。受控生产日志记录异常链和堆栈，同时通过 TurboFilter/专用 converter 对消息、SQL URL、token 等做脱敏。测试环境降低预期异常的日志级别，减少当前超长测试输出。

### P2-7 自建认证授权策略存在多处同步维护成本

证据：

- repoguard-backend/src/main/java/com/repoguard/agent/security/AuthTokenAccessPolicy.java:11
- repoguard-backend/src/main/java/com/repoguard/agent/security/AdminApiKeyAccessPolicy.java:10
- repoguard-backend/src/main/java/com/repoguard/agent/security/RoleAuthorizationInterceptor.java:32
- repoguard-backend/src/test/java/com/repoguard/agent/controller/ControllerAuthorizationContractTest.java:18

现状：新增接口时可能需要同步公开端点表、Admin API Key 端点表、@RequireRole 和测试白名单。现有契约测试已经显著降低遗漏概率，但策略分散仍增加认知负担；公开端点目前还使用 ANY_METHOD，范围比真实 POST 映射更宽。

建议：短期把公开端点改为精确 HTTP method，并增加“Controller 映射与策略表双向一致”测试。长期评估使用 Spring Security 标准 filter chain、Authentication/AuthorizationManager 和方法授权，Admin API Key 作为独立 AuthenticationProvider，减少自定义安全基础设施。

### P2-8 默认 profile 应避免隐式进入 dev,local

证据：

- repoguard-backend/src/main/resources/application.yml:12
- repoguard-backend/src/main/resources/application-dev.yml:4
- docker-compose.prod.yml:58

现状：基础 application.yml 在未设置环境变量时自动启用 dev,local。正式 Compose 显式设置 prod，当前部署路径安全；但直接运行 jar、遗漏环境变量或其他编排方式可能意外使用本地数据库/CORS/默认密钥配置。

建议：基础配置不设置 active profile；本地通过 IDE、脚本或环境文件显式设置 dev,local。非 dev/test 环境缺少 profile、数据库凭据、加密 key、token secret 时启动失败。把生产准出检查放到所有部署入口，而不仅是 Compose。

### P2-9 复杂模块需要按内聚边界收敛，而不是按文件数重构

证据：

- repoguard-backend/src/main/java/com/repoguard/agent/service/impl/DataRetentionServiceImpl.java:70（15 个构造依赖）
- repoguard-backend/src/main/java/com/repoguard/agent/service/impl/DashboardServiceImpl.java:53（12 个构造依赖）
- repoguard-backend/src/main/java/com/repoguard/agent/service/impl/AuthServiceImpl.java:62（9 个构造依赖）
- repoguard-backend/src/main/java/com/repoguard/agent/observability/RepoGuardMetrics.java:19（约 372 行）

现状：后端 653 个生产类中存在较多小型 helper，同时少数 orchestrator 依赖很多组件。细粒度设计提升了单测能力，但跨包跳转、构造器装配和修改影响面开始增加。

建议：先用包依赖/变更耦合数据识别真实边界，再处理：

- Data Retention 分为 candidate query、archive writer、delete executor、audit/lease 四个稳定端口。
- Dashboard 按 overview/quality/snapshot 分 facade，避免一个服务注入所有 assembler。
- RepoGuardMetrics 按 review/github/rabbit/frontend 域拆 recorder，但共享统一 tag policy。
- 增加架构规则限制 controller 直连 mapper、领域包环依赖和跨域 impl 引用。

不要把只被一个调用点使用、没有独立策略的微型类继续拆小，也不要仅为了减少构造参数引入“依赖大对象”掩盖耦合。

### P2-10 供应链治理仍可加强

现状亮点：生产镜像按 digest 固定，release workflow 已包含 npm audit、SBOM 和 Trivy，PR 也有 dependency review。

待优化：

- checkout/setup-java/setup-node/github-script/dependency-review 等 Action 多数仍使用浮动主版本标签。
- 没有看到 Dependabot/Renovate 配置，基础镜像和 Maven/npm 依赖升级主要依赖人工。
- npm audit 只在 release 检查生产依赖；Maven 现存依赖的持续漏洞扫描不够直观。

建议：关键 Action 固定 commit SHA 并由自动化更新；启用 Dependabot/Renovate 分组升级；增加 Maven/文件系统 SCA，对 high/critical 建立例外到期机制，避免只在发布阶段发现问题。

### P2-11 前端文案和错误状态需统一

证据：

- repoguard-frontend/src/features/integrations/composables/useIntegrationConfigPersistence.ts:82
- repoguard-frontend/src/features/integrations/composables/useIntegrationConfigPersistence.ts:101
- repoguard-frontend/src/features/review-detail/composables/useReviewDetailLoader.ts:134

现状：主要界面为中文，但部分配置保存、自动刷新错误仍是英文。错误有的使用页面 alert，有的只用 toast，自动任务失败可能重复提示。

建议：集中维护用户可见文案和错误展示策略；加载失败保留可重试的页面状态，短操作结果使用 toast，后台轮询失败使用非打断式 banner。后端错误 code 映射为稳定中文文案，避免直接展示内部异常。

## 七、建议实施顺序

阶段 A：一致性与错误展示（1~3 天）

1. 修复通知手动重试：状态白名单、CAS claim、after-commit、并发/回滚验证。
2. 修复任务详情和用户搜索乱序响应。
3. 修正用户统计口径。
4. 集成配置先改为按卡片独立保存；若必须原子保存，再设计批量 API。

阶段 B：性能与发布安全（3~7 天）

1. findings/changed files 批量写入并做生产量级基准。
2. Element Plus/CSS 按需加载，建立 bundle budget。
3. 串行化自动刷新并加入页面可见性控制。
4. 建立独立数据库迁移阶段和大表 DDL 演练。

阶段 C：工程治理（1~2 个迭代）

1. Maven Wrapper/Enforcer、JDK/Node 类型版本对齐、覆盖率门禁。
2. 标准 OpenAPI 生成链路逐步替换正则解析。
3. 生产 console-only 结构化日志和 errorId/安全堆栈。
4. 分布式限流、标准 Spring Security、依赖自动更新按扩容计划推进。
5. 建立真实浏览器验收方案；如需把脚本纳入仓库，先调整测试脚本提交约束。

## 八、不建议立即做的事项

- 不建议重写认证系统。当前访问令牌、刷新 Cookie、CSRF、sessionVersion 和刷新令牌 CAS 已有较完整防护；应先降低策略分散成本。
- 不建议为了“类太多”批量合并所有 helper。应以变更耦合、调用关系和性能数据为依据。
- 不建议先升级全部依赖再修一致性问题。Spring Boot/Java 主版本较新，优先稳定当前行为和构建基线。
- 不建议把所有查询改成缓存。任务详情、通知状态和配置页对一致性要求高，应先定位真实慢 SQL。
- 不建议只追求覆盖率百分比。P0 的并发、回滚、重复副作用测试比继续增加简单 getter/mapper 测试更重要。

## 九、完成定义

本轮优化可视为完成，应至少满足：

- 通知重试并发与事务测试通过，无法重复发布已完成事件。
- 集成配置不再静默部分保存。
- 前端路由/筛选乱序测试通过，统计口径与服务端一致。
- 大 PR 批量持久化基准达标，迁移在生产量级副本完成演练。
- 首屏 gzip 预算、Java/Node 版本和 JaCoCo coverage check 成为 CI 门禁。
- 生产日志只有一个权威采集路径，异常可通过 traceId/errorId 定位。
- 所有新增验证资产遵守仓库的 Git 提交约束。


## 十、增量复审记录

复审时间：2026-07-15 16:39:03 +08:00（Asia/Shanghai）
复审分支：PRAgent-test
复审性质：在 2026-07-13 全量审查基础上的增量复核；本节保留原报告，不覆盖历史数据。

10.1 复审结论

项目整体工程质量依然较好，安全基线、任务状态机、数据库迁移、CI 和自动化测试比较完整，不需要大规模重构。本轮确认首轮报告中的通知重试、前端异步竞态和部分保存问题仍然存在，并补充发现通知投递领取、数据清理事务完成时序、最后一个管理员并发保护三个重要的一致性问题。

当前优先级建议调整为：

1. P0：通知手动重试的数据库事务与 RabbitMQ 发布一致性。
2. P1：通知投递缺少原子领取，可能重复调用外部渠道。
3. P1：数据清理成功审计和租约释放早于主事务完成。
4. P1：最后一个有效管理员保护可被并发操作绕过。
5. P1：前端统计口径、异步请求乱序和集成配置部分保存。
6. P2：登录回跳、工具链可复现性、覆盖率门禁和渐进式模块收敛。

10.2 新增重点问题

P1-A 通知投递需要原子领取和完成栅栏

证据：

- repoguard-backend/src/main/java/com/repoguard/agent/notification/NotificationDeliveryWorker.java:100-110
- repoguard-backend/src/main/java/com/repoguard/agent/notification/NotificationDeliverableEventQuery.java:17-27
- repoguard-backend/src/main/java/com/repoguard/agent/notification/NotificationDeliveryEventStateUpdater.java:27-35
- repoguard-backend/src/main/java/com/repoguard/agent/notification/NotificationBindingDeliveryService.java:37-43
- repoguard-backend/src/main/resources/db/migration/V26__create_notification_delivery_logs.sql:42-59

现状：消费者先读取事件，再把状态改为 DELIVERING，随后调用外部渠道。状态更新只排除 DELIVERED，没有限定必须从 PUBLISHED/DELIVERY_FAILED 等状态迁移，也没有检查数据库影响行数。DELIVERING 仍会被查询层视为可投递。投递日志在“检查成功记录”和“发送”之间也存在时间窗口，数据库没有可承担并发幂等语义的唯一约束。

风险：RabbitMQ 的至少一次投递、消费者并发或消息重复均可能让两个执行者同时发送同一通知。即使事后只有一条成功日志，外部 webhook、邮件等副作用已经发生。

建议：

1. 使用条件更新原子领取：仅允许 PUBLISHED/DELIVERY_FAILED -> DELIVERING，并要求影响行数等于 1。
2. 增加 claimed_at、claimed_by 和领取租约，支持宕机后的超时回收。
3. 完成/失败更新携带领取令牌，拒绝过期执行者覆盖新执行者的结果。
4. 明确渠道幂等键和投递日志唯一语义；不能做到严格 exactly-once 的渠道，至少要阻止应用内部并发重复发送。
5. 增加重复消息、双消费者、执行者超时恢复的真实并发集成测试。

P1-B 数据清理审计与租约生命周期早于主事务完成

证据：

- repoguard-backend/src/main/java/com/repoguard/agent/service/impl/DataRetentionServiceImpl.java:108-129
- repoguard-backend/src/main/java/com/repoguard/agent/service/impl/DataRetentionServiceImpl.java:211-234
- repoguard-backend/src/main/java/com/repoguard/agent/service/impl/DataRetentionServiceImpl.java:378-380
- repoguard-backend/src/main/java/com/repoguard/agent/retention/DataRetentionCleanupAuditRecorder.java:57
- repoguard-backend/src/main/java/com/repoguard/agent/retention/DataRetentionCleanupLeaseStore.java:30-45

现状：cleanup() 的外层事务尚未由 Spring 代理提交时，成功审计已经通过 REQUIRES_NEW 独立提交；finally 中的租约释放也在外层事务真正完成前提交。

风险：外层事务在提交阶段失败时，审计仍显示 COMPLETED；由于提交异常发生在方法返回之后，方法内 catch 无法可靠记录失败。租约提前释放还可能让另一实例在前一事务提交窗口内开始下一轮清理。

建议：通过 TransactionSynchronization 在 afterCommit 中记录成功，在 afterCompletion 中记录失败并释放租约；或者将事务边界移动到独立执行器 bean，使调度层能够在事务代理返回后再处理审计和租约。补充强制提交失败及双实例争抢租约的 MySQL 集成测试。

P1-C 最后一个有效管理员不变量需要数据库级串行化

证据：

- repoguard-backend/src/main/java/com/repoguard/agent/service/impl/UserManagementServiceImpl.java:144-186
- repoguard-backend/src/main/java/com/repoguard/agent/service/impl/UserManagementServiceImpl.java:197-204

现状：角色降级或账号禁用前，代码通过普通 selectCount 检查是否还有其他 ACTIVE ADMIN，之后再更新目标用户。检查与更新处于同一业务事务，但没有行锁、守卫记录或乐观版本来串行化这个跨行不变量。

风险：两个管理员并发降级或禁用时，都可能在检查阶段看到对方仍为有效管理员并通过校验，随后同时提交，最终系统中没有有效管理员。

建议：通过固定管理员守卫行 SELECT ... FOR UPDATE，或按固定顺序锁定有效管理员集合，把检查和修改置于同一锁保护范围；角色和状态两个入口必须共用同一保护机制。使用真实 MySQL 并发测试验证两次操作中必须有一次被拒绝。

10.3 首轮问题复核结果

1. 通知手动重试问题仍存在。retryEvent() 仍在数据库事务提交前直接调用 publishExistingEvent()，且缺少可重试状态白名单、CAS 抢占和影响行数判断。应复用现有 NotificationEventPublishCoordinator.publishAfterCommit()。
2. 用户管理统计口径问题仍存在。UserManagementPage.vue:249-258 的总数、启用数、管理员数和禁用数均从当前页 users 数组计算；总数应使用 usersTotal，其他指标应由聚合接口提供或明确标记为“本页统计”。
3. 用户搜索请求竞态仍存在。UserManagementPage.vue:454-457 每次输入立即加载，没有防抖、AbortController 或请求序号，旧响应可能覆盖新结果。项目现有 useReviewTasksList.ts 已有 debounce 和 taskRequestSeq，可复用同一模式。
4. 任务详情加载竞态仍存在。useReviewDetailLoader.ts 写入 selectedTask 前没有校验当前路由 id 或请求序号，快速切换任务时旧响应可能覆盖新页面。
5. 集成配置部分保存问题仍存在。useIntegrationConfigPersistence.ts:88-105 通过 Promise.all 并行执行四个独立 PUT；一个失败不会回滚其他成功项，但界面只反馈整体失败。
6. 登录回跳仍未闭环。LoginPage.vue 登录成功后固定进入概览页，没有消费路由守卫保存的 redirect；应只接受经过校验的站内 /repoguard/... 路径，防止开放重定向。

10.4 本轮验证基线

- 后端 mvn test：1,595 个测试，0 failure，0 error，2 skipped。
- JaCoCo 汇总：instruction 约 90.25%，branch 约 72.18%。
- 前端 vue-tsc：通过。
- 前端 ESLint：通过。
- 前端 Vitest：17 个测试文件、61 个测试全部通过。
- 前端 Vite 生产构建：通过，共转换 3,327 个模块。
- git diff --check：通过。

工具链限制：本机实际运行 JDK 26.0.1，而项目 POM、CI 和 Docker 目标为 Java 25。JaCoCo 0.8.13 对 major version 70 的 JDK 类和部分动态类产生大量插桩异常，Maven 最终仍报告成功。因此覆盖率数值仅作趋势参考，不能视为完全干净的 Java 25 基线。

建议使用 Maven Enforcer 或 Maven Toolchains 将本地和 CI 强制到 Java 25，并在兼容升级 JaCoCo 后增加适度门禁，例如 branch 不低于 70%、instruction/line 不低于当前稳定基线，再逐步提高。仓库当前只有覆盖率报告，没有阻断回退的 check 规则。

10.5 前端构建与维护建议

生产构建已完成路由拆分，但公共依赖仍较大：Element Plus JavaScript 约 287.32 KB（gzip 94.20 KB）、Element Plus CSS 约 357.06 KB（gzip 47.77 KB）、ECharts core 约 392.41 KB（gzip 135.96 KB）、zrender 约 175.65 KB（gzip 58.45 KB）。建议先建立首屏和路由 bundle budget，再按测量结果进行 Element Plus 组件/样式按需加载与图表延迟加载，不建议盲目拆包。

ReviewDetailPage.vue、contracts.ts 和 AuthServiceImpl 等文件承担了较多职责，但项目已有子组件和 composable 基础。建议按变更频率和领域边界渐进拆分，例如把 findings、changed files、missing files、timeline 的分页加载器移入独立 composable；不要仅为降低文件行数进行广泛重写。

10.6 更新后的实施顺序与验收标准

阶段一：事务与并发一致性

1. 通知手动重试改为状态白名单、CAS 和提交后发布。
2. 通知消费者增加原子领取、领取租约和完成栅栏。
3. 数据清理审计与租约迁移到事务完成回调。
4. 管理员角色/状态修改增加数据库级串行化。

验收：重复消息和并发重试不能产生应用内部重复投递；清理事务回滚时不能留下 COMPLETED 审计；两个管理员并发降级时至少有一个操作被拒绝。

阶段二：前端正确性

1. 修正用户统计口径。
2. 用户搜索增加 300~400ms 防抖和最新请求保护。
3. 任务详情加载校验路由 id/请求序号。
4. 集成配置改为逐卡片保存或显示每项保存结果；若产品承诺原子保存，则实现后端批量事务接口。
5. 登录成功后安全恢复站内 redirect。

验收：延迟响应测试中旧请求不能覆盖新状态；统计值与服务端全量数据一致；部分保存必须明确展示并重新同步服务端状态。

阶段三：构建与维护治理

1. 强制 Java 25 工具链，消除 JaCoCo class-version 噪声并增加覆盖率回退门禁。
2. 建立前端 bundle budget，再按数据优化公共依赖。
3. 按领域和变更耦合渐进收敛大型页面/服务，不做全量重构。
4. 如需引入浏览器端到端验证，先与“测试相关脚本不得提交 Git”的仓库约束协调，或将验证资产放在获准的外部 CI 位置。

本次文档更新仅记录复审结果，不修改业务代码、测试文件或 README.md，也不执行 Git 暂存和提交。


## 十一、优化实施进度

## 进度记录 01

完成时间：2026-07-15 16:52:53 +08:00（Asia/Shanghai）
优化项：P0 通知手动重试的状态白名单、并发 CAS 与事务提交后发布
状态：已完成，准备随本记录提交并推送至 PRAgent-test。

实施内容：

1. 手动重试只允许 PUBLISH_FAILED、DELIVERY_FAILED、DEAD 三种失败状态，拒绝 PENDING、PUBLISHED、DELIVERING、DELIVERED 和未知状态。
2. 使用单条条件更新完成失败状态到 PENDING 的迁移，并要求 publish_claimed_at 为空及影响行数等于 1；并发请求或补偿 worker 已领取时，后续请求被拒绝。
3. 成功领取后重置 retry_count、next_retry_at、last_error 和历史发布领取信息。
4. publishExistingEvent() 改为调用 publishAfterCommit()；存在事务时仅注册提交后回调，数据库回滚时不会发布 RabbitMQ 消息，无事务时保持立即发布行为。
5. 更新现有单元测试，覆盖成功重试、已完成事件拒绝、并发状态变化拒绝、已发布事件跳过和待发布事件提交后发布。

涉及文件：

- repoguard-backend/src/main/java/com/repoguard/agent/service/impl/NotificationEventQueryServiceImpl.java
- repoguard-backend/src/main/java/com/repoguard/agent/notification/NotificationDispatchServiceImpl.java
- repoguard-backend/src/test/java/com/repoguard/agent/service/impl/NotificationEventQueryServiceImplTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/notification/NotificationDispatchServiceImplTest.java

验证结果：

- 定向测试：NotificationEventQueryServiceImplTest、NotificationDispatchServiceImplTest、NotificationEventPublishCoordinatorTest，共 11 个测试，0 failure，0 error。
- 后端全量测试：1,598 个测试，0 failure，0 error，2 skipped，BUILD SUCCESS。
- git diff --check：通过。
- 已知环境提示：本机 JDK 26 与 JaCoCo 0.8.13 仍产生 major version 70 插桩告警，但测试进程退出码为 0，应用测试全部通过；该工具链问题按后续 P2 项处理。

验收结论：手动重试已经由“无条件更新并在事务内直接发布”调整为“失败状态 CAS 领取并在事务提交后发布”，本项达到完成标准。


## 进度记录 02

完成时间：2026-07-15 17:10:49 +08:00（Asia/Shanghai）
优化项：P1 通知投递原子领取、领取租约与完成栅栏
状态：已完成，准备随本记录提交并推送至 PRAgent-test。

实施内容：

1. 新增 delivery_claimed_at、delivery_claimed_by 字段和联合查询索引，Flyway 迁移版本为 V55。
2. 消费者只接受 PUBLISHED 事件，并通过单条条件更新完成 PUBLISHED -> DELIVERING；只有影响一行的消费者取得投递所有权。
3. 为每个应用实例生成独立领取标识，领取时间和领取者写入数据库；重复消息或并发消费者丢失 CAS 后直接跳过外部发送。
4. DELIVERED、DELIVERY_FAILED、DEAD 等完成状态更新均携带 eventId、DELIVERING 状态、delivery_claimed_at 和 delivery_claimed_by 条件，过期执行者不能覆盖新执行者结果。
5. 完成或失败后清理投递领取字段；重新发布和手动重试路径也会清理历史投递领取信息。
6. 新增超时领取补偿任务。默认领取租约 300000ms，每 60000ms 扫描最多 20 条过期 DELIVERING 事件，通过同一所有权栅栏转为 DELIVERY_FAILED 或 DEAD，再由现有发布补偿链路按退避时间重新投递。
7. 保留渠道级成功记录检查。事件级 CAS 已消除应用内部两个消费者同时进入同一绑定发送窗口；外部渠道仍建议在后续支持幂等键时传递 eventId/bindingId。

新增配置：

- REPOGUARD_NOTIFICATION_DELIVERY_CLAIM_LEASE_MS，默认 300000。
- REPOGUARD_NOTIFICATION_DELIVERY_RECOVERY_INTERVAL_MS，默认 60000。
- REPOGUARD_NOTIFICATION_DELIVERY_RECOVERY_BATCH_SIZE，默认 20，代码限制在 1~1000。

主要涉及文件：

- repoguard-backend/src/main/resources/db/migration/V55__notification_delivery_claim.sql
- repoguard-backend/src/main/java/com/repoguard/agent/entity/NotificationEvent.java
- repoguard-backend/src/main/java/com/repoguard/agent/notification/NotificationDeliveryClaim.java
- repoguard-backend/src/main/java/com/repoguard/agent/notification/NotificationDeliveryClaimService.java
- repoguard-backend/src/main/java/com/repoguard/agent/notification/NotificationDeliveryEventStateUpdater.java
- repoguard-backend/src/main/java/com/repoguard/agent/notification/NotificationDeliveryRecoveryCompensator.java
- repoguard-backend/src/main/java/com/repoguard/agent/notification/NotificationDeliveryWorker.java

验证结果：

- 生产代码编译：BUILD SUCCESS，657 个生产类。
- 定向测试：领取、恢复、worker、状态更新、完成服务、发布状态、手动重试及 Spring 构造器共 27 个测试，0 failure，0 error。
- 后端全量测试：1,601 个测试，0 failure，0 error，2 skipped，BUILD SUCCESS。
- git diff --check：通过。
- 本机 JDK 26/JaCoCo 0.8.13 的 major version 70 告警仍存在，不影响测试退出结果，继续留待 P2 工具链项处理。

验收结论：同一通知事件只有取得数据库领取所有权的消费者能够调用外部渠道；完成更新受领取标识保护，消费者崩溃后可通过租约超时恢复，本项达到完成标准。


## 进度记录 03

完成时间：2026-07-15 17:16:52 +08:00（Asia/Shanghai）
优化项：P1 数据清理审计及租约的事务完成时序
状态：已完成，准备随本记录提交并推送至 PRAgent-test。

实施内容：

1. 清理响应生成后不再立即提交 COMPLETED 审计；存在真实 Spring 事务时，在 afterCommit 回调中通过现有 REQUIRES_NEW 审计事务记录完成状态和成功指标。
2. 如果清理逻辑已经返回、但外层数据库事务在提交阶段回滚或提交失败，afterCompletion 会把审计记录更新为 FAILED，并记录失败指标，避免“数据未清理但审计显示完成”。
3. 数据库清理租约不再在业务方法 finally 中提前释放；存在真实事务时，在 afterCompletion 中通过 REQUIRES_NEW 释放。
4. 进程内 ReentrantLock 与数据库租约在同一事务完成回调中释放，避免同一实例在数据库提交窗口内启动下一轮清理。
5. 参数校验失败、租约获取失败以及没有事务同步的直接调用继续使用同步释放兜底，避免锁泄漏并保持单元测试和非代理调用的确定性。
6. 清理主体内部发生异常时，原有 FAILED 审计和失败指标行为保持不变；外层事务完成后再释放租约。

涉及文件：

- repoguard-backend/src/main/java/com/repoguard/agent/service/impl/DataRetentionServiceImpl.java
- repoguard-backend/src/test/java/com/repoguard/agent/service/impl/DataRetentionServiceImplTest.java

验证结果：

- 定向测试：DataRetentionServiceImplTest、DataRetentionCleanupAuditRecorderTest、DataRetentionCleanupLeaseStoreTest、SpringBeanConstructorSelectionTest，共 33 个测试，0 failure，0 error。
- 新增事务同步测试覆盖提交成功和提交回滚两条路径，并验证审计、指标及租约释放的先后关系。
- 后端全量测试：1,603 个测试，0 failure，0 error，2 skipped，BUILD SUCCESS。
- git diff --check：通过。
- 本机 JDK 26/JaCoCo 0.8.13 插桩告警继续留待 P2 工具链项处理。

验收结论：成功审计和成功指标只会在清理事务真正提交后产生；提交失败会留下 FAILED 审计；数据库租约及进程锁在事务完成后才释放，本项达到完成标准。


## 进度记录 04

完成时间：2026-07-15 17:22:28 +08:00（Asia/Shanghai）
优化项：P1 最后一个有效管理员的并发保护
状态：已完成，准备随本记录提交并推送至 PRAgent-test。

实施内容：

1. 新增 user_management_guard 单例守卫表和 active_admin 守卫行，Flyway 迁移版本为 V56。
2. UserAccountMapper 新增 SELECT ... FOR UPDATE 锁方法，对 active_admin 守卫行获取数据库排他锁。
3. 所有请求角色降级为 VIEWER 的事务，在读取目标用户和统计管理员之前先获取守卫锁。
4. 所有请求禁用账号的事务使用同一守卫锁，因此“角色降级”和“账号禁用”两个入口之间也会串行执行。
5. 提升为 ADMIN、重新启用账号等不会减少有效管理员数量的操作不获取守卫锁，避免扩大锁竞争范围。
6. 守卫行缺失时立即返回内部错误并停止读取或修改用户，防止迁移异常时退化为无保护更新。

并发保证：

- 两个实例并发降级/禁用不同管理员时，第二个事务会在守卫行等待。
- 第二个事务取得锁后才创建一致性读取视图，并重新统计已提交的有效管理员数量。
- 如果第一个事务已经移除倒数第二个有效管理员，第二个事务会看到计数为 0 并拒绝操作，系统至少保留一个 ACTIVE ADMIN。

涉及文件：

- repoguard-backend/src/main/resources/db/migration/V56__active_admin_invariant_guard.sql
- repoguard-backend/src/main/java/com/repoguard/agent/mapper/UserAccountMapper.java
- repoguard-backend/src/main/java/com/repoguard/agent/service/impl/UserManagementServiceImpl.java
- repoguard-backend/src/test/java/com/repoguard/agent/service/impl/UserManagementServiceImplTest.java

验证结果：

- 定向测试：UserManagementServiceImplTest、UserManagementControllerTest，共 17 个测试，0 failure，0 error。
- 测试验证数据库守卫锁先于 selectById、管理员计数和 updateById；非危险的重新启用操作不获取锁；守卫缺失时不读取用户。
- 后端全量测试：1,604 个测试，0 failure，0 error，2 skipped，BUILD SUCCESS。
- git diff --check：通过。
- 首次定向测试因沙箱无法访问本地 Maven 父 POM/中央仓库而失败，获准访问依赖缓存后测试通过；该失败与代码无关。

验收结论：最后一个有效管理员不变量已由普通计数检查升级为数据库守卫行串行化，可覆盖同实例和多实例并发操作，本项达到完成标准。


## 进度记录 05

完成时间：2026-07-15 17:27:34 +08:00（Asia/Shanghai）
优化项：P1 用户管理统计口径
状态：已完成，准备随本记录提交并推送至 PRAgent-test。

实施内容：

1. 用户总数不再使用当前页 users.length，而是使用服务端分页响应的 total。
2. 存在角色、状态或关键词筛选时，总量卡片显示“匹配账号数”和“当前筛选结果”，避免把筛选后的 total 表述为系统全量。
3. 启用、管理员和禁用三个分类仍基于当前页数据时，明确标记为“本页启用”“本页管理员”“本页禁用”，并显示当前页账号数量。
4. 表格直接展示服务端已经按角色、状态和关键词筛选的 users，移除重复的本地二次过滤，避免当前页数据再次被不同口径裁剪。
5. 指标计算提取为纯函数 buildUserManagementMetrics，便于独立验证服务端总量和当前页分项口径。

涉及文件：

- repoguard-frontend/src/pages/UserManagementPage.vue
- repoguard-frontend/src/features/user-management/userManagementMetrics.ts
- repoguard-frontend/src/features/user-management/userManagementMetrics.test.ts

验证结果：

- 新增指标单元测试：2 个测试通过，覆盖未筛选总量和筛选结果标签。
- 前端 TypeScript：通过。
- 前端 ESLint：通过。
- 前端 Vitest：18 个测试文件、63 个测试全部通过。
- 前端 Vite 生产构建：通过，共转换 3,328 个模块。
- git diff --check：通过。

验收结论：用户管理页不再把当前页数量展示为系统总数，所有分页统计的范围都有明确标签，本项达到完成标准。


## 进度记录 06

完成时间：2026-07-15 17:32:11 +08:00（Asia/Shanghai）
优化项：P1 用户搜索请求风暴和乱序响应覆盖
状态：已完成，准备随本记录提交并推送至 PRAgent-test。

实施内容：

1. 用户关键词搜索增加 350ms 防抖，连续输入只触发停止输入后的最终请求。
2. 角色和状态选择仍立即加载；触发时会取消尚未执行的关键词防抖计时器，避免旧条件随后补发请求。
3. 用户列表请求通过 createLatestOnlyLoader 分配递增序号，只有最新请求可以写入 users 和 usersTotal。
4. 较早请求晚于新请求返回时，其数据被忽略；较早请求晚到的异常也被抑制，不会覆盖或干扰最新页面状态。
5. 组件卸载时清理防抖计时器并使所有在途请求失效，避免卸载后写入响应式状态。
6. 筛选触发的请求失败会显示稳定的用户列表加载错误信息，不再形成未处理 Promise rejection。

涉及文件：

- repoguard-frontend/src/pages/UserManagementPage.vue
- repoguard-frontend/src/features/user-management/latestOnlyLoader.ts
- repoguard-frontend/src/features/user-management/latestOnlyLoader.test.ts

验证结果：

- 新增 latest-only loader 单元测试：2 个测试通过，覆盖旧响应晚到、旧异常和卸载取消。
- 前端 TypeScript：通过。
- 前端 ESLint：通过。
- 前端 Vitest：19 个测试文件、65 个测试全部通过。
- 前端 Vite 生产构建：通过，共转换 3,329 个模块。
- git diff --check：通过。

验收结论：快速输入不会产生逐字符请求风暴，旧请求无法覆盖最新筛选结果，组件卸载后不会写回状态，本项达到完成标准。

## 进度记录 07

完成时间：2026-07-15 17:40:10 +08:00（Asia/Shanghai）
优化项：P1 任务详情路由切换的乱序响应覆盖
状态：已完成，准备随本记录提交并推送至 PRAgent-test。

实施内容：

1. 详情加载和状态轮询使用同一个递增请求序号，只有最新请求可以写入任务详情、错误、轮询计数和刷新时间。
2. 路由切换触发新详情请求后，旧任务即使更晚返回，也不会覆盖当前路由对应的任务。
3. 旧请求的异常被抑制，不会清空新任务、显示过期错误或干扰最新轮询状态。
4. loading 与 silentRefreshing 仅由最新请求收尾，旧请求完成时不会提前结束新请求的加载状态。
5. 页面卸载时主动使所有在途详情请求失效并清理加载状态，避免卸载后继续写入响应式数据。

涉及文件：

- repoguard-frontend/src/features/review-detail/composables/useReviewDetailLoader.ts
- repoguard-frontend/src/features/review-detail/composables/useReviewDetailLoader.test.ts
- repoguard-frontend/src/pages/ReviewDetailPage.vue

验证结果：

- 新增详情加载器单元测试：2 个测试通过，覆盖旧响应晚到、旧异常和卸载取消。
- 前端 TypeScript：通过。
- 前端 ESLint：通过。
- 前端 Vitest：20 个测试文件、67 个测试全部通过。
- 前端 Vite 生产构建：通过，共转换 3,329 个模块。
- git diff --check：通过。

验收结论：路由快速切换时，旧任务响应、旧错误和旧加载收尾均无法覆盖当前任务状态，页面卸载后也不会写回详情状态，本项达到完成标准。

## 进度记录 08

完成时间：2026-07-15 17:45:47 +08:00（Asia/Shanghai）
优化项：P1 集成配置部分保存的逐项反馈与服务端重同步
状态：已完成，准备随本记录提交并推送至 PRAgent-test。

实施内容：

1. 四项配置加载由 Promise.all 改为 Promise.allSettled，单项失败不再阻止其他可用配置展示。
2. “保存全部”对 GitHub、MySQL、RabbitMQ、Review Policy 四个独立请求逐项结算，反馈中明确列出成功项、失败项及失败原因。
3. 四项均成功时统一应用服务端响应，并显示包含全部成功项名称的完成消息。
4. 任一保存请求失败时，自动重新拉取四项服务端配置，确保页面最终展示数据库中的实际状态，而不是保留可能失真的本地表单状态。
5. 重同步本身出现单项失败时，错误消息继续列出无法同步的配置，避免把未知状态误报为已恢复一致。

涉及文件：

- repoguard-frontend/src/features/integrations/composables/useIntegrationConfigPersistence.ts
- repoguard-frontend/src/features/integrations/composables/useIntegrationConfigPersistence.test.ts

验证结果：

- 新增集成配置持久化单元测试：2 个测试通过，覆盖单项保存失败后的逐项反馈、全量重同步和部分加载。
- 前端 TypeScript：通过。
- 前端 ESLint：通过。
- 前端 Vitest：21 个测试文件、69 个测试全部通过。
- 前端 Vite 生产构建：通过，共转换 3,329 个模块。
- git diff --check：通过。

验收结论：独立 PUT 发生部分成功时，用户可以看到每项结果，页面会主动重新同步服务端状态，不再出现静默部分保存，本项达到完成标准。

## 进度记录 09

完成时间：2026-07-15 18:14:46 +08:00（Asia/Shanghai）
优化项：GitHub Actions 前端运行镜像 HIGH 漏洞修复
状态：已完成，Release Images 验证通过，准备推送本进度记录至 PRAgent-test。

问题根因：

1. `Release Images / Build and Push Images` 在前端镜像 Trivy 扫描步骤失败。
2. 原运行镜像 `nginxinc/nginx-unprivileged:1.30.3-alpine3.23` 包含 curl/libcurl 8.19.0-r0。
3. Trivy 检出 CVE-2026-5773 和 CVE-2026-6276；curl 与 libcurl 合计 4 个 HIGH 漏洞，固定修复版本为 8.20.0-r0。

实施内容：

1. 保持 NGINX 稳定版本 1.30.3 不变，将前端运行镜像切换为官方 `1.30.3-alpine-slim` 变体。
2. 固定官方多架构镜像 digest `sha256:3b24c4bfb2b9f60359b1475605ca1c8ed6e4963eb8369c6835be4d96bdb3ea81`，避免使用可漂移标签。
3. 项目 Nginx 配置未使用完整版镜像中的 GeoIP、NJS、XSLT、image-filter 等动态模块；slim 变体满足静态文件服务、反向代理、envsubst 和非 root 运行要求。
4. 没有在项目镜像层临时执行 `apk upgrade`，避免引入 root 切换和依赖仓库时间漂移；通过移除不需要的 curl/libcurl 及动态模块缩小攻击面。

涉及文件：

- repoguard-frontend/Dockerfile

验证结果：

- 前端 TypeScript：通过。
- 前端 ESLint：通过。
- 前端 Vitest：21 个测试文件、69 个测试全部通过。
- 前端 Vite 生产构建：通过，共转换 3,329 个模块。
- GitHub Actions `Release Images` run 29407163130：成功。
- Actions 中后端镜像构建与 Trivy HIGH/CRITICAL 扫描：通过。
- Actions 中前端 slim 镜像构建、推送与 Trivy HIGH/CRITICAL 扫描：通过。
- git diff --check：通过。

验收结论：前端运行镜像已移除本项目不需要的 curl/libcurl 依赖和动态模块，原 4 个 HIGH 漏洞不再出现，Release Images 恢复成功，本项达到完成标准。

## 进度记录 10

完成时间：2026-07-15 18:25:31 +08:00（Asia/Shanghai）
优化项：P2 登录成功后的安全站内回跳
状态：已完成，准备随本记录提交并推送至 PRAgent-test。

实施内容：

1. 登录和注册成功后不再固定跳转总览，而是消费路由守卫写入的 redirect 参数，并使用 replace 避免登录页残留在浏览器返回历史中。
2. 新增统一的回跳解析器，只允许当前站点下 `/repoguard` 或 `/repoguard/...` 路径，同时保留合法查询参数和 hash。
3. 绝对外部 URL、协议相对 URL、非 RepoGuard 路径、路径穿越、反斜杠、控制字符、脚本协议和重复 query 值全部回退到 `/repoguard/overview`。
4. 已登录用户直接访问带 redirect 的登录页时，路由守卫复用同一个安全解析器，避免出现两套校验口径。

涉及文件：

- repoguard-frontend/src/router/authRedirect.ts
- repoguard-frontend/src/router/authRedirect.test.ts
- repoguard-frontend/src/router/index.ts
- repoguard-frontend/src/pages/LoginPage.vue
- repoguard-frontend/src/e2e/appRouteSmoke.test.ts

验证结果：

- 安全回跳与路由烟雾定向测试：2 个测试文件、13 个测试全部通过。
- 前端 TypeScript：通过。
- 前端 ESLint：通过。
- 前端 Vitest：22 个测试文件、79 个测试全部通过。
- 前端 Vite 生产构建：通过，共转换 3,330 个模块。
- git diff --check：通过。

验收结论：认证成功后可以恢复原站内页面，同时所有外站和异常回跳输入均被安全降级到总览页，不形成开放重定向，本项达到完成标准。

## 进度记录 11

完成时间：2026-07-15 18:32:53 +08:00（Asia/Shanghai）
优化项：P2 Java/Node 工具链与后端覆盖率回退门禁
状态：已完成，准备随本记录提交并推送至 PRAgent-test，由 CI 的 JDK 25 环境复核默认构建路径。

实施内容：

1. 新增 Maven Enforcer 3.6.3，在 validate 阶段要求 JDK `[25,26)` 和 Maven `[3.9.9,4.0.0)`，错误工具链会在编译和测试前快速失败。
2. JaCoCo 从 0.8.13 升级到 0.8.15；该版本正式支持 Java 26，消除开发机 JDK 26 对 major version 70 的插桩异常。
3. JaCoCo verify 阶段增加 BUNDLE 覆盖率 check：line 不低于 89%，branch 不低于 70%；PR 全量后端 Job 执行 `mvn verify`，定向集成 Job 保持 `mvn test`，避免用局部测试覆盖率误判全局基线。
4. 前端 `@types/node` 从 25.x 对齐到 Node 22 的 22.20.1，并同步更新 package-lock.json；与 `.nvmrc` 和 CI Node 22 保持一致。

涉及文件：

- repoguard-backend/pom.xml
- repoguard-frontend/package.json
- repoguard-frontend/package-lock.json

验证结果：

- Maven 3.9.16 + JDK 26 默认 validate：按设计失败，明确提示后端构建必须使用 JDK 25。
- 本机通过 `-Denforcer.skip=true` 复核后端代码和 JaCoCo：1,604 个测试，0 failure，0 error，2 skipped，BUILD SUCCESS。
- JaCoCo 0.8.15：661 个类分析完成，无 major version 70 插桩异常，coverage check 通过。
- 覆盖率：instruction 90.23%、line 89.86%、branch 72.29%、method 89.84%、class 96.07%。
- 前端 `@types/node`：22.20.1。
- 前端 TypeScript、ESLint：通过。
- 前端 Vitest：22 个测试文件、79 个测试全部通过。
- 前端 Vite 生产构建：通过，共转换 3,330 个模块。
- git diff --check：通过。

验收结论：Java 和 Maven 版本偏差会默认快速失败，JaCoCo 在允许验证的 JDK 上不再产生 class-version 噪声，后端覆盖率回退成为构建门禁，Node 运行与类型版本一致，本项达到本地完成标准。

## 进度记录 12

完成时间：2026-07-15 18:48:02 +08:00（Asia/Shanghai）
优化项：P2 前端生产产物体积预算
状态：已完成，准备随本记录提交并推送至 PRAgent-test，由现有 PR Quality 与 Release Images 的生产构建复核门禁。

实施内容：

1. 在 Vite 构建阶段新增产物预算插件，直接分析入口静态依赖图及其 CSS，不依赖哈希文件名或额外测试脚本。
2. 首屏预加载 JavaScript gzip 预算设为 190 KiB，当前为 179.7 KiB；首屏 CSS gzip 预算设为 60 KiB，当前为 53.9 KiB。
3. 最大异步 JavaScript chunk gzip 预算设为 140 KiB，当前最大项为 ECharts core 132.8 KiB。
4. 任一预算超限会使 `vite build` 失败并打印实际值、预算值和最大异步 chunk 文件名；现有 PR 与镜像工作流均执行生产构建，因此无需增加独立 CI 脚本即可阻断体积回退。

涉及文件：

- repoguard-frontend/vite.config.ts

验证结果：

- 前端 TypeScript：通过。
- 前端 ESLint：通过。
- 前端 Vitest：22 个测试文件、79 个测试全部通过。
- 前端 Vite 生产构建：通过，共转换 3,330 个模块。
- 构建预算：首屏 JavaScript gzip 179.7/190.0 KiB，首屏 CSS gzip 53.9/60.0 KiB，最大异步 JavaScript gzip 132.8/140.0 KiB，全部通过。
- git diff --check：通过。

验收结论：生产构建现在能够持续阻断首屏 JavaScript、首屏 CSS 和最大异步 JavaScript 的明显体积回退，同时保留约 5% 至 11% 的合理构建波动空间，本项达到完成标准。

## 进度记录 13

完成时间：2026-07-15 18:57:15 +08:00（Asia/Shanghai）
优化项：P2 Element Plus 按需样式与首屏 CSS 减重
状态：已完成，准备随本记录提交并推送至 PRAgent-test，由 PR Quality 与 Release Images 复核生产构建和镜像发布链路。

实施内容：

1. 移除入口中的 Element Plus 完整主题 `element-plus/dist/index.css`，改为项目实际使用组件对应的官方 `style/css.mjs` 样式入口。
2. 覆盖 alert、button、dialog、form、table、select 等模板组件，同时保留 loading 指令、message、message-box 等编程式组件样式。
3. 通过 Element Plus 官方样式入口递归加载 base、overlay、popper、scrollbar 等内部依赖，避免直接拼接 theme-chalk 文件造成隐式样式缺失。
4. Element Plus CSS 从 357.06 kB（gzip 47.77 kB）降至 152.58 kB（gzip 20.36 kB），原始体积和 gzip 体积均减少约 57%。
5. 首屏 CSS gzip 从 53.9 KiB 降至 27.1 KiB，减少约 49.7%；同步将构建预算从 60 KiB 收紧至 32 KiB，确保不能退回完整主题。

涉及文件：

- repoguard-frontend/src/main.ts
- repoguard-frontend/vite.config.ts

验证结果：

- 前端 TypeScript：通过。
- 前端 ESLint：通过。
- 前端 Vitest：22 个测试文件、79 个测试全部通过。
- 前端 Vite 生产构建：通过，共转换 3,389 个模块。
- 构建预算：首屏 JavaScript gzip 179.7/190.0 KiB，首屏 CSS gzip 27.1/32.0 KiB，最大异步 JavaScript gzip 132.8/140.0 KiB，全部通过。
- git diff --check：通过。

验收结论：未使用的 Element Plus 主题样式不再进入首屏，实际组件及其隐式依赖样式均由官方入口保留，且新的 CSS 基线已由构建门禁固化，本项达到完成标准。

## 进度记录 14

完成时间：2026-07-15 19:16:33 +08:00（Asia/Shanghai）
优化项：P2 审查详情重型分区加载边界收敛
状态：已完成，准备随本记录提交并推送至 PRAgent-test，由 PR Quality 与 Release Images 复核。

实施内容：

1. 将 findings、changed files、missing tests 和 timeline 的 API 调用、分页、loading/loaded 状态从 ReviewDetailPage 移入独立领域 composable。
2. 三类分页明细复用统一加载流程，集中维护 20 条分页上限、归档任务空明细行为、服务端 total 回写和稳定错误反馈。
3. 每个分区增加独立请求序号；路由切换、详情重载和组件卸载会统一失效在途请求，旧响应与旧异常不能写入新任务。
4. 旧请求完成时仅可收尾自己的 loading 状态，不能提前清除重置后新请求的 loading；新增延迟 Promise 测试覆盖该竞争场景。
5. ReviewDetailPage 从 735 行降至 605 行，页面保留编排和展示派生逻辑，数据加载边界集中到 review-detail 领域目录。

涉及文件：

- repoguard-frontend/src/pages/ReviewDetailPage.vue
- repoguard-frontend/src/features/review-detail/index.ts
- repoguard-frontend/src/features/review-detail/composables/useReviewDetailSectionLoaders.ts
- repoguard-frontend/src/features/review-detail/composables/useReviewDetailSectionLoaders.test.ts

验证结果：

- 新增分区加载单元测试：3 个测试通过，覆盖四类接口参数/回写、过期请求竞争和归档明细短路。
- 前端 TypeScript：通过。
- 前端 ESLint：通过。
- 前端 Vitest：23 个测试文件、82 个测试全部通过。
- 前端 Vite 生产构建：通过，共转换 3,390 个模块。
- 构建预算：首屏 JavaScript gzip 179.7/190.0 KiB，首屏 CSS gzip 27.1/32.0 KiB，最大异步 JavaScript gzip 132.8/140.0 KiB，全部通过。
- git diff --check：通过。

验收结论：详情页重型分区的加载职责已按领域边界收敛，路由切换时的在途分区请求不会污染新任务或错误状态，同时页面规模得到有行为测试保护的渐进缩减，本项达到完成标准。

## 进度记录 15

完成时间：2026-07-15 19:29:19 +08:00（Asia/Shanghai）
优化项：P2 图表容器自适应与可访问名称
状态：已完成，准备随本记录提交并推送至 PRAgent-test，由 PR Quality 与 Release Images 复核。

实施内容：

1. EChartPanel 改为使用 ResizeObserver 监听自身容器，侧栏折叠、网格重排和父容器尺寸变化无需等待 window resize 即可触发图表 resize。
2. 不支持 ResizeObserver 的环境保留 window resize fallback；组件卸载时按实际路径 disconnect/removeEventListener，并 dispose 图表实例。
3. 图表组件强制接收 accessibleLabel，可选接收可读 summary；容器提供 role、aria-label、aria-describedby，并以视觉隐藏文本暴露数据摘要。
4. 启用 ECharts AriaComponent 和 decal 纹理，ARIA description 使用稳定中文名称与数据摘要，降低只依赖颜色区分图形的风险。
5. 审查趋势、风险分布、规则命中、LLM 质量趋势四个图表均生成与当前数据同步的文本摘要。
6. 侧栏折叠按钮、顶部菜单按钮和折叠状态下的导航链接补充 type、动态 aria-label 与 title，图标控件具备稳定可访问名称和原生提示。

涉及文件：

- repoguard-frontend/src/components/EChartPanel.vue
- repoguard-frontend/src/components/EChartPanel.test.ts
- repoguard-frontend/src/features/dashboard/components/DashboardChartSection.vue
- repoguard-frontend/src/features/dashboard/components/LlmQualitySection.vue
- repoguard-frontend/src/layouts/RepoGuardLayout.vue

验证结果：

- 新增 EChartPanel 组件测试：验证容器观察、ARIA 关联、ECharts 描述/纹理、ResizeObserver 回调、disconnect 和 dispose。
- 前端 TypeScript：通过。
- 前端 ESLint：通过。
- 前端 Vitest：24 个测试文件、83 个测试全部通过。
- 前端 Vite 生产构建：通过，共转换 3,391 个模块。
- 构建预算：首屏 JavaScript gzip 179.9/190.0 KiB，首屏 CSS gzip 27.1/32.0 KiB，最大异步 JavaScript gzip 133.9/140.0 KiB，全部通过。
- git diff --check：通过。

验收结论：图表能够随真实容器尺寸变化自适应，卸载后不保留观察器或窗口监听；读屏用户可获得稳定名称与数据摘要，侧栏图标控件也具备明确可访问名称，本项达到完成标准。

## 进度记录 16

完成时间：2026-07-15 19:46:15 +08:00（Asia/Shanghai）
优化项：P2 关键 API 成功响应运行时契约校验
状态：已完成，准备随本记录提交并推送至 PRAgent-test，由 PR Quality 与 Release Images 复核。

实施内容：

1. 保留项目现有 OpenAPI 3.1 文档、前端端点目录和生成客户端契约测试作为静态单一事实源，在此基础上为认证、审查详情、GitHub/MySQL/RabbitMQ 集成配置和审查策略等关键成功响应增加轻量运行时校验。
2. API 请求层统一先将成功响应视为 unknown；只有通过端点声明的类型守卫后才向业务层返回，未配置校验器的非关键端点维持原有行为，避免一次性扩大改造范围。
3. HTTP 200 但结构不完整或字段类型错误时，统一抛出状态码为 200、错误码为 INVALID_API_RESPONSE 的 RequestError，不将异常载荷泄漏给页面状态。
4. 响应校验发生在成功观测之前；契约异常会被前端可观测性记录为 failed，并保留 operation、status 与耗时，避免把格式错误误计为成功请求。
5. 新增关键响应的正反例单元测试，并在 API 请求层验证畸形登录响应会被拒绝且写入失败观测；同步将既有测试桩调整为满足已公开契约的有效响应。

涉及文件：

- repoguard-frontend/src/api/responseValidation.ts
- repoguard-frontend/src/api/responseValidation.test.ts
- repoguard-frontend/src/api/contracts.ts
- repoguard-frontend/src/api/contracts.test.ts

验证结果：

- 前端 TypeScript：通过。
- 前端 ESLint：通过。
- 前端 Vitest：25 个测试文件、86 个测试全部通过。
- 前端 Vite 生产构建：通过，共转换 3,392 个模块。
- 构建预算：首屏 JavaScript gzip 180.3/190.0 KiB，首屏 CSS gzip 27.1/32.0 KiB，最大异步 JavaScript gzip 133.9/140.0 KiB，全部通过。
- 后端跨语言/API 契约定向测试：4 个测试类、36 个测试全部通过，BUILD SUCCESS。

验收结论：关键成功响应现在同时受静态 OpenAPI 契约和运行时边界校验保护；服务端返回格式漂移会在进入页面状态前以稳定错误码失败并留下观测记录，本项达到本地完成标准。

## 进度记录 17

完成时间：2026-07-15 19:53:56 +08:00（Asia/Shanghai）
优化项：P2 Release Images 密钥泄漏门禁测试桩兼容修复
状态：本地修复与同源门禁验证已完成，准备提交并推送至 PRAgent-test，等待 GitHub Actions 复核。

问题定位：

- GitHub Actions `Release Images` run 29413007985 在 `Repository governance guard` 失败，镜像构建与扫描尚未开始。
- `gh run view --log-failed` 定位到 repoguard-frontend/src/api/contracts.test.ts 的伪 accessToken；测试值 `new-access-token` 满足仓库密钥泄漏启发式规则，被正确阻断。

实施内容：

1. 将测试桩令牌改为扫描规则明确允许的 `test-` 前缀伪凭据，保留认证成功响应结构与契约校验覆盖。
2. 不放宽 secret/token 启发式规则，不排除测试源文件，也不增加例外清单，确保真实高熵凭据仍会阻断发布。
3. 未新增或提交任何测试辅助脚本；本地复用仓库现有 production-readiness-check.ps1 验证与 Actions 相同的治理门禁。

涉及文件：

- repoguard-frontend/src/api/contracts.test.ts

验证结果：

- API 请求层定向 Vitest：1 个测试文件、12 个测试全部通过。
- Production readiness quick guard：空白检查、跟踪文件治理、Flyway 命名/重复版本、演示数据隔离、secret/token 泄漏扫描全部通过。
- git diff --check：通过。

验收结论：测试伪凭据不再误触发布密钥门禁，同时门禁强度与扫描范围保持不变；本项达到本地完成标准，最终完成以新一轮 Release Images 全绿为准。

## 进度记录 18

完成时间：2026-07-15 20:08:08 +08:00（Asia/Shanghai）
优化项：P2 生产容器日志单一采集路径
状态：本地实现与全量后端验证已完成，准备提交并推送至 PRAgent-test，由生产 profile 集成测试和 Release Images 复核。

实施内容：

1. CONSOLE appender 与 INFO root logger 作为所有环境的基础日志出口，继续输出 traceId、taskId、prNumber 和 repository 等结构化上下文字段。
2. ROLLING_FILE appender 仅在 `(dev | local) & !prod` profile 表达式成立时创建并挂载；即使 prod 与本地 profile 被误配为同时启用，`!prod` 也会优先阻止容器文件落盘。
3. prod、test 及其他非本地环境不再创建 logs/backend 滚动文件，生产日志只经 stdout 进入现有 Docker/Promtail 链路，避免重复 I/O、2GB writable layer 占用和双采集。
4. 新增 Logback XML 静态契约测试，约束基础 root 仅引用 CONSOLE、唯一 profile 表达式和本地 ROLLING_FILE 引用，防止后续配置回退。
5. 扩展现有 ProductionRuntimeContextIntegrationTest，在真实 prod profile、MySQL、RabbitMQ 上下文中断言 root logger 存在 CONSOLE 且不存在 ROLLING_FILE。

涉及文件：

- repoguard-backend/src/main/resources/logback-spring.xml
- repoguard-backend/src/test/java/com/repoguard/agent/config/LogbackProfileContractTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/integration/ProductionRuntimeContextIntegrationTest.java

验证结果：

- Logback profile 定向测试：1 个测试通过，BUILD SUCCESS。
- 后端全量 `mvn verify`：1,605 个测试，0 failure，0 error，2 skipped，BUILD SUCCESS。
- JaCoCo 0.8.15：661 个类分析完成，覆盖率门禁全部通过。
- Production readiness quick guard：空白检查、跟踪文件治理、Flyway 命名/重复版本、演示数据隔离、secret/token 泄漏扫描全部通过。
- git diff --check：通过。

验收结论：生产容器只保留 stdout 这一条权威日志采集路径，本地开发仍保留有界滚动文件；配置边界同时受静态契约和生产运行时断言保护，本项达到本地完成标准。

## 进度记录 19

完成时间：2026-07-15 20:35:37 +08:00（Asia/Shanghai）
优化项：P2 未处理异常 errorId 与安全完整堆栈
状态：本地实现、全量后端验证和前端生产构建均已完成，准备提交并推送至 PRAgent-test，由生产 profile 集成测试和 Release Images 复核。

实施内容：

1. 未处理异常继续向客户端返回固定的安全 500 响应体，同时新增 `X-Error-Id` 响应头；不改变现有 API body 契约，也不向客户端暴露异常类型、消息或堆栈。
2. 每次未处理异常生成独立 UUID，并在同一次日志事件的消息、MDC `errorId` 字段和响应头中保持一致；记录结束后恢复原 MDC 值，避免线程复用造成关联标识串扰。
3. 错误日志恢复完整 Throwable 及 cause 链，通过 Logback `safeMsg`、`safeEx` 转换器统一清洗格式化消息和完整堆栈，在保留异常类型、调用位置及因果链的同时遮蔽 JDBC URL、URI 凭据、敏感查询参数、密码/令牌赋值、Bearer 令牌和 OpenAI 密钥。
4. JDBC 连接串改为整体归一化为 `jdbc:****`，不再保留数据库主机、端口、库名和连接参数；连接探测错误摘要同步采用同一安全边界。
5. CORS 显式暴露 `X-Trace-Id` 与 `X-Error-Id`；前端 `RequestError` 捕获服务端 errorId，便于支持人员关联后端日志，但现有页面错误展示行为保持不变。

涉及文件：

- repoguard-backend/src/main/java/com/repoguard/agent/common/GlobalExceptionHandler.java
- repoguard-backend/src/main/java/com/repoguard/agent/common/SensitiveTextSanitizer.java
- repoguard-backend/src/main/java/com/repoguard/agent/common/SanitizingMessageConverter.java
- repoguard-backend/src/main/java/com/repoguard/agent/common/SanitizingThrowableConverter.java
- repoguard-backend/src/main/java/com/repoguard/agent/config/CorsConfig.java
- repoguard-backend/src/main/resources/logback-spring.xml
- repoguard-backend/src/test/java/com/repoguard/agent/common/GlobalExceptionHandlerTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/common/SensitiveTextSanitizerTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/common/SanitizingLogConverterTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/config/CorsConfigTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/config/LogbackProfileContractTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/service/impl/ConnectionProbeErrorMessageTest.java
- repoguard-frontend/src/api/apiEnvelope.ts
- repoguard-frontend/src/api/client.test.ts
- repoguard-frontend/src/utils/errors.ts

验证结果：

- 后端定向测试：13 个测试全部通过，覆盖异常关联、完整因果链、敏感信息清洗、CORS 暴露头和 Logback 配置契约。
- 后端全量 `mvn verify`：1,607 个测试，0 failure，0 error，2 skipped，BUILD SUCCESS。
- JaCoCo 0.8.15：覆盖率门禁全部通过。
- 前端 TypeScript、ESLint：通过。
- 前端 Vitest：25 个测试文件、87 个测试全部通过。
- 前端 Vite 生产构建：通过，共转换 3,392 个模块。
- 构建预算：初始 JavaScript gzip 180.4/190.0 KiB，初始 CSS gzip 27.1/32.0 KiB，最大异步 JavaScript gzip 133.9/140.0 KiB，全部通过。
- Production readiness quick guard：空白检查、跟踪文件治理、Flyway 命名/重复版本、演示数据隔离、secret/token 泄漏扫描全部通过。

验收结论：客户端获得可供排障关联的安全 errorId，服务端恢复完整可诊断异常链且不会把连接地址或凭据写入日志；响应契约和现有用户提示保持稳定，本项达到本地完成标准。

## 进度记录 20

完成时间：2026-07-15 21:08:28 +08:00（Asia/Shanghai）
优化项：P2 认证授权策略精确化与双向契约
状态：本地实现、定向权限测试、全量后端验证和仓库治理门禁均已完成，准备提交并推送至 PRAgent-test，由 PR Quality 与 Release Images 复核。

实施内容：

1. 将注册、登录、刷新、刷新令牌重置、退出和 GitHub webhook 六个匿名入口从 `ANY_METHOD` 收紧为精确 `POST`；相同路径的 GET、DELETE 等非映射方法不再绕过访问令牌过滤器。
2. 新增 `@AllowAnonymous` 语义标记并应用到六个真实 Controller 方法；匿名策略表与 Controller 映射通过 HTTP method + path 完整集合比较，任何单边新增、删除或方法漂移都会使契约测试失败。
3. 非匿名写接口必须显式声明 `@RequireRole`；前端性能上报明确允许 `ADMIN/VIEWER` 访问，替代原先测试中的方法名白名单，同时保持其必须携带有效用户访问令牌的现有边界。
4. Admin API Key 契约改为双向检查：所有 ADMIN-only Controller 映射必须被策略覆盖，每个策略项必须至少对应一个真实映射，且策略不得匹配非 ADMIN-only 接口。
5. 移除没有任何 Controller 映射的 `/api/v1/config` 精确策略项；保留 `/api/v1/config/**` 对现有配置接口的保护，后续若新增根路径接口，双向测试会要求显式补充策略。
6. 明确 Admin API Key 只覆盖 ADMIN-only 管理接口，不因普通接口同时允许 ADMIN 与 VIEWER 就自动扩权；性能上报仍不能使用 Admin API Key 建立管理员身份。

涉及文件：

- repoguard-backend/src/main/java/com/repoguard/agent/security/AllowAnonymous.java
- repoguard-backend/src/main/java/com/repoguard/agent/security/AuthTokenAccessPolicy.java
- repoguard-backend/src/main/java/com/repoguard/agent/security/AdminApiKeyAccessPolicy.java
- repoguard-backend/src/main/java/com/repoguard/agent/controller/AuthController.java
- repoguard-backend/src/main/java/com/repoguard/agent/controller/GithubWebhookController.java
- repoguard-backend/src/main/java/com/repoguard/agent/controller/FrontendPerformanceController.java
- repoguard-backend/src/test/java/com/repoguard/agent/security/AuthTokenAccessPolicyTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/security/AuthTokenFilterTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/security/AdminApiKeyAccessPolicyTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/security/RoleAuthorizationInterceptorTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/controller/ControllerAuthorizationContractTest.java

验证结果：

- 权限策略与过滤器定向测试：31 个测试全部通过，0 failure，0 error。
- 后端全量 `mvn verify`：1,611 个测试，0 failure，0 error，2 skipped，BUILD SUCCESS。
- JaCoCo 0.8.15：覆盖率门禁全部通过。
- Production readiness quick guard：空白检查、跟踪文件治理、Flyway 命名/重复版本、演示数据隔离、secret/token 泄漏扫描全部通过。
- git diff --check：通过。

验收结论：匿名访问面已精确到真实 Controller 方法与 HTTP method，普通用户写接口和管理员专用接口具备可读的权限声明；访问令牌策略、RBAC 映射和 Admin API Key 策略均由双向契约约束，本项达到本地完成标准。

## 进度记录 21

完成时间：2026-07-15 21:39:32 +08:00（Asia/Shanghai）
优化项：P2 默认运行 profile 与本地默认值安全边界
状态：本地实现、配置契约测试、全量后端验证、可执行 JAR 启动验证和仓库治理门禁均已完成，准备提交并推送至 PRAgent-test，由 PR Quality 与 Release Images 复核。

实施内容：

1. 移除基础 `application.yml` 中默认激活 `dev,local` 的行为；新增 Spring Boot 4 `EnvironmentPostProcessor`，配置数据装载后若仍未显式指定 profile，则在创建应用上下文和连接外部基础设施前立即拒绝启动。
2. 将 `dev`、`local`、`test` 定义为本地/测试安全域，其余包括 `prod`、`staging` 在内的未知 profile 均按生产强度校验；同时禁止本地/测试 profile 与生产类 profile 混合启用，避免生产环境通过叠加本地 profile 重新获得弱默认值。
3. 基础配置不再为 RabbitMQ 主机与凭据、加密密钥与密钥编号、认证 token secret 提供本地默认值；用户注册默认关闭，CORS 默认来源为空，使未完整配置的生产类环境以失败关闭方式启动。
4. 将 localhost RabbitMQ、开发加密密钥、开发 token secret 和注册开启等便利默认值迁移到 `application-dev.yml` 与 `application-test.yml`，本地开发和测试仍可在显式 profile 下保持原有体验。
5. CORS、认证密钥、Admin API Key、GitHub webhook 和敏感配置加密校验统一复用运行 profile 策略；`staging` 等非本地环境不再绕过过去只针对 `prod` 的强校验。
6. README 后端本地启动命令显式增加 `-Dspring-boot.run.profiles=dev,local`，消除安全默认值收紧后的操作歧义；提交时仅纳入该单行变更，不包含工作区中既有的其他 README 修改。

涉及文件：

- README.md
- repoguard-backend/src/main/java/com/repoguard/agent/config/RuntimeProfilePolicy.java
- repoguard-backend/src/main/java/com/repoguard/agent/config/ExplicitProfileEnvironmentPostProcessor.java
- repoguard-backend/src/main/java/com/repoguard/agent/config/CorsConfig.java
- repoguard-backend/src/main/java/com/repoguard/agent/security/AuthProperties.java
- repoguard-backend/src/main/java/com/repoguard/agent/security/AdminApiKeyProperties.java
- repoguard-backend/src/main/java/com/repoguard/agent/security/SecretCryptoService.java
- repoguard-backend/src/main/java/com/repoguard/agent/github/webhook/GithubWebhookProperties.java
- repoguard-backend/src/main/resources/META-INF/spring.factories
- repoguard-backend/src/main/resources/application.yml
- repoguard-backend/src/main/resources/application-dev.yml
- repoguard-backend/src/main/resources/application-test.yml
- repoguard-backend/src/test/java/com/repoguard/agent/config/RuntimeProfilePolicyTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/config/ExplicitProfileEnvironmentPostProcessorTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/config/ApplicationProfileConfigurationTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/config/CorsConfigTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/security/AuthPropertiesTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/security/AdminApiKeyFilterTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/security/SecretCryptoServiceTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/github/webhook/GithubWebhookPropertiesTest.java

验证结果：

- profile 策略、Boot 注册、配置边界和生产类强校验定向测试：54 个测试全部通过，0 failure，0 error。
- 后端全量 `mvn verify`：1,626 个测试，0 failure，0 error，2 skipped，BUILD SUCCESS。
- JaCoCo 0.8.15：覆盖率门禁全部通过。
- 可执行 JAR 无 profile 启动验证：按预期退出码 1，并明确报告必须显式指定 Spring profile；未进入数据库或 RabbitMQ 连接阶段。
- Production readiness quick guard：空白检查、跟踪文件治理、Flyway 命名/重复版本、演示数据隔离、secret/token 泄漏扫描全部通过。
- git diff --check：通过。

验收结论：基础配置不再静默降级为开发/本地环境，弱默认值仅在显式本地或测试 profile 下可用；未知部署环境默认继承生产强校验，混合 profile 会被提前拒绝，本项达到本地完成标准。

## 进度记录 22

完成时间：2026-07-15 22:02:04 +08:00（Asia/Shanghai）
优化项：P2 Data Retention 持久化内聚边界收敛（复杂模块第一阶段）
状态：本地实现、持久化顺序回归测试、全量后端验证和仓库治理门禁均已完成，准备提交并推送至 PRAgent-test，由 PR Quality 与 Release Images 复核。

实施内容：

1. 根据变更耦合审计选择 Data Retention 作为复杂模块收敛的第一阶段：该编排器在最近 8 天连续发生 12 次相关变更，候选查询、归档写入和级联删除是反复共同变化且可独立验证的真实边界。
2. 新建 `com.repoguard.agent.retention` 领域包，将“按状态和截止时间统计并选择最老任务”收敛到 `DataRetentionCandidateQuery`，以不可变 `CandidateSelection` 返回总候选数和本次任务编号。
3. 将归档摘要写入收敛到 `DataRetentionArchiveWriter`；空任务集合在访问 Mapper 前失败，避免未来调用点意外生成无意义归档 SQL。
4. 将七类子表和任务主表的级联删除顺序收敛到 `DataRetentionDeleteExecutor`，以不可变 `DeletionResult` 返回每类删除数量；子表先于任务主表的外键安全顺序由独立测试锁定。
5. `DataRetentionServiceImpl` 不再直接注入 8 个持久化 Mapper 和状态机，只编排候选查询、归档、删除三个领域组件，并继续负责参数策略、事务、审计、指标和租约生命周期；构造依赖由 15 个降至 9 个，文件由 437 行降至 371 行。
6. 编排器测试改为面向领域边界验证，保留归档先于删除、归档失败不删除、事务提交后完成审计、回滚失败审计、跨实例租约和进程内并发互斥等既有语义；各持久化组件新增独立行为测试。
7. 未新增或提交任何测试辅助脚本，也未为减少文件数合并现有单一职责组件；本阶段只处理有变更耦合证据和独立策略的边界。

涉及文件：

- repoguard-backend/src/main/java/com/repoguard/agent/retention/DataRetentionCandidateQuery.java
- repoguard-backend/src/main/java/com/repoguard/agent/retention/DataRetentionArchiveWriter.java
- repoguard-backend/src/main/java/com/repoguard/agent/retention/DataRetentionDeleteExecutor.java
- repoguard-backend/src/main/java/com/repoguard/agent/service/impl/DataRetentionServiceImpl.java
- repoguard-backend/src/test/java/com/repoguard/agent/retention/DataRetentionCandidateQueryTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/retention/DataRetentionArchiveWriterTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/retention/DataRetentionDeleteExecutorTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/service/impl/DataRetentionServiceImplTest.java

验证结果：

- Data Retention 领域组件、编排器和 Spring Bean 构造器定向测试：29 个测试全部通过，0 failure，0 error。
- 后端全量 `mvn verify`：1,634 个测试，0 failure，0 error，2 skipped，BUILD SUCCESS。
- JaCoCo 0.8.15：覆盖率门禁全部通过。
- Production readiness quick guard：空白检查、跟踪文件治理、Flyway 命名/重复版本、演示数据隔离、secret/token 泄漏扫描全部通过。
- git diff --check：通过。

验收结论：数据保留编排器已从具体 Mapper 调用细节解耦，候选选择、归档和级联删除分别具备清晰输入输出与顺序契约；事务、审计和租约语义保持不变，修改持久化步骤时无需再改动整个编排器测试，本阶段达到本地完成标准。

## 进度记录 23

完成时间：2026-07-15 22:16:42 +08:00（Asia/Shanghai）
优化项：P2 Dashboard overview/quality 内聚边界收敛（复杂模块第二阶段）
状态：本地实现、Dashboard 定向回归、全量后端验证和仓库治理门禁均已完成，准备提交并推送至 PRAgent-test，由 PR Quality 与 Release Images 复核。

实施内容：

1. 新增 `DashboardOverviewFacade`，集中承接 overview 兼容响应、核心指标、审查趋势、风险分布、规则命中和高风险审查的查询窗口、组装器协作与最后成功快照回退。
2. 新增 `DashboardQualityFacade`，集中承接 LLM 质量的模型、仓库和趋势窗口查询与响应组装；LLM 天数归一化、趋势补点和最后成功快照 key 语义保持不变。
3. `DashboardServiceImpl` 只保留 DashboardService API 契约、Spring Cache 的独立模块缓存声明和端点委派；系统健康继续通过已有 `DashboardSystemHealthProbe` 边界获取。
4. Dashboard 服务不再直接注入 Mapper、九类 assembler/window/snapshot 组件；构造依赖由 12 个降至 3 个，文件由 224 行降至 92 行，使 overview 与 quality 后续变化不再共同扩大 API 服务装配面。
5. 保留各模块独立 `@Cacheable(sync = true)` 与 snapshot store 两层保护：外层防止 TTL 击穿，内层在异步刷新失败时返回最近成功值；现有测试继续验证指标模块不会加载趋势/规则/质量聚合，质量模块不会加载 overview 聚合。
6. 未重复拆分现有 assembler，也未引入通用“依赖大对象”；新 facade 只聚合已经存在且共同参与同一页面领域响应的组件。

涉及文件：

- repoguard-backend/src/main/java/com/repoguard/agent/dashboard/DashboardOverviewFacade.java
- repoguard-backend/src/main/java/com/repoguard/agent/dashboard/DashboardQualityFacade.java
- repoguard-backend/src/main/java/com/repoguard/agent/service/impl/DashboardServiceImpl.java
- repoguard-backend/src/test/java/com/repoguard/agent/service/impl/DashboardServiceImplTest.java

验证结果：

- Dashboard facade、快照服务、缓存回退与 Spring Bean 构造器定向测试：24 个测试全部通过，0 failure，0 error。
- 后端全量 `mvn verify`：1,634 个测试，0 failure，0 error，2 skipped，BUILD SUCCESS。
- JaCoCo 0.8.15：覆盖率门禁全部通过。
- Production readiness quick guard：空白检查、跟踪文件治理、Flyway 命名/重复版本、演示数据隔离、secret/token 泄漏扫描全部通过。
- git diff --check：通过。

验收结论：Dashboard API 服务已从多聚合器编排器收敛为稳定入口，overview 与 quality 各自拥有内聚的查询、组装和快照恢复边界；缓存与查询隔离行为保持原样，本阶段达到本地完成标准。

## 进度记录 24

完成时间：2026-07-15 22:37:45 +08:00（Asia/Shanghai）
优化项：P2 指标记录领域边界与统一 Tag 策略（复杂模块第三阶段）
状态：本地实现、指标调用链定向回归、全量后端验证和仓库治理门禁均已完成，准备提交并推送至 PRAgent-test，由 PR Quality 与 Release Images 复核。

实施内容：

1. 将原本同时承担指标注册、Tag 归一化及四类业务指标记录的 `RepoGuardMetrics` 收敛为稳定公共门面，现有生产调用方法和重载保持不变，调用方无需感知内部拆分。
2. 新增 `MetricRecorderSupport`，统一承接 Counter、Timer、DistributionSummary、Gauge 注册，以及空值、负时长、HTTP method/status、路径和普通 Tag 的低基数归一化策略，避免各领域自行复制并逐步漂移。
3. 新增 `ReviewMetricsRecorder`，内聚审查任务、执行阶段、失败分类、刷新令牌复用和数据保留指标；失败重试属性仍复用 `ReviewExecutionFailureClassifier` 与外部调用异常语义。
4. 新增 `ExternalMetricsRecorder`，内聚外部调用失败/重试、GitHub API/diff/comment 与 LLM 请求/fallback 指标；同一操作的计时器和计数器继续共享完全一致的 Tag 集合。
5. 新增 `ObservabilityMetricsRecorder`，内聚 HTTP API、SQL、Dashboard cache、前端 waterfall/long task 与阈值越界指标，响应字节数、查询行数和时长继续执行非负保护。
6. 新增 `RabbitMetricsRecorder`，内聚发布失败、消费、补偿和队列深度指标；并发 Gauge 状态映射被限制在 Rabbit 指标领域内，Gauge 名称、Tag 与更新语义保持不变。
7. `RepoGuardMetrics` 文件由 421 行降至 193 行，生产构造器只依赖 4 个内聚 recorder；测试通过显式 `forTesting` 工厂复用同一支持组件和领域实现，避免增加 Spring 构造器歧义。
8. 未新增或提交任何测试辅助脚本；既有指标行为、指标名称、Tag 名称和公共调用契约由原有指标测试与各调用方测试共同回归。

涉及文件：

- repoguard-backend/src/main/java/com/repoguard/agent/observability/RepoGuardMetrics.java
- repoguard-backend/src/main/java/com/repoguard/agent/observability/MetricRecorderSupport.java
- repoguard-backend/src/main/java/com/repoguard/agent/observability/ReviewMetricsRecorder.java
- repoguard-backend/src/main/java/com/repoguard/agent/observability/ExternalMetricsRecorder.java
- repoguard-backend/src/main/java/com/repoguard/agent/observability/ObservabilityMetricsRecorder.java
- repoguard-backend/src/main/java/com/repoguard/agent/observability/RabbitMetricsRecorder.java
- repoguard-backend/src/test/java/com/repoguard/agent/observability/RepoGuardMetricsTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/observability/ApiRequestObservationFilterTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/observability/SqlQueryObservationInterceptorTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/observability/ObservedCacheTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/observability/ObservabilityThresholdMonitorTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/observability/ObservabilityFilterConfigTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/github/GithubPullRequestClientImplTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/messaging/RabbitReviewTaskPublisherTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/notification/RabbitNotificationEventPublisherTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/service/impl/CacheStatsServiceImplTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/service/impl/FrontendPerformanceObservationServiceImplTest.java

验证结果：

- 指标门面、领域 recorder 与关键调用方定向测试：12 个测试类、85 个测试全部通过，0 failure，0 error。
- 后端全量 `mvn verify`：1,634 个测试，0 failure，0 error，2 skipped，BUILD SUCCESS。
- JaCoCo 0.8.15：覆盖率门禁全部通过。
- Production readiness quick guard：空白检查、跟踪文件治理、Flyway 命名/重复版本、演示数据隔离、secret/token 泄漏扫描全部通过。
- git diff --check：通过。

验收结论：指标采集公共契约保持稳定，Review、External、HTTP/SQL/Frontend 与 Rabbit 四类变化已具备独立内聚边界，并共享一套 Tag 基数和空值策略；队列 Gauge 可变状态不再污染其他指标领域，本阶段达到本地完成标准。

## 进度记录 25

完成时间：2026-07-16 01:08:08 +08:00（Asia/Shanghai）
优化项：P2 自动化架构边界与渐进式循环依赖门禁（复杂模块第四阶段）
状态：本地实现、架构契约、受影响调用链回归、全量后端验证和仓库治理门禁均已完成，准备提交并推送至 PRAgent-test，由 PR Quality 与 Release Images 复核。

实施内容：

1. 新增 `ApplicationArchitectureTest`，直接使用 JDK 编译器语法树解析全部生产 Java 源码，不引入新的第三方架构测试依赖，也不会把注释或字符串误识别为代码依赖。
2. 固化 Controller 边界：`controller` 包及其子包不得直接引用 `mapper`，新增接口必须通过应用服务或领域边界访问持久化层。
3. 固化实现包可见性：任何 `impl` 包只能被自身包及其子包引用，其他领域不得跨包依赖具体实现类；规则同时覆盖普通 import、static import 和源码中的全限定类型引用。
4. 对领域顶层包建立有向依赖图并计算可达路径；现有 24 条参与循环的依赖边作为已审查技术债基线，删除旧边可直接收敛，任何新边只要形成循环就会使测试失败，避免为一次性清零历史循环而进行高风险大重构。
5. 审计发现 `DashboardSystemHealthProbe` 直接依赖 `service.impl.RabbitRuntimeHealthProbe`；将 Rabbit 运行时探针和专属执行器配置迁移至 `messaging` 领域，使 Dashboard、通知和消息队列应用服务共同依赖稳定消息基础设施边界。
6. Rabbit 探针的缓存、并发 in-flight 复用、超时、失败退避和线程池 Bean 语义保持不变；四参数 Spring 构造器继续保持包内可见，跨包测试通过显式 `forTesting` 工厂注入执行器，未扩大生产构造 API。
7. 现有 Controller→Mapper 引用为 0，领域外引用 `service.impl` 为 0；Dashboard/Retention 等已收敛领域没有被纳入任何新增循环。
8. 未新增 Maven 依赖，未新增或提交任何测试辅助脚本，也未提交 README 工作区中的既有修改。

涉及文件：

- repoguard-backend/src/main/java/com/repoguard/agent/messaging/RabbitRuntimeHealthProbe.java
- repoguard-backend/src/main/java/com/repoguard/agent/messaging/RabbitHealthProbeExecutorConfig.java
- repoguard-backend/src/main/java/com/repoguard/agent/dashboard/DashboardSystemHealthProbe.java
- repoguard-backend/src/main/java/com/repoguard/agent/service/impl/MessageQueueRuntimeConfigAssembler.java
- repoguard-backend/src/main/java/com/repoguard/agent/service/impl/NotificationServiceImpl.java
- repoguard-backend/src/main/java/com/repoguard/agent/service/impl/RabbitMqConnectionProbe.java
- repoguard-backend/src/test/java/com/repoguard/agent/architecture/ApplicationArchitectureTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/dashboard/DashboardSystemHealthProbeTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/service/impl/DashboardServiceImplTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/service/impl/MessageQueueHealthServiceImplTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/service/impl/MessageQueueRuntimeConfigAssemblerTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/service/impl/NotificationServiceImplTest.java

验证结果：

- 架构规则、Rabbit 探针、Dashboard/通知/消息队列调用方与 Spring Bean 构造选择定向回归：43 个测试全部通过；最终构造 API 可见性收紧后核心 16 个测试再次通过。
- 后端全量 `mvn verify`：1,637 个测试，0 failure，0 error，2 skipped，BUILD SUCCESS。
- JaCoCo 0.8.15：覆盖率门禁全部通过。
- Production readiness quick guard：空白检查、跟踪文件治理、Flyway 命名/重复版本、演示数据隔离、secret/token 泄漏扫描全部通过。
- git diff --check：通过。

验收结论：复杂模块前三阶段形成的边界已由可执行架构契约保护；Controller 无法绕过服务层、具体实现包不再跨域泄漏，历史领域循环以显式债务基线渐进收敛且不会继续扩张，本阶段达到本地完成标准。

## 进度记录 26

完成时间：2026-07-16 01:16:03 +08:00（Asia/Shanghai）
优化项：优化进度文档 Markdown 迁移与治理规则精确例外
状态：文档迁移、Markdown 结构化、仓库治理规则和定向验证均已完成，准备作为独立提交推送至 PRAgent-test。

实施内容：

1. 将根目录 `代码优化审查报告.txt` 完整迁移为 `代码优化审查报告.md`，保留原有审查结论、证据、建议和 25 条历史优化进度。
2. 为文档标题、十一个主体章节、P0/P1/P2 优化条目和全部进度记录补充 Markdown 标题层级，使目录导航和后续增量维护具备稳定结构。
3. 更新 `.gitignore`，只为根目录 `代码优化审查报告.md` 增加精确例外；其他目录或文件名的非 README Markdown 仍保持忽略和禁止跟踪。
4. 更新 Production Readiness 仓库治理门禁，仅允许根目录 `README.md` 与本优化进度文档；为兼容 Windows PowerShell 5 的脚本源码编码行为，中文文件名通过固定 Unicode 码点构造后进行精确比较。
5. 同步更新 `RepositoryGovernanceGuardTest` 的允许集合和分类器测试，明确验证进度文档可跟踪、目录内 Markdown、子项目 README 和其他根目录 Markdown 仍被拒绝。
6. 测试或临时脚本禁入规则保持不变，README 工作区中的既有未暂存修改仍未纳入本项。

涉及文件：

- .gitignore
- 代码优化审查报告.md
- scripts/production-readiness-check.ps1
- repoguard-backend/src/test/java/com/repoguard/agent/quality/RepositoryGovernanceGuardTest.java

验证结果：

- Markdown 结构检查：1 个一级标题、36 个二级标题（11 个主体章节与 25 条既有进度），迁移后历史内容完整；追加本记录后进度标题共 26 个。
- `RepositoryGovernanceGuardTest`：3 个测试全部通过，0 failure，0 error。
- Production readiness quick guard：空白检查、跟踪文件治理、Flyway 命名/重复版本、演示数据隔离、secret/token 泄漏扫描全部通过。
- git diff --cached --check：通过。

验收结论：优化进度已经以可导航的 Markdown 文档持续维护，仓库只对该指定根目录文件开放精确例外，不会放宽其他 Markdown 或测试脚本治理边界，本项达到完成标准。

## 进度记录 27

完成时间：2026-07-16 12:34:15 +08:00（Asia/Shanghai）
优化项：P2 Action 不可变引用、自动依赖更新与 PR SCA（供应链治理）
状态：本地实现、供应链契约、后端全量验证和仓库治理门禁均已完成，准备提交并推送至 PRAgent-test，由 GitHub Actions 复核。

实施内容：

1. 将六个受版本控制工作流中的外部 GitHub Actions 全部从浮动主版本标签固定到 40 位提交 SHA，并保留对应版本注释；使用 `gh api` 解析当前官方主版本引用，其中 Trivy Action 固定为 v0.36.0 对应提交。
2. 新增 Dependabot 配置，按周分组更新 GitHub Actions、后端 Maven、前端 npm 以及前后端 Docker 基础镜像；五组更新在 Asia/Shanghai 时区的周一错峰执行，控制并发依赖 PR 数量。
3. 在 PR 后端质量任务完成 `mvn verify` 后，对构建产物目录执行 Trivy 文件系统 library 扫描；发现 HIGH 或 CRITICAL 漏洞即失败，使应用依赖漏洞在发布镜像之前进入门禁。
4. 统一 PR 构建产物与后端、前端发布镜像的 Trivy 忽略清单为根目录 `.trivyignore.yaml`；清单默认无例外，未来例外必须提供说明和 ISO-8601 到期日，过期例外由供应链契约测试拒绝。
5. 通过官方固定版本 `action.yaml` 核对 Trivy 输入契约，确认忽略清单参数为 `trivyignores`，避免使用无效输入导致扫描策略未生效。
6. 新增 `SupplyChainPolicyTest`，自动检查所有外部 Action 的不可变引用、Dependabot 五类更新覆盖、PR 构建产物扫描及漏洞例外说明/有效期。
7. 修复 Production Readiness 在 Windows 上读取中文跟踪路径时受 Git `core.quotePath` 转义影响的问题；显式关闭路径引号转义后仍只允许根目录 README 与指定优化进度 Markdown，不放宽治理范围。
8. 未新增生产或测试依赖，未新增或提交测试辅助脚本，也未提交 README 工作区中的既有修改。

涉及文件：

- .github/workflows/auto-create-pr.yml
- .github/workflows/pr-quality.yml
- .github/workflows/real-chain-smoke.yml
- .github/workflows/release-images.yml
- .github/workflows/repository-governance.yml
- .github/workflows/worker-concurrency-retest.yml
- .github/dependabot.yml
- .trivyignore.yaml
- scripts/production-readiness-check.ps1
- repoguard-backend/src/test/java/com/repoguard/agent/quality/SupplyChainPolicyTest.java
- 代码优化审查报告.md

验证结果：

- `SupplyChainPolicyTest` 与 `RepositoryGovernanceGuardTest`：7 个测试全部通过，0 failure，0 error。
- 后端全量 `mvn verify`：1,641 个测试，0 failure，0 error，2 skipped，BUILD SUCCESS。
- JaCoCo 0.8.15：覆盖率门禁全部通过。
- Action 引用静态审计：六个工作流不存在浮动外部 Action 引用。
- Production readiness quick guard：空白检查、跟踪文件治理、Flyway 命名/重复版本、演示数据隔离、secret/token 泄漏扫描全部通过。
- `git diff --check`：通过。

验收结论：CI 外部执行代码已由可变标签切换为可审计的不可变提交，依赖更新具备自动分组入口，PR 与发布镜像共享高危漏洞门禁和有期限的例外契约，本阶段达到本地完成标准，待远端 Actions 最终验收。

## 进度记录 28

完成时间：2026-07-16 12:57:07 +08:00（Asia/Shanghai）
优化项：P2 前端用户文案、错误码映射与可重试状态统一
状态：本地实现、定向行为测试、完整前端质量门禁、生产构建和仓库治理检查均已完成，准备提交并推送至 PRAgent-test，由 GitHub Actions 复核。

实施内容：

1. 新增集中式用户文案模块，统一通用错误、集成配置服务名称与加载/保存结果、审查详情自动刷新等用户可见文本，消除同一流程中中英文混用和重复模板。
2. 为后端 `BAD_REQUEST`、`PAYLOAD_TOO_LARGE`、`TOO_MANY_REQUESTS`、`UNAUTHORIZED`、`FORBIDDEN`、`TASK_NOT_FOUND`、`INTERNAL_ERROR` 以及前端网络/契约错误建立稳定中文映射。
3. `RequestError` 对未知错误码不再直接展示后端 message，而是使用调用方安全 fallback，避免数据库地址、内部异常或实现细节进入用户界面；本地表单等普通 `Error` 仍保留可操作提示。
4. 集成配置部分加载失败从一次性 warning toast 改为页面内持久 warning banner，并提供“重试”入口；其他成功返回的配置仍正常应用，重试成功后自动清除旧错误状态。
5. 集成配置保存作为短操作继续使用 toast，但成功、部分失败、失败服务和服务端重新同步结果统一为中文；GitHub/MySQL/RabbitMQ 技术名保留，`Review Policy` 统一为“审查策略”。
6. 审查详情首次加载失败只保留页面 error alert 与既有“重新加载”入口，不再重复弹 toast；后台状态轮询失败只更新非打断式中文 warning banner，连续达到阈值后暂停并提示手动刷新。
7. 新增错误映射、内部消息隔离、集成加载重试状态和详情轮询降噪测试；未新增依赖，未新增或提交测试辅助脚本，也未提交 README 工作区中的既有修改。

涉及文件：

- repoguard-frontend/src/utils/userMessages.ts
- repoguard-frontend/src/utils/errors.ts
- repoguard-frontend/src/utils/errors.test.ts
- repoguard-frontend/src/features/integrations/composables/useIntegrationConfigPersistence.ts
- repoguard-frontend/src/features/integrations/composables/useIntegrationConfigPersistence.test.ts
- repoguard-frontend/src/features/review-detail/composables/useReviewDetailLoader.ts
- repoguard-frontend/src/features/review-detail/composables/useReviewDetailLoader.test.ts
- repoguard-frontend/src/pages/IntegrationsPage.vue
- 代码优化审查报告.md

验证结果：

- 定向测试：错误映射、集成配置持久化、审查详情加载器共 3 个测试文件、14 个测试全部通过。
- 前端完整质量门禁：`vue-tsc -b`、ESLint 和 Vitest 全部通过；26 个测试文件、97 个测试全部通过。
- 前端生产构建：Vite 构建成功；初始 JavaScript gzip 181.1 KiB / 190.0 KiB、初始 CSS gzip 27.1 KiB / 32.0 KiB、最大异步 JavaScript gzip 133.9 KiB / 140.0 KiB，体积预算全部通过。
- Production readiness quick guard：空白检查、跟踪文件治理、Flyway 命名/重复版本、演示数据隔离、secret/token 泄漏扫描全部通过。
- `git diff --check`：通过。

验收结论：用户可见错误已由分散的服务端 message 和中英文模板收敛为稳定文案契约；页面加载、短操作与后台轮询分别使用可重试 banner、toast 和非打断 banner，关键失败状态不再消失或重复打断用户，本项达到本地完成标准，待远端 Actions 最终验收。

## 进度记录 29

完成时间：2026-07-16 13:10:12 +08:00（Asia/Shanghai）
优化项：复审收尾——GitHub Actions Node 24 运行时升级与回退门禁
状态：官方版本核验、工作流升级、供应链契约、后端全量验证和仓库治理门禁均已完成，准备提交并推送至 PRAgent-test，由 GitHub Actions 进行实际兼容性验收。

实施内容：

1. 使用 `gh api` 查询九个官方 Action 的最新 Release、解析发布标签对应的 40 位提交，并读取各版本 `action.yml`；确认 checkout、setup-java、setup-node、upload-artifact、github-script、dependency-review 及三个 Docker Action 均明确使用 Node 24。
2. 将 checkout 升级至 v7.0.0、setup-java 至 v5.5.0、setup-node 至 v7.0.0、upload-artifact 至 v7.0.1、github-script 至 v9.0.0、dependency-review 至 v5.0.0，并继续固定到已核验提交 SHA。
3. 将 setup-buildx 升级至 v4.2.0、login-action 至 v4.4.0、build-push-action 至 v7.3.0；现有构建上下文、登录参数、镜像标签、digest 输出及推送策略保持不变。
4. Trivy Action 不在远端 Node 20 弃用注解列表中，且上一项已通过 PR 产物和双镜像扫描，因此继续保留已验证的 v0.36.0 固定提交，避免无关升级扩大变更面。
5. 扩展 `SupplyChainPolicyTest`，为九个经 `gh` 验证的 Node 24 Action 建立最低主版本基线，允许后续升级但禁止回退到已弃用运行时的旧主版本。
6. 修复供应链测试对 YAML `- uses:` 简写的识别盲点；不可变 SHA 和运行时基线规则现在同时覆盖命名步骤的 `uses:` 与匿名步骤的 `- uses:`。
7. 未新增依赖，未新增或提交测试辅助脚本，也未提交 README 工作区中的既有修改。

涉及文件：

- .github/workflows/auto-create-pr.yml
- .github/workflows/pr-quality.yml
- .github/workflows/real-chain-smoke.yml
- .github/workflows/release-images.yml
- .github/workflows/repository-governance.yml
- .github/workflows/worker-concurrency-retest.yml
- repoguard-backend/src/test/java/com/repoguard/agent/quality/SupplyChainPolicyTest.java
- 代码优化审查报告.md

验证结果：

- 官方元数据核验：九个升级目标的发布提交均为 40 位 SHA，`action.yml` 的 `runs.using` 均为 Node 24。
- `SupplyChainPolicyTest`：5 个测试全部通过，0 failure，0 error。
- 后端全量 `mvn verify`：1,642 个测试，0 failure，0 error，2 skipped，BUILD SUCCESS。
- JaCoCo 0.8.15：覆盖率门禁全部通过。
- Production readiness quick guard：空白检查、跟踪文件治理、Flyway 命名/重复版本、演示数据隔离、secret/token 泄漏扫描全部通过。
- `git diff --check`：通过。

验收结论：工作流执行基础已从 Node 20 旧主版本整体迁移至经官方元数据验证的 Node 24 发布，并由可执行最低主版本契约防止回退；本阶段达到本地完成标准，待远端 Actions 验证输入兼容性和弃用注解清零。

## 进度记录 30

完成时间：2026-07-16 13:37:13 +08:00（Asia/Shanghai）
优化项：复审遗留——Dependabot Low 级 esbuild 漏洞最小升级修复
状态：告警定位、最小依赖升级、官方 npm 审计、完整前端质量门禁、生产构建和仓库治理检查均已完成，准备提交并推送至 PRAgent-test；默认分支告警将在 PR 合并后关闭。

实施内容：

1. 使用 `gh api` 读取仓库唯一未关闭的 Dependabot 告警，确认 `repoguard-frontend/package-lock.json` 中 esbuild 受 GHSA-g7r4-m6w7-qqqr 影响，漏洞范围为 `>=0.27.3 <0.28.1`，首个修复版本为 0.28.1。
2. 追踪依赖链为 `vite@7.3.5 → esbuild@0.27.7`；查询 npm 官方元数据后确认 Vite 7.3.6 将 esbuild 范围扩展为 `^0.27.0 || ^0.28.0`，因此选择同主版本补丁升级，不跨到 Vite 8。
3. 将 Vite 开发依赖从 `^7.2.4` 收口至 `^7.3.6`，并刷新传递依赖到 esbuild 0.28.1；没有把 esbuild 添加为直接依赖，也没有使用 `overrides` 强制跨主版本解析。
4. 使用 npm 官方 registry 重新生成相关锁文件条目，所有 esbuild 平台包统一为 0.28.1，Vite 和 esbuild 的 resolved 来源保持为 npm 官方地址。
5. npm 官方 audit 返回 0 vulnerabilities；原有类型检查、lint、单元测试、生产构建和 bundle budget 均保持通过。
6. 未新增生产或测试依赖类别，未新增或提交测试辅助脚本，也未提交 README 工作区中的既有修改。

涉及文件：

- repoguard-frontend/package.json
- repoguard-frontend/package-lock.json
- 代码优化审查报告.md

验证结果：

- 依赖树：`vite@7.3.6 → esbuild@0.28.1`，Vite 在 plugin-vue 与 Vitest 依赖树中正确去重。
- npm 官方 `audit --audit-level=low`：0 vulnerabilities。
- 前端完整质量门禁：`vue-tsc -b`、ESLint 和 Vitest 全部通过；26 个测试文件、97 个测试全部通过。
- 前端生产构建：Vite 7.3.6 构建成功；初始 JavaScript gzip 181.1 KiB / 190.0 KiB、初始 CSS gzip 27.1 KiB / 32.0 KiB、最大异步 JavaScript gzip 133.9 KiB / 140.0 KiB，体积预算全部通过。
- Production readiness quick guard：空白检查、跟踪文件治理、Flyway 命名/重复版本、演示数据隔离、secret/token 泄漏扫描全部通过。
- `git diff --check`：通过。

验收结论：已通过上游同主版本补丁升级消除唯一已知 Low 级前端供应链漏洞，没有引入直接依赖或跨主版本迁移风险；测试分支依赖树和 npm 官方审计已清零，待远端门禁验证并在 PR 合并后由 GitHub 关闭默认分支告警。

## 进度记录 31

完成时间：2026-07-18 18:15:41 +08:00（Asia/Shanghai）
优化项：增量复审——认证事务安全、部署入口、外部响应边界与架构演进
状态：全量静态审查和可执行本地验证已完成；本记录仅沉淀问题、证据和实施顺序，尚未修改生产代码、测试代码或部署配置。

### 总体评价

RepoGuard 已超过普通 MVP 水平，具备较成熟的生产化基础：审查任务采用数据库状态机、提交后发布、RabbitMQ 发布确认与失败补偿，任务具有 PR + commit 幂等约束；通知链路具备 Outbox、重试和投递状态；认证链路使用内存访问令牌、HttpOnly 刷新 Cookie、CSRF 校验、会话版本与一次性刷新令牌；CI 已覆盖前后端质量门禁、真实 MySQL/RabbitMQ 上下文、依赖审查、镜像扫描、SBOM 和供应链来源证明。

当前最合适的演进方向仍是“模块化单体 + 独立 Worker”，不建议在业务边界和横向扩展语义尚未完全闭环时拆分微服务。正式发布前应先解决下面的 P0/P1 问题。

### P0 阻断项

#### P0-1 刷新令牌复用后的安全失效操作会被事务回滚

证据：

- `repoguard-backend/src/main/java/com/repoguard/agent/service/impl/AuthServiceImpl.java:243`
- `repoguard-backend/src/main/java/com/repoguard/agent/service/impl/AuthServiceImpl.java:254`
- `repoguard-backend/src/main/java/com/repoguard/agent/service/impl/AuthServiceImpl.java:401`
- `repoguard-backend/src/main/java/com/repoguard/agent/common/BusinessException.java:3`
- `repoguard-backend/src/test/java/com/repoguard/agent/service/impl/AuthServiceImplTest.java:400`

现状：`refresh()` 整体带 `@Transactional`。检测到已撤销刷新令牌后，代码先轮换用户 session version、撤销该用户所有活动刷新令牌、更新复用时间并记录审计，随后抛出继承自 `RuntimeException` 的 `BusinessException`。Spring 默认会在未处理的运行时异常离开事务方法时回滚事务，因此前述数据库安全失效操作和失败审计不会提交。官方语义参考：[Rolling Back a Declarative Transaction](https://docs.spring.io/spring-framework/reference/data-access/transaction/declarative/rolling-back.html)。

风险：系统虽然向客户端返回未授权并增加内存指标，但服务端会话版本与活动令牌仍可能保持有效，令牌复用检测没有达到预期的持久安全效果。现有 Mockito 单元测试只能证明 Mapper 方法被调用，不能证明事务最终提交。

建议：

1. 将事务内刷新逻辑改为返回 `SUCCESS / REUSED / INVALID` 等结果，先让内部事务正常提交，再由外层非事务门面转换为业务异常。
2. 或将复用后的会话失效放入独立 Spring Bean 的 `REQUIRES_NEW` 方法，避免同类自调用绕过代理；失效事务提交后再抛出异常。
3. 不建议直接在整个 `refresh()` 上宽泛配置 `noRollbackFor = BusinessException.class`，以免其他分支的部分写入被意外提交。
4. 增加真实 MySQL 集成测试：捕获 401 异常后重新查询数据库，确认 session version 已递增、全部活动刷新令牌已撤销、复用审计已持久化。

验收标准：复用已撤销令牌后，客户端收到未授权；新旧访问令牌均因 session version 变化失效；活动刷新令牌数为 0；安全审计与指标均保留。

### P1 高优先级项

#### P1-1 IP 部署 Compose 端口映射与容器监听端口不一致

证据：

- `docker-compose.ip.yml:102` 使用 `80:80`
- `repoguard-frontend/nginx.ip.conf:7` 监听 `8080`
- `repoguard-frontend/Dockerfile:13` 暴露 `8080`

风险：使用 `docker-compose.ip.yml` 启动后，宿主机 80 端口被映射到容器内没有监听的 80 端口，IP 部署模式无法正常访问前端。

建议：将映射改为 `80:8080`，并在 IP 部署检查中增加容器启动后的首页与 `/actuator/health` HTTP smoke test。

#### P1-2 生产 HTTP catch-all 允许未知 Host 使用完整系统

证据：

- `Caddyfile:10`
- `Caddyfile:15`
- `repoguard-backend/src/main/java/com/repoguard/agent/web/AuthSessionCookieManager.java:77`
- `repoguard-backend/src/main/java/com/repoguard/agent/web/AuthSessionCookieManager.java:106`

现状：规范域名站点由 Caddy 自动处理 HTTPS，但额外的 `:80` catch-all 会把未知 Host 和服务器 IP 的明文 HTTP 请求直接代理到前端。刷新 Cookie 与 CSRF Cookie 的 `Secure` 属性又根据当前请求是否为 HTTPS 动态决定。

风险：用户通过服务器 IP或未知域名访问时，可以在明文 HTTP 上传输账号密码、访问令牌和非 Secure 刷新 Cookie；同源访问不会被 CORS 阻止。

建议：将 HTTP catch-all 限制为内部健康检查，其余请求拒绝或跳转到规范 HTTPS 域名；生产环境增加显式配置并强制认证 Cookie `Secure=true`，启动检查应拒绝缺少规范域名或安全 Cookie 配置的生产实例。

#### P1-3 多标签页刷新令牌存在竞态和误清 Cookie 风险

证据：

- `repoguard-frontend/src/api/authRefreshCoordinator.ts:17`
- `repoguard-frontend/src/api/authRefreshCoordinator.ts:23`
- `repoguard-backend/src/main/java/com/repoguard/agent/service/impl/AuthServiceImpl.java:276`
- `repoguard-backend/src/main/java/com/repoguard/agent/controller/AuthController.java:126`

现状：前端的 `refreshPromise` 只能协调当前标签页；多个标签页共享 HttpOnly 刷新 Cookie，却没有跨标签页 single-flight。并发刷新中，一个请求成功轮换 Cookie，另一个请求可能使用旧令牌失败；Controller 对任何刷新运行时异常都会无条件清除认证 Cookie。

风险：失败响应可能清除另一个成功响应刚设置的新 Cookie；在修复 P0 后，稍晚到达的正常并发请求还可能被当成令牌复用并撤销全部会话。

建议：使用 Web Locks API 或 `BroadcastChannel` 建立跨标签页刷新协调；服务端引入 token family、短时间并发宽限或幂等刷新结果；仅在确认失败 Cookie 仍是当前代次时才清除认证 Cookie。补充两个浏览器上下文同时刷新的端到端测试。

#### P1-4 外部 HTTP 响应缺少大小上限

证据：

- `repoguard-backend/src/main/java/com/repoguard/agent/external/ExternalHttpResponseReader.java:13`
- `repoguard-backend/src/main/java/com/repoguard/agent/external/ExternalHttpResponseReader.java:16`
- `repoguard-backend/src/main/java/com/repoguard/agent/github/GithubPaginator.java:25`
- `docker-compose.prod.yml:116`

现状：成功和失败响应都先执行 `response.getBody().readAllBytes()`，没有检查 `Content-Length` 或流式读取上限；该读取器被 GitHub、LLM、连接探测和通知适配器共同使用。

风险：上游故障、恶意响应或错误页面可以一次性占用大量堆内存，在默认 800 MiB 容器限制下触发 OOM；错误状态响应同样会在状态判断前被完整读取。

建议：先校验可信的 `Content-Length`，再用 `readNBytes(limit + 1)` 做硬上限；为 GitHub 页面、LLM 响应、连接探测和通知响应分别设置合理限额，溢出时中止连接、记录集成类型与指标，但不记录敏感响应体。

### P2 架构与工程优化

#### P2-1 横向扩展语义需要从“单 JVM”收敛为显式部署契约

证据：

- `repoguard-backend/src/main/java/com/repoguard/agent/security/AuthAttemptLimiter.java:17`
- `repoguard-backend/src/main/java/com/repoguard/agent/security/AuthAttemptLimiter.java:47`
- `repoguard-backend/src/main/java/com/repoguard/agent/github/webhook/GithubWebhookRateLimiter.java:16`
- `repoguard-backend/src/main/java/com/repoguard/agent/github/webhook/GithubWebhookRateLimiter.java:50`
- `repoguard-backend/src/main/java/com/repoguard/agent/dashboard/DashboardSnapshotStore.java:19`
- `docker-compose.prod.yml:70`
- `docker-compose.prod.yml:120`

现状：认证和 Webhook 限流采用进程内 Map，并对整个 Map 加同步锁；Dashboard 缓存与快照也是节点本地状态。Worker split profile 新增独立 Worker 时，API 服务默认仍同时启用 Worker；官方部署脚本有保护，但直接运行 Compose profile 仍可能形成双消费者。

建议：近期单 API 实例可以继续使用本地实现，但要在生产配置和运行检查中明确限制；准备横向扩展时，将安全限流迁移到 Redis 原子计数或网关集中限流，缓存使用版本号/事件失效机制。运行角色收敛为互斥的 `api | worker | combined`，由启动校验拒绝冲突配置。

#### P2-2 保持模块化单体，但需要建立可执行领域边界

规模基线：后端生产 Java 673 个文件、测试 Java 373 个文件，单一 Maven 模块中 `service`、`dto`、`entity`、`mapper` 仍是主要依赖中心；`ReviewExecutionWorkflow` 构造器包含 12 个协作者，`AuthServiceImpl` 超过 500 行。

建议：按 `identity`、`review-orchestration`、`scm-github`、`notification`、`operations-reporting` 重组包和应用端口；使用 ArchUnit、Maven 模块或等价契约阻止 Controller 越过应用服务、领域之间直接依赖具体实现。优先拆分 `AuthServiceImpl` 中的凭据认证、会话令牌、账户锁定和认证审计，不为减少文件数量进行大规模合并，也暂不拆微服务。

#### P2-3 API 契约应由自研解析逐步迁移为真实 OpenAPI 产物

证据：

- `repoguard-backend/src/test/java/com/repoguard/agent/testsupport/OpenApiContractDocument.java:55`
- `repoguard-backend/src/test/java/com/repoguard/agent/testsupport/FrontendApiContractCatalog.java:16`
- `repoguard-backend/src/test/java/com/repoguard/agent/testsupport/FrontendApiContractCatalog.java:38`
- `repoguard-frontend/src/api/contracts.ts`

现状：现有反射生成 OpenAPI-like 文档、快照比对和前端契约正则解析对漂移控制很有价值，但维护了较多自研协议和源代码格式假设，`contracts.ts` 也已成为较大的集中式注册表。

建议：输出运行时真实 OpenAPI 文档，使用标准 OpenAPI 校验和差异检查，并生成 TypeScript DTO/Client；按领域拆分前端契约。保留现有 Controller 授权契约测试，因为它覆盖的是生成客户端无法替代的权限语义。

#### P2-4 前端轮询、取消和包体预算需要保留增长余量

证据：

- `repoguard-frontend/src/features/review-detail/composables/useReviewDetailPolling.ts:18`
- `repoguard-frontend/src/features/message-queue/composables/useMessageQueueHealth.ts:24`
- `repoguard-frontend/src/features/message-queue/composables/useMessageQueueHealth.ts:107`
- `repoguard-frontend/vite.config.ts:9`

建议：轮询在页面隐藏、离线和请求尚未完成时暂停，使用完成后递归定时器、退避和抖动，API 层统一支持 `AbortSignal`；任务状态并发量上升后再评估 SSE。当前初始 JavaScript gzip 181.1 KiB / 190.0 KiB、最大异步 JavaScript gzip 133.9 KiB / 140.0 KiB，均接近 95%，应增加 90% 预警并继续保持图表按路由懒加载。

#### P2-5 统一 JDK 文档、构建与发布门禁

证据：

- `README.md:26` 与 `README.md:52` 声明 Java/JDK 26
- `repoguard-backend/pom.xml:21` 与 `repoguard-backend/pom.xml:148` 强制 JDK 25
- `.github/workflows/pr-quality.yml:26` 使用 JDK 25
- `.github/workflows/release-images.yml:57` 使用 JDK 25
- `repoguard-backend/Dockerfile:1` 使用 Java 25 运行时
- `.github/workflows/release-images.yml:62` 使用 `mvn -B package`

建议：以 POM、CI 和容器的一致配置为准，将 README 环境要求统一为 JDK 25；使用 Maven Toolchains 或开发环境检查提供可操作提示。发布流水线将后端命令改为 `mvn -B verify`，确保 verify 阶段的 JaCoCo 89% line / 70% branch 门禁不会被发布任务绕过。

### 建议实施顺序

1. 第一批发布阻断修复：P0 刷新令牌事务提交语义、IP Compose 端口、Caddy HTTP catch-all 与生产 Secure Cookie。
2. 第二批认证与资源边界：跨标签页刷新协调、刷新接口限流、外部响应硬上限和对应集成测试。
3. 第三批部署收敛：互斥运行角色、多实例限流/缓存一致性契约、发布流水线统一 `verify`。
4. 第四批维护性演进：领域模块边界、真实 OpenAPI 生成客户端、前端轮询取消与包体余量治理。

### 本次验证结果

- 前端 TypeScript 类型检查：通过。
- 前端 ESLint：通过。
- 前端 Vitest：26 个测试文件、97 个测试全部通过。
- 前端 Vite 7.3.6 生产构建：通过；初始 JavaScript gzip 181.1 KiB / 190.0 KiB，初始 CSS gzip 27.1 KiB / 32.0 KiB，最大异步 JavaScript gzip 133.9 KiB / 140.0 KiB。
- Production readiness quick guard：空白检查、跟踪文件治理、Flyway 命名/重复版本、演示数据隔离和 secret/token 泄漏启发式扫描全部通过。
- 后端 `mvn -B verify`：未进入测试阶段；当前本机为 JDK 26.0.1，Maven Enforcer 按 POM 要求拒绝非 JDK 25 环境。该结果不记为测试失败或通过，而记为工具链阻断。
- 本次仅更新 `代码优化审查报告.md`，未修改生产代码、测试代码或脚本，未暂存、未提交，也未处理工作区内既有未跟踪文件。

验收结论：本次增量复审确认项目的异步可靠性、幂等、通知 Outbox、权限契约和前端质量门禁基础较好，同时识别出一个认证事务 P0 缺陷、两个部署入口 P1 缺陷以及认证并发、外部响应边界和横向扩展方面的优化项。建议在完成第一、第二批修复并以 JDK 25 执行后端全量 `mvn verify` 与真实 MySQL 事务测试后，再进入生产发布验收。

## 进度记录 32

完成时间：2026-07-18 18:31:19 +08:00（Asia/Shanghai）
优化项：P0 刷新令牌复用失效事务提交语义修复
状态：生产代码、事务边界单元测试、真实 MySQL 集成测试和本地后端全量质量门禁均已完成；未进行模块拆分或其他架构调整，待推送 PRAgent-test 后由 JDK 25 与 MySQL/RabbitMQ CI 复核。

实施内容：

1. 移除 `refresh()` 方法外层声明式事务，改为复用认证服务既有的 `REQUIRES_NEW TransactionTemplate` 执行完整刷新写事务。
2. 新增事务结果对象，事务内部对成功、令牌无效、令牌过期、账号不可用、会话版本变化、并发刷新失败和令牌复用分别返回结果；所有预期失败分支先正常提交撤销、session version 轮换和审计，再由事务外门面统一抛出 `UNAUTHORIZED`。
3. 保持新令牌签发与旧令牌 CAS 撤销在同一事务内；签发、审计或数据库出现非预期异常时仍由 `TransactionTemplate` 回滚，避免旧令牌已撤销但新令牌未持久化的半成品。
4. 未使用宽泛的 `noRollbackFor = BusinessException.class`，不会改变注册、登录、密码修改、令牌重置或其他认证失败的事务语义。
5. 新增轻量事务边界测试，通过记录型事务管理器断言复用令牌路径在向调用方抛出未授权前已经执行 1 次提交且没有回滚。
6. 扩展现有生产上下文集成测试，在真实 MySQL 中写入同一用户的一枚已撤销令牌和一枚活动令牌，调用 Spring 代理后的 `AuthService`，并断言异常返回后 session version 递增、两枚令牌均为 `REVOKED`、复用令牌 `last_used_at` 与失败审计已持久化；测试数据在 `finally` 中按唯一用户名清理。
7. 本项仅修改认证事务逻辑及其既有 Java 测试，没有新增依赖、迁移、测试辅助脚本或架构模块。

涉及文件：

- repoguard-backend/src/main/java/com/repoguard/agent/service/impl/AuthServiceImpl.java
- repoguard-backend/src/test/java/com/repoguard/agent/service/impl/AuthServiceImplTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/integration/ProductionRuntimeContextIntegrationTest.java
- 代码优化审查报告.md

验证结果：

- `AuthServiceImplTest`：26 个测试全部通过，0 failure，0 error；新增用例确认复用失效事务 1 次提交、0 次回滚。
- 本地后端全量 `mvn -B -Denforcer.skip=true verify`：1,653 个测试，0 failure，0 error，3 skipped，BUILD SUCCESS；JaCoCo line 89% / branch 70% 门禁通过。
- 本地使用 JDK 26.0.1，因此只跳过 Maven Enforcer 的 JDK 25 工具链规则；生产代码和测试仍按 `--release 25` 编译。
- 新增真实 MySQL 用例已完成编译并能够启动生产 profile；本机 3306 服务的实际凭据与项目示例不匹配，Flyway 在建连阶段被拒绝，未修改该未知数据库。该用例由现有 `backend-integration` CI 使用隔离 MySQL 8.0.46 与 RabbitMQ 3.13.7、JDK 25 执行。
- 推送后远端 Pull Request Quality 工作流 `29641035018` 已完成复核：JDK 25 后端单元/契约与 JaCoCo、MySQL 8.0.46/RabbitMQ 3.13.7 生产上下文集成、前端类型检查/ESLint/Vitest/生产构建、依赖审查与 Trivy 扫描全部通过。
- `git diff --check`：通过。

验收结论：刷新令牌复用检测已从“执行失效操作后随业务异常回滚”改为“安全失效事务先提交、调用方再收到未授权”，同时保持成功刷新和非预期异常的原子回滚语义；本地单元与全量质量门禁通过，真实数据库提交语义已纳入现有生产上下文 CI，达到本地提交标准。

## 进度记录 33

完成时间：2026-07-18 18:44:45 +08:00（Asia/Shanghai）
优化项：P1 IP 部署前端端口映射修复
状态：部署配置修复、跨配置契约测试、本地后端全量质量门禁和生产就绪快速检查均已完成；未调整系统架构，待推送 PRAgent-test 后由远端 CI 复核。

实施内容：

1. 将 `docker-compose.ip.yml` 的前端端口映射由 `80:80` 修正为 `80:8080`，使宿主机 80 端口准确转发到前端容器内 Nginx 实际监听的 8080 端口。
2. 新增 `FrontendDeploymentContractTest`，直接解析 IP Compose YAML，同时读取前端 Dockerfile 与 IP 模式 Nginx 配置，断言 Docker `EXPOSE`、Nginx `listen` 和 Compose 容器端口三者一致，并断言宿主机仍仅发布 80 端口。
3. 契约测试不复制固定的容器端口常量：监听端口来自 Nginx 配置、镜像端口来自 Dockerfile、发布端口来自 Compose，后续任一配置单独漂移都会在质量门禁中失败。
4. 本项未改变域名生产部署、后端业务、数据库、依赖、迁移、运行架构或测试辅助脚本。

涉及文件：

- docker-compose.ip.yml
- repoguard-backend/src/test/java/com/repoguard/agent/quality/FrontendDeploymentContractTest.java
- 代码优化审查报告.md

验证结果：

- 定向 `FrontendDeploymentContractTest`：1 个测试通过，0 failure，0 error，0 skipped。
- 本地后端全量 `mvn -B -Denforcer.skip=true verify`：1,654 个测试，0 failure，0 error，3 skipped，BUILD SUCCESS；JaCoCo line 89% / branch 70% 门禁通过。
- 本地使用 JDK 26.0.1，因此只跳过 Maven Enforcer 的 JDK 25 工具链规则；生产代码和测试仍按 `--release 25` 编译。
- Production readiness quick：空白检查、跟踪文件治理、Flyway 命名/重复版本、演示数据隔离及 secret/token 泄漏启发式扫描全部通过。
- 推送后远端 Pull Request Quality 工作流 `29641424927` 已完成复核：JDK 25 后端单元/契约与 JaCoCo、MySQL 8.0.46/RabbitMQ 3.13.7 生产上下文集成、前端类型检查/ESLint/Vitest/生产构建、依赖审查与 Trivy 扫描全部通过。

验收结论：IP 部署入口已由“宿主机 80 转发到容器内无人监听的 80”修正为“宿主机 80 转发到前端 Nginx 实际监听的 8080”，并通过跨 Compose、Dockerfile、Nginx 的可执行契约防止端口再次漂移，达到本地提交标准。

## 进度记录 34

完成时间：2026-07-18 19:08:56 +08:00（Asia/Shanghai）
优化项：P1 生产 HTTP catch-all 收紧与认证 Cookie Secure 强制
状态：入口配置、生产启动约束、Cookie 写入逻辑、部署契约、后端全量门禁和生产就绪快速检查均已完成；未调整系统架构，待推送 PRAgent-test 后由真实 Caddy/Compose 与 JDK 25 CI 复核。

实施内容：

1. 将 Caddy `:80` catch-all 从全量反向代理改为仅放行精确路径 `/actuator/health`，其余未知 Host 或服务器 IP 的明文 HTTP 请求统一返回 421，不再暴露登录、注册、刷新及业务 API。
2. 保留规范域名的 Caddy 自动 HTTP→HTTPS 跳转；Caddy 官方自动 HTTPS 规则会把带 Host 匹配的跳转插入用户 catch-all 之前，因此规范域名仍跳转 HTTPS，未知 Host 才进入 421 fallback。参考：[Automatic HTTPS](https://caddyserver.com/docs/automatic-https)。
3. 将生产 Compose 中前端与 Caddy 的 `REPOGUARD_FRONTEND_SERVER_NAME` 改为必填插值，缺少规范域名时在 Compose 配置阶段直接失败，不再让前端回退到 `_` 或让 Caddy 以空站点启动。
4. 新增 `repoguard.auth.secure-cookies`：生产 profile 与生产 Compose 默认值均为 `true`，示例生产环境文件显式记录该值，API 和拆分 Worker 使用同一部署契约。
5. 在 `AuthProperties` 的生产类 profile 启动校验中强制 `secure-cookies=true`；即使运维人员显式覆盖为 `false`，应用也会拒绝启动。
6. `AuthSessionCookieManager` 在配置强制或请求本身为 HTTPS 时为刷新 Cookie 与 CSRF Cookie 设置 `Secure`。配置值以单一布尔量注入，未新增 `web→security` 包依赖；架构循环基线保持不变。
7. 扩展部署契约测试，校验 Caddy 明文 catch-all、生产 Compose 必填域名、API/Worker Secure Cookie 默认值和生产 profile 默认值；同时在现有 Pull Request Quality 中新增生产边缘配置作业，使用 `docker compose config --quiet` 和固定摘要的 Caddy 2.11.4 `validate` 做真实解析。
8. 本项未新增依赖、数据库迁移、测试辅助脚本、模块或服务，也未改变 IP 部署模式与业务 API 契约。

涉及文件：

- .github/workflows/pr-quality.yml
- .env.prod.example
- Caddyfile
- docker-compose.prod.yml
- repoguard-backend/src/main/java/com/repoguard/agent/security/AuthProperties.java
- repoguard-backend/src/main/java/com/repoguard/agent/web/AuthSessionCookieManager.java
- repoguard-backend/src/main/resources/application-prod.yml
- repoguard-backend/src/test/java/com/repoguard/agent/config/ApplicationProfileConfigurationTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/controller/AuthControllerTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/quality/FrontendDeploymentContractTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/security/AuthPropertiesTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/web/AuthSessionCookieManagerTest.java
- 代码优化审查报告.md

验证结果：

- 定向认证、配置、部署与架构测试：57 个测试通过，0 failure，0 error，0 skipped。
- 首轮全量验证准确拦截了新增的 `web→security` 循环依赖；改为注入单一配置值后，`ApplicationArchitectureTest` 重新通过，未修改或放宽架构基线。
- 最终本地后端全量 `mvn -B -Denforcer.skip=true verify`：1,659 个测试，0 failure，0 error，3 skipped，BUILD SUCCESS；JaCoCo line 89% / branch 70% 门禁通过。
- 本地使用 JDK 26.0.1，因此只跳过 Maven Enforcer 的 JDK 25 工具链规则；生产代码和测试仍按 `--release 25` 编译。
- Production readiness quick：空白检查、跟踪文件治理、Flyway 命名/重复版本、演示数据隔离及 secret/token 泄漏启发式扫描全部通过。
- 本机未安装 Docker/Caddy，可执行配置解析由本项新增的远端 `Production edge configuration` 作业在推送后完成。

验收结论：未知 Host 的明文 HTTP 攻击面已从完整系统缩减为只读健康检查；生产认证 Cookie 不再依赖请求代理头决定安全属性，而由生产配置和启动校验双重强制。规范域名 HTTPS 跳转、健康检查、Compose 必填项、Cookie 属性与实际 Caddy 解析均有对应门禁，达到本地提交标准。

## 进度记录 35

完成时间：2026-07-18 19:16:16 +08:00（Asia/Shanghai）
优化项：P1 生产 Caddy 真实校验命令修正
状态：远端失败原因已定位并完成最小修正；重新触发的真实 Caddy/Compose 与全量 Pull Request Quality 已全部通过，不涉及生产配置或架构语义变更。

实施内容：

1. 首次远端 Pull Request Quality 工作流 `29642188664` 中，生产 Compose 真实解析已通过，JDK 25 后端、MySQL/RabbitMQ 集成、前端、依赖审查与 Trivy 也全部通过。
2. 新增的 Caddy 校验步骤以 127 退出；原始日志显示固定摘要的 Caddy 镜像已成功拉取，但该镜像没有把 `caddy` 设置为容器 ENTRYPOINT，Docker 因而尝试直接执行不存在的 `validate` 文件。
3. 将容器参数由 `validate --config ...` 修正为显式的 `caddy validate --config ...`，保持镜像版本、摘要、环境变量、只读挂载和验证目标不变。
4. 本项仅修正现有质量工作流的一行调用命令并更新既有优化记录，没有新增脚本、依赖、生产代码、测试代码或架构调整。

涉及文件：

- .github/workflows/pr-quality.yml
- 代码优化审查报告.md

验证结果：

- 失败作业原始日志与退出码已核对：失败发生于容器进程创建阶段，错误为 `exec: "validate": executable file not found in $PATH`，Caddyfile 尚未进入解析阶段。
- `git diff --check` 与 Production readiness quick 在前一提交中均已通过；本次为单行命令修正，最终结果以重新触发的远端真实 Caddy 2.11.4 校验为准。
- 修正后远端 Pull Request Quality 工作流 `29642346824` 已全部通过：生产 Compose 真实解析与 Caddy 2.11.4 `validate`、JDK 25 后端单元/契约与 JaCoCo、MySQL 8.0.46/RabbitMQ 3.13.7 生产上下文集成、前端类型检查/ESLint/Vitest/生产构建、依赖审查及 Trivy 扫描均成功。

验收结论：CI 已改为显式调用镜像内 `/usr/bin/caddy` 对应命令入口，消除了将子命令误当可执行文件的包装错误；真实生产边缘配置和其余 Pull Request Quality 门禁均已通过，本项关闭。

## 进度记录 36

完成时间：2026-07-18 19:49:25 +08:00（Asia/Shanghai）
优化项：P1 多标签页刷新协调与刷新失败 Cookie 竞态修复
状态：前后端协调逻辑、服务端并发宽限、配置与指标、定向回归、前后端全量质量门禁和生产就绪快速检查均已完成；未调整系统架构，待推送 PRAgent-test 后由 JDK 25 与真实 MySQL/RabbitMQ CI 复核。

实施内容：

1. 前端刷新协调器在浏览器支持时使用 Web Locks API，以固定锁名在同源标签页之间串行执行刷新；保留原有单标签页 `refreshPromise`，同一标签页和不同标签页均不会重复发起正常刷新。
2. 使用 `BroadcastChannel` 发布短生命周期的刷新结果；成功消息只携带内存访问令牌，不读取、持久化或广播 HttpOnly 刷新令牌。等待标签页复用成功结果并更新本地内存令牌，减少第二次轮换。
3. Web Locks 不可用时保留兼容回退：并发失败请求短暂等待其他标签页的成功广播；页面已退出且本地会话标记已清除时拒绝应用迟到消息，避免广播恢复已注销会话。
4. 服务端新增默认 5 秒、可配置且必须非负的刷新并发宽限窗口。已轮换旧令牌在窗口内再次到达时返回未授权并记录独立审计和指标，但不递增 session version、也不撤销当前有效刷新令牌；超过窗口或没有轮换时间证据时仍按真正的令牌复用执行全会话失效。
5. 刷新接口失败时不再写入清除认证 Cookie 的 `Set-Cookie` 响应，避免并发失败响应覆盖另一请求刚轮换成功的刷新 Cookie；客户端失败路径仍按既有会话逻辑清理本地访问令牌和会话标记。
6. 为并发宽限新增低基数指标 `repoguard.auth.refresh_token.concurrent_replay`，与安全复用指标分离，便于区分正常浏览器竞态和超窗复用告警。
7. 生产 Compose 的 API/Worker、基础应用配置和示例环境文件统一暴露 `REPOGUARD_AUTH_REFRESH_CONCURRENCY_GRACE_SECONDS=5`；部署契约测试验证两个运行角色使用相同默认值。
8. 本项未新增依赖、数据库迁移、测试辅助脚本、模块或服务，也未修改既有架构边界与循环依赖基线。

涉及文件：

- .env.prod.example
- docker-compose.prod.yml
- repoguard-backend/src/main/java/com/repoguard/agent/controller/AuthController.java
- repoguard-backend/src/main/java/com/repoguard/agent/observability/RepoGuardMetrics.java
- repoguard-backend/src/main/java/com/repoguard/agent/observability/ReviewMetricsRecorder.java
- repoguard-backend/src/main/java/com/repoguard/agent/security/AuthProperties.java
- repoguard-backend/src/main/java/com/repoguard/agent/service/impl/AuthServiceImpl.java
- repoguard-backend/src/main/resources/application.yml
- repoguard-backend/src/test/java/com/repoguard/agent/config/ApplicationProfileConfigurationTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/controller/AuthControllerTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/observability/RepoGuardMetricsTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/quality/FrontendDeploymentContractTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/security/AuthPropertiesTest.java
- repoguard-backend/src/test/java/com/repoguard/agent/service/impl/AuthServiceImplTest.java
- repoguard-frontend/src/api/authRefreshCoordinator.ts
- repoguard-frontend/src/api/authRefreshCoordinator.test.ts
- 代码优化审查报告.md

验证结果：

- 后端认证、控制器、配置、部署和指标定向回归：88 个测试通过，0 failure，0 error，0 skipped；同时覆盖 5 秒内并发回放不撤销会话、超过窗口仍执行安全失效、刷新失败不再清 Cookie、负配置启动拒绝和新增指标实际计数。
- 前端协调器与 API 客户端定向回归：2 个测试文件、12 个测试全部通过；覆盖共享浏览器锁 single-flight、无 Web Locks 的成功广播回退及退出后拒绝迟到广播。
- 前端 TypeScript 类型检查与 ESLint：全部通过。
- 前端全量 Vitest：27 个测试文件、100 个测试全部通过。首次与后端干净编译、Vite 构建四路并行时，既有路由冒烟用例触发一次 5 秒超时；空闲环境隔离复跑 4/4 通过，随后全量复跑 100/100 通过，未修改该既有测试超时或断言。
- 前端 Vite 7.3.6 生产构建通过；初始 JavaScript gzip 181.9 KiB / 190.0 KiB，初始 CSS gzip 27.1 KiB / 32.0 KiB，最大异步 JavaScript gzip 133.9 KiB / 140.0 KiB，均未超过预算。
- 本地后端先执行干净全量 `mvn -B clean verify -Denforcer.skip=true`，补充指标断言后再以最终工作区执行全量 `mvn -B verify -Denforcer.skip=true`：最终 1,662 个测试，0 failure，0 error，3 skipped，BUILD SUCCESS；JaCoCo line 89% / branch 70% 门禁通过，架构契约未放宽。
- 本地使用 JDK 26.0.1，因此只跳过 Maven Enforcer 的 JDK 25 工具链规则；生产代码和测试仍按 `--release 25` 完整重新编译。
- Production readiness quick：空白检查、跟踪文件治理、Flyway 命名/重复版本、演示数据隔离及 secret/token 泄漏启发式扫描全部通过。

验收结论：跨标签页刷新已由单标签页内存锁扩展为浏览器同源协调，兼容环境通过短等待复用成功广播；服务端将短时正常并发与真正令牌复用分开处理，且失败响应不会再覆盖成功轮换的 Cookie。刷新令牌仍仅存在于 HttpOnly Cookie，超窗复用仍撤销全部会话，本项达到本地提交标准。
