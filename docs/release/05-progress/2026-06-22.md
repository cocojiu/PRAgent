# 2026-06-22 优化进度记录

### 2026-06-22 架构优化 OPT-ARCH-004-A MQ 健康查询边界抽取

已完成：
- 按 `05-企业级线上项目优化方案.md` v1.1 的优化顺序，在 `OPT-ARCH-003` Worker 关键边界抽取后进入 `OPT-ARCH-004`。
- 复核 `ReviewTaskExecutorImpl` 剩余职责，确认继续抽 `ReviewExecutionWorkflow` 会触碰事务重试和执行日志主编排，当前收益低于行为漂移风险，因此按顺序转入 MQ 运维边界拆分。
- 新增 `MessageQueueHealthQueryService`，集中承载 MQ 健康响应组装、RabbitMQ runtime 连接探测、拓扑 DTO、队列指标、补偿状态和异常任务列表派生。
- `MessageQueueHealthServiceImpl#getHealth` 改为委托 `MessageQueueHealthQueryService#getHealth`，使 `/api/v1/message-queue/health` 只读查询路径不再直接混入手动重入队命令细节。
- 清理 `MessageQueueHealthServiceImpl` 中已迁出的健康查询 helper，仅保留重入队命令、事务、timeline、发布失败恢复和审计相关逻辑。
- 本轮仅迁移健康查询边界，不改变重入队接口、响应结构、RabbitMQ 连接探测语义、异常任务状态映射和指标记录语义。

验证记录：
- `mvn "-Dtest=MessageQueueHealthServiceImplTest,MessageQueueHealthControllerTest,SpringBeanConstructorSelectionTest" test` 通过；3 个相关测试类共 9 个用例全部成功。
- `mvn "-Dtest=GithubPullRequestClientImplTest,GithubCommentPublishServiceImplTest,GithubCommentDraftPublisherTest,GithubCommentPublishPlanBuilderTest,GithubCommentPublishGuardTest,GithubCommentPublicationRecorderTest,GithubCommentWritebackCheckBuilderTest,GithubConnectionProbeTest,ReviewServiceImplTest,ReviewTaskExecutorImplTest,MessageQueueHealthServiceImplTest,MessageQueueHealthControllerTest,SpringBeanConstructorSelectionTest" test` 通过；13 个相关测试类共 85 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- MQ 健康查询已形成独立只读边界，`MessageQueueHealthServiceImpl` 不再直接维护健康响应组装、runtime probe、指标 DTO 和异常任务列表派生逻辑。
- `OPT-ARCH-004` 仍剩余手动重入队命令、审计记录和 Rabbit runtime 探测可继续拆分；下一步建议做 `OPT-ARCH-004-B`：抽取 `ReviewTaskRequeueService` 或 `MessageQueueAuditRecorder`，优先把写命令和审计细节从门面服务中迁出。

### 2026-06-22 架构优化 OPT-ARCH-004-B MQ 重入队审计边界抽取

已完成：
- 按 `OPT-ARCH-004-A` 的下一步建议继续拆分 MQ 运维写路径中的外围职责，优先抽取重入队审计记录边界。
- 新增 `MessageQueueAuditRecorder`，集中承载 MQ 手动重入队操作的 `SystemSettingLog` 构造、action 裁剪、状态写入和创建时间设置。
- `MessageQueueHealthServiceImpl#requeueTask` 和 `prepareRequeue` 改为通过 `MessageQueueAuditRecorder#recordRequeue` 记录成功、发布失败、任务不存在、状态不允许和任务已 claim 等审计结果。
- 从 `MessageQueueHealthServiceImpl` 移除 `SystemSettingLog` 构造细节和 `SystemSettingLogMapper` 成员字段，避免门面服务继续直接维护审计落库模型。
- 本轮仅迁移审计调用所有权，不改变重入队状态转换、消息发布时间、失败恢复、审计文案和审计状态。

验证记录：
- `mvn "-Dtest=MessageQueueHealthServiceImplTest,MessageQueueHealthControllerTest,SpringBeanConstructorSelectionTest" test` 通过；3 个相关测试类共 9 个用例全部成功。
- `mvn "-Dtest=GithubPullRequestClientImplTest,GithubCommentPublishServiceImplTest,GithubCommentDraftPublisherTest,GithubCommentPublishPlanBuilderTest,GithubCommentPublishGuardTest,GithubCommentPublicationRecorderTest,GithubCommentWritebackCheckBuilderTest,GithubConnectionProbeTest,ReviewServiceImplTest,ReviewTaskExecutorImplTest,MessageQueueHealthServiceImplTest,MessageQueueHealthControllerTest,SpringBeanConstructorSelectionTest" test` 通过；13 个相关测试类共 85 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- MQ 手动重入队审计已形成独立边界，后续如需扩展操作者、审计类型或结构化审计内容，可以围绕 `MessageQueueAuditRecorder` 演进。
- `MessageQueueHealthServiceImpl` 当前仍保留手动重入队命令主流程、状态转换、timeline 写入、消息发布和发布失败恢复；下一步建议继续 `OPT-ARCH-004-C`：抽取 `ReviewTaskRequeueService`，把写命令主流程从健康服务门面中迁出。

### 2026-06-22 架构优化 OPT-ARCH-004-C MQ 手动重入队命令边界抽取

已完成：
- 按 `OPT-ARCH-004` 的拆分目标继续收敛 MQ 运维写路径，抽取手动重入队命令主流程。
- 新增 `ReviewTaskRequeueService`，集中承载重入队允许性校验、`PUBLISH_FAILED -> QUEUED` 状态重置、发布字段清理、timeline 写入、事务内落库、事务后消息发布、发布失败恢复和审计调用。
- `MessageQueueHealthServiceImpl#requeueTask` 改为委托 `ReviewTaskRequeueService#requeueTask`，不再直接维护 `ReviewTaskMessage` 构造、发布失败恢复、timeline 追加和事务执行逻辑。
- `MessageQueueHealthServiceImpl` 收敛为 MQ 运维门面：`getHealth` 委托 `MessageQueueHealthQueryService`，`requeueTask` 委托 `ReviewTaskRequeueService`，兼容构造器保留以保护既有测试和调用方式。
- 本轮保持重入队语义不变：事务内先保存 `QUEUED`，提交后发布消息；发布失败时恢复为 `PUBLISH_FAILED` 并写失败 timeline 与审计。

验证记录：
- `mvn "-Dtest=MessageQueueHealthServiceImplTest,MessageQueueHealthControllerTest,SpringBeanConstructorSelectionTest" test` 通过；3 个相关测试类共 9 个用例全部成功。
- `mvn "-Dtest=GithubPullRequestClientImplTest,GithubCommentPublishServiceImplTest,GithubCommentDraftPublisherTest,GithubCommentPublishPlanBuilderTest,GithubCommentPublishGuardTest,GithubCommentPublicationRecorderTest,GithubCommentWritebackCheckBuilderTest,GithubConnectionProbeTest,ReviewServiceImplTest,ReviewTaskExecutorImplTest,MessageQueueHealthServiceImplTest,MessageQueueHealthControllerTest,SpringBeanConstructorSelectionTest" test` 通过；13 个相关测试类共 85 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- `OPT-ARCH-004` 的核心查询/命令分离已完成：健康查询、手动重入队命令和审计记录均已有独立边界。
- 下一步建议继续 `OPT-ARCH-004-D`：抽取 `RabbitRuntimeHealthProbe`，把 RabbitMQ runtime 连接探测从 `MessageQueueHealthQueryService` 中拆出，使健康响应组装和外部连接探测进一步分离。

### 2026-06-22 架构优化 OPT-ARCH-004-D RabbitMQ runtime 健康探测边界抽取

已完成：
- 按 `OPT-ARCH-004` 的剩余拆分点继续收敛 MQ 健康查询链路，抽取 RabbitMQ runtime 连接探测边界。
- 新增 `RabbitRuntimeHealthProbe`，集中承载 RabbitMQ runtime 连接状态探测、健康检查超时、异常降级和中断恢复处理。
- `MessageQueueHealthQueryService` 改为通过 `RabbitRuntimeHealthProbe#connectionStatus` 获取 runtime 连接状态，不再直接持有 `RabbitTemplate` 或维护 `CompletableFuture` 超时探测细节。
- 保留原有探测语义：Rabbit channel open 时返回 `CONNECTED`；超时、执行异常、运行时异常或中断时返回 `DISCONNECTED`，中断场景继续恢复线程中断标记。
- 本轮仅迁移外部连接探测所有权，不改变健康响应结构、配置版本、拓扑、队列指标、补偿状态和异常任务列表派生。

验证记录：
- `mvn "-Dtest=MessageQueueHealthServiceImplTest,MessageQueueHealthControllerTest,SpringBeanConstructorSelectionTest" test` 通过；3 个相关测试类共 9 个用例全部成功。
- `mvn "-Dtest=GithubPullRequestClientImplTest,GithubCommentPublishServiceImplTest,GithubCommentDraftPublisherTest,GithubCommentPublishPlanBuilderTest,GithubCommentPublishGuardTest,GithubCommentPublicationRecorderTest,GithubCommentWritebackCheckBuilderTest,GithubConnectionProbeTest,ReviewServiceImplTest,ReviewTaskExecutorImplTest,MessageQueueHealthServiceImplTest,MessageQueueHealthControllerTest,SpringBeanConstructorSelectionTest" test` 通过；13 个相关测试类共 85 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- `OPT-ARCH-004` 已完成健康查询、重入队命令、审计记录和 runtime 探测四个边界抽取，`MessageQueueHealthServiceImpl` 已收敛为 MQ 运维门面服务。
- 下一步建议按优化顺序进入 `OPT-ARCH-005`：统一任务来源、发布状态和回写状态枚举，优先从当前散落的 `MANUAL_INPUT`、`GITHUB_PR_PICKER`、`EXISTING_REUSED`、`already_published`、`downgraded_to_pr_comment` 等字符串边界开始。

### 2026-06-22 架构优化 OPT-ARCH-005-A 任务来源枚举边界抽取

已完成：
- 按优化顺序进入 `OPT-ARCH-005`，优先处理任务来源和触发来源中散落的 `MANUAL_INPUT`、`GITHUB_PR_PICKER`、`EXISTING_REUSED` 字符串。
- 新增 `ReviewTaskSource` 枚举，集中承载存库 code、DTO 小写 code、创建来源归一化和缺省来源兜底。
- `ManualReviewCreationService` 改为使用 `ReviewTaskSource` 解析手动创建来源、写入 `source/triggerSource`、记录任务创建指标，并生成对外小写响应字段。
- `ReviewTaskListItemAssembler` 改为使用 `ReviewTaskSource.dtoCodeOrDefault` 输出任务来源和触发来源，保持 DTO 小写契约不变。
- 本轮不修改数据库字段、不改变 Controller/API 响应结构、不改变列表过滤入参语义，仅收敛内部来源判断边界。

验证记录：
- `mvn "-Dtest=ReviewServiceImplTest,ReviewControllerTest,RepoGuardMetricsTest,SpringBeanConstructorSelectionTest" test` 通过；4 个相关测试类共 53 个用例全部成功。
- `mvn "-Dtest=GithubPullRequestClientImplTest,GithubCommentPublishServiceImplTest,GithubCommentDraftPublisherTest,GithubCommentPublishPlanBuilderTest,GithubCommentPublishGuardTest,GithubCommentPublicationRecorderTest,GithubCommentWritebackCheckBuilderTest,GithubConnectionProbeTest,ReviewServiceImplTest,ReviewControllerTest,RepoGuardMetricsTest,ReviewTaskExecutorImplTest,MessageQueueHealthServiceImplTest,MessageQueueHealthControllerTest,SpringBeanConstructorSelectionTest" test` 通过；15 个相关测试类共 101 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- 任务来源/触发来源已具备统一枚举边界，新增来源时可以先扩展 `ReviewTaskSource`，再按需接入创建、查询和 DTO 映射。
- `OPT-ARCH-005` 仍剩余 GitHub 评论 target type、发布状态、批次状态等字符串边界；下一步建议继续 `OPT-ARCH-005-B`：优先抽取 GitHub 评论目标类型或评论发布状态枚举，保护 DTO 仍输出前端需要的小写格式。

### 2026-06-22 架构优化 OPT-ARCH-005-B GitHub 评论目标类型与发布状态枚举边界抽取

已完成：
- 按 `OPT-ARCH-005` 的下一步建议继续收敛 GitHub 评论链路中的字符串状态边界。
- 新增 `GithubCommentTargetType` 枚举，集中承载 `pull_request` 和 `line` 两类 GitHub 评论目标类型，并提供字符串归一化、`isPullRequest`、`isLine` 等内部判断方法。
- 新增 `GithubCommentPublicationStatus` 枚举，集中承载 `published`、`downgraded_to_pr_comment`、`failed`、`already_published`、`skipped` 等评论发布结果状态。
- `GithubCommentWriter` 改为使用 `GithubCommentTargetType` 判断 PR 评论、行评论和行评论降级路径，使用 `GithubCommentPublicationStatus` 输出发布成功、降级和失败状态。
- `GithubCommentPublishPlanBuilder` 改为使用 `GithubCommentPublicationStatus` 输出已发布跳过和普通跳过状态。
- `GithubCommentPreviewItemBuilder` 改为使用 `GithubCommentTargetType` 派生预览项目标类型和 PR 评论兜底原因。
- 本轮保持数据库字段、DTO 字段类型和前端小写字符串契约不变，仅收敛内部判断与状态常量来源。

验证记录：
- `mvn "-Dtest=GithubPullRequestClientImplTest,GithubCommentPreviewItemBuilderTest,GithubCommentPreviewResponseAssemblerTest,GithubCommentPreviewServiceImplTest,GithubCommentPublishPlanBuilderTest,GithubCommentPublishServiceImplTest,GithubCommentDraftPublisherTest,GithubCommentPublicationRecorderTest,GithubCommentPublishGuardTest,GithubCommentWritebackCheckBuilderTest,SpringBeanConstructorSelectionTest" test` 通过；11 个相关测试类共 40 个用例全部成功。
- `mvn "-Dtest=GithubPullRequestClientImplTest,GithubCommentPreviewItemBuilderTest,GithubCommentPreviewResponseAssemblerTest,GithubCommentPreviewServiceImplTest,GithubCommentPublishPlanBuilderTest,GithubCommentPublishServiceImplTest,GithubCommentDraftPublisherTest,GithubCommentPublicationRecorderTest,GithubCommentPublishGuardTest,GithubCommentWritebackCheckBuilderTest,GithubConnectionProbeTest,ReviewServiceImplTest,ReviewControllerTest,RepoGuardMetricsTest,ReviewTaskExecutorImplTest,MessageQueueHealthServiceImplTest,MessageQueueHealthControllerTest,SpringBeanConstructorSelectionTest" test` 通过；18 个相关测试类共 112 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- GitHub 评论目标类型和评论发布状态已具备统一枚举边界，评论预览、发布计划和实际写回路径不再各自散落维护核心 target/status 字符串。
- `OPT-ARCH-005` 下一步建议继续 `OPT-ARCH-005-C`：抽取 GitHub 评论批次状态或写回失败分类状态边界，重点保护历史记录、发布批次和失败诊断 DTO 的字符串契约不变。

### 2026-06-22 架构优化 OPT-ARCH-005-C GitHub 评论发布批次状态枚举边界抽取

已完成：
- 按 `OPT-ARCH-005` 的状态枚举收敛方向，继续处理 GitHub 评论发布历史中的批次状态字符串。
- 新增 `GithubCommentPublicationBatchStatus` 枚举，集中承载 `empty`、`completed`、`skipped`、`partial_failed`、`failed` 等发布批次状态。
- `GithubCommentPublicationRecorder#resolvePublicationBatchStatus` 改为通过 `GithubCommentPublicationBatchStatus` 输出批次状态，不再在记录器中散落维护批次状态字符串。
- 本轮保持发布历史表字段、DTO 输出和前端小写字符串契约不变，仅收敛批次状态常量来源。

验证记录：
- `mvn "-Dtest=GithubCommentPublicationRecorderTest,GithubCommentPublishServiceImplTest,GithubCommentPublishPlanBuilderTest,GithubCommentPreviewServiceImplTest,SpringBeanConstructorSelectionTest" test` 通过；5 个相关测试类共 14 个用例全部成功。
- `mvn "-Dtest=GithubPullRequestClientImplTest,GithubCommentPreviewItemBuilderTest,GithubCommentPreviewResponseAssemblerTest,GithubCommentPreviewServiceImplTest,GithubCommentPublishPlanBuilderTest,GithubCommentPublishServiceImplTest,GithubCommentDraftPublisherTest,GithubCommentPublicationRecorderTest,GithubCommentPublishGuardTest,GithubCommentWritebackCheckBuilderTest,GithubConnectionProbeTest,ReviewServiceImplTest,ReviewControllerTest,RepoGuardMetricsTest,ReviewTaskExecutorImplTest,MessageQueueHealthServiceImplTest,MessageQueueHealthControllerTest,SpringBeanConstructorSelectionTest" test` 通过；18 个相关测试类共 112 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- GitHub 评论发布状态、目标类型和批次状态均已有枚举边界，评论写回链路的核心状态字符串进一步集中。
- 下一步建议继续 `OPT-ARCH-005-D`：复核 GitHub 写回失败分类或通知事件状态是否还有散落字符串，若收益有限则进入后续 `OPT-ARCH-006` 连接测试策略化。

### 2026-06-22 架构优化 OPT-ARCH-005-D GitHub 写回失败分类枚举边界抽取

已完成：
- 按 `OPT-ARCH-005-C` 的下一步建议继续收敛 GitHub 写回失败诊断链路中的分类字符串边界。
- 新增 `GithubWritebackFailureCategory` 枚举，集中承载 `github_token_missing`、`github_token_invalid`、`github_permission_denied`、`github_target_not_found`、`github_rate_limited`、`github_writeback_timeout`、`github_service_unavailable`、`github_repository_not_configured`、`github_comment_position_invalid`、`github_writeback_failed` 等写回失败分类。
- `GithubWritebackFailureClassifier` 改为通过 `GithubWritebackFailureCategory#code` 和 `categoryMarker` 输出及识别内部分类，不再在分类器中散落维护主要失败分类字符串。
- 本轮保持失败原因、修复建议、DTO 输出字段和前端小写字符串契约不变，仅收敛内部失败分类常量来源。
- 保留对外部历史消息 `category=github_timeout` 的兼容识别，避免旧错误消息或上游标记失效。

验证记录：
- `mvn "-Dtest=GithubWritebackFailureClassifierTest,GithubCommentWritebackCheckBuilderTest,GithubCommentPublicationRecorderTest,GithubCommentPublishServiceImplTest,SpringBeanConstructorSelectionTest" test` 通过；5 个相关测试类共 27 个用例全部成功。
- `mvn "-Dtest=GithubPullRequestClientImplTest,GithubWritebackFailureClassifierTest,GithubCommentPreviewItemBuilderTest,GithubCommentPreviewResponseAssemblerTest,GithubCommentPreviewServiceImplTest,GithubCommentPublishPlanBuilderTest,GithubCommentPublishServiceImplTest,GithubCommentDraftPublisherTest,GithubCommentPublicationRecorderTest,GithubCommentPublishGuardTest,GithubCommentWritebackCheckBuilderTest,GithubConnectionProbeTest,ReviewServiceImplTest,ReviewControllerTest,RepoGuardMetricsTest,ReviewTaskExecutorImplTest,MessageQueueHealthServiceImplTest,MessageQueueHealthControllerTest,SpringBeanConstructorSelectionTest" test` 通过；19 个相关测试类共 128 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- `OPT-ARCH-005` 已完成任务来源、GitHub 评论目标类型、评论发布状态、发布批次状态和写回失败分类的枚举边界抽取，核心状态字符串已从主要业务判断路径收敛到领域枚举。
- 下一步建议按优化顺序进入 `OPT-ARCH-006`：连接测试策略化。优先复核 `ConnectionTestServiceImpl` 当前是否直接编排 GitHub、LLM、MySQL、RabbitMQ probe 细节，并设计统一 `ConnectionProbe` 策略接口，保持保存配置测试和临时提交配置测试语义不变。

### 2026-06-22 架构优化 OPT-ARCH-006-A LLM 连接探测边界抽取

已完成：
- 按优化顺序进入 `OPT-ARCH-006`，先复核 `ConnectionTestServiceImpl` 的连接测试职责边界。
- 确认当前 GitHub、MySQL、RabbitMQ 已具备独立 probe 类，但 LLM `/chat/completions` 探测、请求构造、超时设置、响应解析校验和错误归一化仍直接维护在 `ConnectionTestServiceImpl` 内部。
- 新增 `LlmConnectionProbe`，集中承载 LLM 连接测试的请求构造、`max_tokens` 下限/上限保护、温度兜底、响应内容抽取、评审 JSON 校验和错误消息收敛。
- `ConnectionTestServiceImpl#testReviewPolicy` 改为委托 `LlmConnectionProbe#probe`，服务层仅保留已保存配置/临时提交配置解析、未配置兜底和统一 DTO 响应组装。
- 新增 `LlmConnectionProbeTest`，独立覆盖 LLM smoke response 解析、Authorization 透传、`max_tokens` 下限保护和 malformed review JSON 失败诊断。
- 本轮不改变 Controller/API 响应结构、不改变保存配置测试和临时提交配置测试语义、不修改数据库字段，仅迁移外部依赖探测细节。

验证记录：
- `mvn "-Dtest=ConnectionTestServiceImplTest,LlmConnectionProbeTest,LlmConnectionProbeResponseParserTest,GithubConnectionProbeTest,MysqlConnectionProbeTest,RabbitMqConnectionProbeTest,SystemConfigServiceImplTest,SystemConfigControllerTest,SpringBeanConstructorSelectionTest" test` 通过；9 个相关测试类共 50 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- `ConnectionTestServiceImpl` 已不再直接维护 LLM HTTP 探测细节，GitHub、LLM、MySQL、RabbitMQ 四类外部依赖均具备独立 probe 承载点。
- `OPT-ARCH-006` 尚未完成统一策略接口；下一步建议继续 `OPT-ARCH-006-B`：引入轻量 `ConnectionProbe` 策略接口或 registry，逐步把 provider 路由和 `probeSavedConfig/probeSubmittedConfig/markChecked` 语义从门面服务中下沉，避免新增 provider 时继续修改大方法。

### 2026-06-22 架构优化 OPT-ARCH-006-B 连接探测统一策略接口抽取

已完成：
- 按 `OPT-ARCH-006-A` 的下一步建议继续收敛连接测试策略边界，优先引入轻量统一接口，而不是一次性迁移所有 provider 路由和持久化标记逻辑。
- 新增 `ConnectionProbe<T>` 策略接口，定义 `provider()` 和 `probe(T config)` 两个基础契约，为后续 registry 化和 provider 扩展提供共同边界。
- `GithubConnectionProbe`、`LlmConnectionProbe`、`MysqlConnectionProbe`、`RabbitMqConnectionProbe` 均实现 `ConnectionProbe`，统一暴露 provider code 和配置探测入口。
- `MysqlConnectionProbe`、`RabbitMqConnectionProbe` 保留 `runtimeProbe` 与 `configuredProbe` 语义，同时通过 `probe(config)` 代理到已保存/提交配置探测，避免破坏运行时配置诊断。
- `ConnectionTestServiceImpl` 改为引用各 probe 的 provider code，并通过 `probe(config)` 执行 GitHub、MySQL、RabbitMQ 的配置探测，减少服务层对具体探测方法名的依赖。
- 补充 GitHub、LLM、MySQL、RabbitMQ probe 测试，覆盖 provider 标识和统一 `probe` 返回结果，保护后续 registry 迁移时 provider code 不漂移。
- 本轮不改变 Controller/API 响应结构、不改变保存配置测试和临时提交配置测试语义、不改变 MySQL/RabbitMQ runtime 诊断输出，仅抽取策略接口并适配现有探测类。

验证记录：
- `mvn "-Dtest=ConnectionTestServiceImplTest,LlmConnectionProbeTest,LlmConnectionProbeResponseParserTest,GithubConnectionProbeTest,MysqlConnectionProbeTest,RabbitMqConnectionProbeTest,SystemConfigServiceImplTest,SystemConfigControllerTest,SpringBeanConstructorSelectionTest" test` 通过；9 个相关测试类共 54 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- GitHub、LLM、MySQL、RabbitMQ 已具备统一连接探测策略接口，`OPT-ARCH-006` 的策略化基础边界已经形成。
- `ConnectionTestServiceImpl` 当前仍保留 provider 路由、已保存配置查询、临时提交配置拼装和 mark checked 持久化标记；下一步建议继续 `OPT-ARCH-006-C`：抽取连接测试上下文/结果组装或 registry 路由，逐步让新增 provider 不再修改大方法。

### 2026-06-22 架构优化 OPT-ARCH-006-C 服务集成连接测试 Runner 抽取

已完成：
- 按 `OPT-ARCH-006-B` 的下一步建议继续收敛连接测试编排边界，优先处理 MySQL/RabbitMQ 两类形态一致的服务集成连接测试。
- 新增 `ServiceIntegrationConnectionTestRunner`，集中承载“runtime 探测 + 已保存/提交配置探测 + mark checked + DTO 诊断字段组装”的通用流程。
- `ConnectionTestServiceImpl#testMysqlConnection` 和 `testRabbitMqConnection` 改为委托对应 runner，服务层仅保留 provider 查询、临时提交配置拼装和方法级路由。
- MySQL/RabbitMQ 的 `runtime_config`、`saved_config`、`submitted_config`、`runtimeHealthy`、`savedConfigHealthy`、`mismatch`、`runtimeConnectionStatus`、`savedConfigStatus` 等响应语义保持不变。
- 新增 `ServiceIntegrationConnectionTestRunnerTest`，独立覆盖提交配置不持久化、保存配置探测后 mark checked、无保存配置时回退 runtime 诊断三类关键路径。
- 本轮不改变 Controller/API 响应结构、不改变数据库字段、不迁移 GitHub/LLM 连接测试，以避免一次性扩大到不同配置模型。

验证记录：
- `mvn "-Dtest=ConnectionTestServiceImplTest,ServiceIntegrationConnectionTestRunnerTest,LlmConnectionProbeTest,LlmConnectionProbeResponseParserTest,GithubConnectionProbeTest,MysqlConnectionProbeTest,RabbitMqConnectionProbeTest,SystemConfigServiceImplTest,SystemConfigControllerTest,SpringBeanConstructorSelectionTest" test` 通过；10 个相关测试类共 57 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- MySQL/RabbitMQ 服务集成连接测试的重复编排已从 `ConnectionTestServiceImpl` 下沉到 runner，服务层对运行时探测、配置探测和诊断 DTO 组装细节的直接承担明显减少。
- `OPT-ARCH-006` 仍剩余 GitHub/LLM 两类差异化配置模型的路由和 mark checked 边界；下一步建议继续 `OPT-ARCH-006-D`：抽取 GitHub 集成连接测试 Runner 或连接测试结果工厂，进一步让门面服务只承担接口编排。

### 2026-06-22 架构优化 OPT-ARCH-006-D GitHub 集成连接测试 Runner 抽取

已完成：
- 按 `OPT-ARCH-006-C` 的下一步建议继续收敛连接测试编排边界，优先处理 GitHub 集成连接测试中仍留在门面服务里的 probe、mark checked 和普通结果组装逻辑。
- 新增 `GithubIntegrationConnectionTestRunner`，集中承载 GitHub 未配置兜底、已保存/提交配置探测、保存配置成功/失败打标、异常消息收敛和普通 `ConnectionTestResultDto` 组装。
- `ConnectionTestServiceImpl#testGithubIntegration` 改为委托 `GithubIntegrationConnectionTestRunner`，服务层仅保留已保存配置查询、临时提交配置拼装和方法级路由。
- 保持 GitHub token mask 复用逻辑、保存配置成功清理 `last_error`、失败写入 `last_error`、提交配置不持久化等语义不变。
- 新增 `GithubIntegrationConnectionTestRunnerTest`，独立覆盖未配置、保存配置成功打标、提交配置不持久化、保存配置失败打标四类关键路径。
- 本轮不改变 Controller/API 响应结构、不改变数据库字段、不迁移 LLM review policy 配置模型，避免把不同配置来源混到同一轮。

验证记录：
- `mvn "-Dtest=ConnectionTestServiceImplTest,GithubIntegrationConnectionTestRunnerTest,ServiceIntegrationConnectionTestRunnerTest,LlmConnectionProbeTest,LlmConnectionProbeResponseParserTest,GithubConnectionProbeTest,MysqlConnectionProbeTest,RabbitMqConnectionProbeTest,SystemConfigServiceImplTest,SystemConfigControllerTest,SpringBeanConstructorSelectionTest" test` 通过；11 个相关测试类共 61 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- GitHub、MySQL、RabbitMQ 三类连接测试的主要编排逻辑已经从 `ConnectionTestServiceImpl` 下沉到专用 runner；门面服务进一步收敛为配置查询、临时配置拼装和接口路由。
- `OPT-ARCH-006` 仍剩余 LLM review policy 连接测试的配置拼装和结果组装边界；下一步建议继续 `OPT-ARCH-006-E`：抽取 LLM review policy 连接测试 Runner，或先抽普通连接测试结果工厂，统一 GitHub/LLM 的基础响应组装。

### 2026-06-22 架构优化 OPT-ARCH-006-E LLM Review Policy 连接测试 Runner 抽取

已完成：
- 按 `OPT-ARCH-006-D` 的下一步建议继续收敛连接测试编排边界，处理 LLM review policy 连接测试中仍留在门面服务里的未配置兜底、probe 调用和普通结果组装逻辑。
- 新增 `LlmReviewPolicyConnectionTestRunner`，集中承载 LLM 配置缺失兜底、LLM probe 调用和普通 `ConnectionTestResultDto` 组装。
- `ConnectionTestServiceImpl#testReviewPolicy` 改为委托 `LlmReviewPolicyConnectionTestRunner`，服务层仅保留已保存 review policy 查询、临时提交配置拼装和方法级路由。
- 保持 LLM disabled、base URL/model/API key 缺失、malformed review JSON、`checkedAt` 输出等语义不变。
- 新增 `LlmReviewPolicyConnectionTestRunnerTest`，独立覆盖未配置、probe 成功、probe 失败消息透传和 `checkedAt` 输出。
- 本轮不改变 Controller/API 响应结构、不改变数据库字段、不改变 review policy 提交配置解析语义。

验证记录：
- `mvn "-Dtest=ConnectionTestServiceImplTest,GithubIntegrationConnectionTestRunnerTest,LlmReviewPolicyConnectionTestRunnerTest,ServiceIntegrationConnectionTestRunnerTest,LlmConnectionProbeTest,LlmConnectionProbeResponseParserTest,GithubConnectionProbeTest,MysqlConnectionProbeTest,RabbitMqConnectionProbeTest,SystemConfigServiceImplTest,SystemConfigControllerTest,SpringBeanConstructorSelectionTest" test` 通过；12 个相关测试类共 64 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- `OPT-ARCH-006` 中 GitHub、LLM、MySQL、RabbitMQ 四类连接测试均已形成独立 probe 与 runner 边界，`ConnectionTestServiceImpl` 已收敛为配置查询、临时配置拼装和接口路由门面。
- 下一步建议复核 `ConnectionTestServiceImpl` 剩余职责：如仅剩配置拼装和 mapper 查询，可进入 `OPT-ARCH-007` 规则审查器插件化；若仍希望进一步收敛，可在 `OPT-ARCH-006-F` 抽取配置构建器或连接测试结果工厂，但应先评估收益，避免为抽象而抽象。

### 2026-06-22 架构优化 OPT-ARCH-006 阶段收口复核

已完成：
- 复核 `ConnectionTestServiceImpl` 当前剩余职责，确认核心外部连接探测细节、运行时诊断流程和普通结果组装已分别下沉到 GitHub、LLM、MySQL、RabbitMQ probe 与 runner。
- 当前门面服务主要保留 mapper 查询、临时提交配置拼装、mask secret 复用、保存配置状态打标和接口路由；这些职责仍与连接测试入口和配置模型转换直接相关。
- 评估 `OPT-ARCH-006-F` 继续抽取配置构建器或结果工厂的收益有限，容易变成低收益抽象，因此本阶段不继续扩大拆分。

验证记录：
- 沿用 `OPT-ARCH-006-E` 回归结果：`ConnectionTestServiceImplTest,GithubIntegrationConnectionTestRunnerTest,LlmReviewPolicyConnectionTestRunnerTest,ServiceIntegrationConnectionTestRunnerTest,LlmConnectionProbeTest,LlmConnectionProbeResponseParserTest,GithubConnectionProbeTest,MysqlConnectionProbeTest,RabbitMqConnectionProbeTest,SystemConfigServiceImplTest,SystemConfigControllerTest,SpringBeanConstructorSelectionTest` 共 12 个测试类、64 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- `OPT-ARCH-006` 达成“连接测试策略化”的核心目标：新增 provider 可优先扩展独立 probe/runner，`ConnectionTestServiceImpl` 不再直接维护具体外部依赖探测细节。
- 下一步按优化顺序进入 `OPT-ARCH-007`：规则审查器插件化，优先复核 `RuleBasedPullRequestReviewer` 中集中维护的规则检测逻辑，并选择一条低风险规则先抽为独立插件。

### 2026-06-22 架构优化 OPT-ARCH-007-A 首个逐行规则插件边界抽取

已完成：
- 按优化顺序进入 `OPT-ARCH-007`，先复核 `RuleBasedPullRequestReviewer`，确认当前多类规则检测、规则适用性判断、finding 组装和 PR 级规则仍集中在一个类中。
- 新增 `ReviewRule` 插件接口，定义逐行规则的 `id()` 与 `evaluate(context)` 契约。
- 新增 `ReviewRuleLineContext`，集中承载文件路径、行号、原始新增行、trim 后内容和规则配置，并提供规则启用/文件模式匹配判断。
- 新增 `ReviewFindingFactory`，承载 finding 组装、confidence、blocking、review dimension 和 impact 派生，避免插件各自复制 finding 结构。
- 新增 `StandardOutputLoggingRule`，将 `RG-JAVA-002` 标准输出日志规则从 `RuleBasedPullRequestReviewer` 中抽为首个独立逐行规则插件。
- `RuleBasedPullRequestReviewer` 保留整体扫描流程、PR 级规则和其它规则逻辑不变，仅通过 `evaluateLineRules` 调用插件列表，并用 `ReviewFindingFactory` 复用原 finding 组装语义。
- 新增 `StandardOutputLoggingRuleTest`，独立覆盖命中、禁用规则和文件模式不匹配三类路径。

验证记录：
- `mvn "-Dtest=RuleBasedPullRequestReviewerTest,StandardOutputLoggingRuleTest,LlmRuleReviewMergerTest,LlmPullRequestReviewerTest,ReviewRuleProviderTest,ReviewRuleConfigServiceImplTest,SpringBeanConstructorSelectionTest" test` 通过；7 个相关测试类共 29 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- `OPT-ARCH-007` 已建立最小插件化骨架，首个逐行规则 `RG-JAVA-002` 已独立测试，原 reviewer 行为回归保持不变。
- 下一步建议继续 `OPT-ARCH-007-B`：迁移另一条低耦合逐行规则，例如 `RG-JAVA-003` 固定休眠或 `RG-GEN-001` TODO/FIXME，并逐步让 `RuleBasedPullRequestReviewer` 从规则判断集合收敛为扫描编排器。

### 2026-06-22 架构优化 OPT-ARCH-007-B 固定休眠逐行规则插件化

已完成：
- 按 `OPT-ARCH-007-A` 的下一步建议继续迁移低耦合逐行规则，优先处理与首个插件形态一致的 `RG-JAVA-003` 固定休眠规则。
- 新增 `FixedSleepRule`，将 `Thread.sleep(` 检测、规则适用性判断和 finding 组装迁出 `RuleBasedPullRequestReviewer`。
- `RuleBasedPullRequestReviewer` 的 `lineRules` 插件列表加入 `FixedSleepRule`，删除内联 `RG-JAVA-003` 判断，扫描流程和其它规则逻辑保持不变。
- 新增 `FixedSleepRuleTest`，独立覆盖命中、禁用规则和文件模式不匹配三类路径。
- 本轮不迁移 PR 级规则、不改变风险等级计算、不改变 `ReviewFindingResult` 字段语义。

验证记录：
- `mvn "-Dtest=RuleBasedPullRequestReviewerTest,StandardOutputLoggingRuleTest,FixedSleepRuleTest,LlmRuleReviewMergerTest,LlmPullRequestReviewerTest,ReviewRuleProviderTest,ReviewRuleConfigServiceImplTest,SpringBeanConstructorSelectionTest" test` 通过；8 个相关测试类共 32 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- `RG-JAVA-002` 和 `RG-JAVA-003` 两条简单逐行 Java 规则已完成插件化，`RuleBasedPullRequestReviewer` 的逐行规则判断开始向插件列表迁移。
- 下一步建议继续 `OPT-ARCH-007-C`：迁移 `RG-GEN-001` TODO/FIXME 规则，继续扩大低风险逐行规则插件集合。

### 2026-06-22 架构优化 OPT-ARCH-007-C TODO/FIXME 逐行规则插件化

已完成：
- 按 `OPT-ARCH-007-B` 的下一步建议继续迁移低耦合逐行规则，处理 `RG-GEN-001` TODO/FIXME 规则。
- 新增 `TodoCommentRule`，将 `TODO` / `FIXME` 检测、规则适用性判断和 finding 组装迁出 `RuleBasedPullRequestReviewer`。
- `RuleBasedPullRequestReviewer` 的 `lineRules` 插件列表加入 `TodoCommentRule`，删除内联 `RG-GEN-001` 判断，扫描流程和其它规则逻辑保持不变。
- 新增 `TodoCommentRuleTest`，独立覆盖 TODO 命中、FIXME 命中、禁用规则和文件模式不匹配四类路径。
- 本轮不迁移 PR 级规则、不改变风险等级计算、不改变 `ReviewFindingResult` 字段语义。

验证记录：
- `mvn "-Dtest=RuleBasedPullRequestReviewerTest,StandardOutputLoggingRuleTest,FixedSleepRuleTest,TodoCommentRuleTest,LlmRuleReviewMergerTest,LlmPullRequestReviewerTest,ReviewRuleProviderTest,ReviewRuleConfigServiceImplTest,SpringBeanConstructorSelectionTest" test` 通过；9 个相关测试类共 36 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- `RG-JAVA-002`、`RG-JAVA-003`、`RG-GEN-001` 三条低耦合逐行规则已完成插件化，`RuleBasedPullRequestReviewer` 的简单规则判断正在持续向插件集合迁移。
- 下一步建议继续 `OPT-ARCH-007-D`：迁移 `RG-JAVA-001` 宽泛异常捕获规则，完成基础 Java 逐行规则的插件化闭环。

### 2026-06-22 架构优化 OPT-ARCH-007-D 宽泛异常捕获逐行规则插件化

已完成：
- 按 `OPT-ARCH-007-C` 的下一步建议继续迁移基础 Java 逐行规则，处理 `RG-JAVA-001` 宽泛异常捕获规则。
- 新增 `BroadExceptionCatchRule`，保持原有 `catch (Exception`、`catch(Throwable`、`catch (Throwable` 三类匹配条件不变，并将规则适用性判断和 finding 组装迁出 `RuleBasedPullRequestReviewer`。
- `RuleBasedPullRequestReviewer` 的 `lineRules` 插件列表加入 `BroadExceptionCatchRule`，删除内联 `RG-JAVA-001` 判断，扫描流程和其它规则逻辑保持不变。
- 新增 `BroadExceptionCatchRuleTest`，独立覆盖 `Exception`、`Throwable` 两类写法、禁用规则和文件模式不匹配四类路径。
- 本轮不迁移安全、状态、MQ、外部调用、数据库和 GitHub 回写等高风险项目规则，不改变风险等级计算和 `ReviewFindingResult` 字段语义。

验证记录：
- `mvn "-Dtest=RuleBasedPullRequestReviewerTest,BroadExceptionCatchRuleTest,StandardOutputLoggingRuleTest,FixedSleepRuleTest,TodoCommentRuleTest,LlmRuleReviewMergerTest,LlmPullRequestReviewerTest,ReviewRuleProviderTest,ReviewRuleConfigServiceImplTest,SpringBeanConstructorSelectionTest" test` 通过；10 个相关测试类共 40 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- 基础 Java/通用逐行规则 `RG-JAVA-001`、`RG-JAVA-002`、`RG-JAVA-003`、`RG-GEN-001` 已完成插件化，`RuleBasedPullRequestReviewer` 中的简单逐行规则已基本迁出。
- 下一步建议继续 `OPT-ARCH-007-E`：评估是否迁移 `RG-SECRET-001` 或 `RG-LOG-001` 安全类逐行规则；这类规则仍低耦合但风险更高，应逐条迁移并保持完整回归。

### 2026-06-22 架构优化 OPT-ARCH-007-E 敏感字面量逐行规则插件化

已完成：
- 按 `OPT-ARCH-007-D` 的下一步建议继续迁移安全类低耦合逐行规则，优先处理 `RG-SECRET-001` 硬编码敏感信息规则。
- 新增 `SensitiveLiteralRule`，保持原有关键字匹配范围不变：`password`、`secret`、`token`、`apikey`、`api_key`、`accesskey`、`access_key`，且仍要求同一行同时包含赋值符号和字符串引号。
- `RuleBasedPullRequestReviewer` 的 `lineRules` 插件列表加入 `SensitiveLiteralRule`，删除内联 `RG-SECRET-001` 判断，扫描流程、风险等级、finding 字段语义和安全规则维度保持不变。
- 新增 `SensitiveLiteralRuleTest`，独立覆盖敏感字面量命中、多个敏感关键字、无字面量赋值不命中、禁用规则和文件模式不匹配路径。
- 本轮不迁移 `RG-AUTH-001`，因为该规则依赖整段 patch 的授权 guard 语义，不属于纯逐行低耦合规则；也不迁移状态、MQ、外部调用、数据库和 GitHub 回写规则，避免扩大改动范围。

验证记录：
- `mvn "-Dtest=RuleBasedPullRequestReviewerTest,SensitiveLiteralRuleTest,BroadExceptionCatchRuleTest,StandardOutputLoggingRuleTest,FixedSleepRuleTest,TodoCommentRuleTest,LlmRuleReviewMergerTest,LlmPullRequestReviewerTest,ReviewRuleProviderTest,ReviewRuleConfigServiceImplTest,SpringBeanConstructorSelectionTest" test` 通过；11 个相关测试类共 45 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- `RG-JAVA-001`、`RG-JAVA-002`、`RG-JAVA-003`、`RG-GEN-001`、`RG-SECRET-001` 已完成插件化，`RuleBasedPullRequestReviewer` 中纯逐行、低耦合规则继续减少。
- 下一步建议继续 `OPT-ARCH-007-F`：迁移 `RG-LOG-001` 日志敏感信息规则。该规则仍为逐行检测且与 `RG-SECRET-001` 同属安全维度，适合单独迁移并保持完整回归。

### 2026-06-22 架构优化 OPT-ARCH-007-F 日志敏感信息逐行规则插件化

已完成：
- 按 `OPT-ARCH-007-E` 的下一步建议继续迁移安全类低耦合逐行规则，处理 `RG-LOG-001` 日志敏感信息规则。
- 新增 `SensitiveLoggingRule`，保持原有检测语义不变：同一行包含 `log.` 或 `logger.`，并包含 `token`、`secret`、`password`、`apikey`、`api_key`、`webhook` 任一敏感关键词时命中。
- `RuleBasedPullRequestReviewer` 的 `lineRules` 插件列表加入 `SensitiveLoggingRule`，删除内联 `RG-LOG-001` 判断，扫描流程、风险等级、finding 字段语义和安全规则维度保持不变。
- 新增 `SensitiveLoggingRuleTest`，独立覆盖日志敏感信息命中、`logger` 别名和多个敏感关键词、普通日志不命中、非日志敏感字面量不命中、禁用规则和文件模式不匹配路径。
- 本轮不迁移 `RG-AUTH-001`，因为该规则依赖整段 patch 授权 guard；不迁移状态、MQ、外部调用、数据库和 GitHub 回写规则，继续保持一轮一条规则的小步边界。

验证记录：
- `mvn "-Dtest=RuleBasedPullRequestReviewerTest,SensitiveLoggingRuleTest,SensitiveLiteralRuleTest,BroadExceptionCatchRuleTest,StandardOutputLoggingRuleTest,FixedSleepRuleTest,TodoCommentRuleTest,LlmRuleReviewMergerTest,LlmPullRequestReviewerTest,ReviewRuleProviderTest,ReviewRuleConfigServiceImplTest,SpringBeanConstructorSelectionTest" test` 通过；12 个相关测试类共 50 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- 基础 Java/通用逐行规则和两条安全类逐行规则已完成插件化：`RG-JAVA-001`、`RG-JAVA-002`、`RG-JAVA-003`、`RG-GEN-001`、`RG-SECRET-001`、`RG-LOG-001`。
- 下一步建议继续 `OPT-ARCH-007-G`：评估迁移 `RG-STATE-001` 任务状态字符串规则。该规则仍为逐行检测，但已涉及任务状态语义，迁移时需要重点回归风险等级和状态相关 finding 维度。

### 2026-06-22 架构优化 OPT-ARCH-007-G 任务状态字符串逐行规则插件化

已完成：
- 按 `OPT-ARCH-007-F` 的下一步建议继续迁移状态类逐行规则，处理 `RG-STATE-001` 任务状态字符串直接写入规则。
- 新增 `TaskStatusStringRule`，保持原有检测语义不变：命中 `setStatus("`、`setReviewStatus("`、`setHumanReviewStatus("` 任一 setter，且同一行包含 `QUEUED`、`REVIEWING`、`COMPLETED`、`FAILED`、`PUBLISH_FAILED`、`PENDING_HUMAN_REVIEW` 任一状态字符串时触发。
- `RuleBasedPullRequestReviewer` 的 `lineRules` 插件列表加入 `TaskStatusStringRule`，删除内联 `RG-STATE-001` 判断，扫描流程、风险等级、finding 字段语义和规则维度保持不变。
- 新增 `TaskStatusStringRuleTest`，独立覆盖状态字符串命中、三类 setter、多种状态值、变量状态/未知状态/非目标 setter 不命中、禁用规则和文件模式不匹配路径。
- 本轮不迁移 `RG-AUTH-001`，因为该规则依赖整段 patch 授权 guard；不迁移 MQ、外部调用、数据库和 GitHub 回写规则，继续保持一轮一条规则的小步边界。

验证记录：
- `mvn "-Dtest=RuleBasedPullRequestReviewerTest,TaskStatusStringRuleTest,SensitiveLoggingRuleTest,SensitiveLiteralRuleTest,BroadExceptionCatchRuleTest,StandardOutputLoggingRuleTest,FixedSleepRuleTest,TodoCommentRuleTest,LlmRuleReviewMergerTest,LlmPullRequestReviewerTest,ReviewRuleProviderTest,ReviewRuleConfigServiceImplTest,SpringBeanConstructorSelectionTest" test` 通过；13 个相关测试类共 55 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- `RG-JAVA-001`、`RG-JAVA-002`、`RG-JAVA-003`、`RG-GEN-001`、`RG-SECRET-001`、`RG-LOG-001`、`RG-STATE-001` 已完成插件化，`RuleBasedPullRequestReviewer` 剩余内联逐行规则进一步减少。
- 下一步建议继续 `OPT-ARCH-007-H`：迁移 `RG-MQ-001` RabbitMQ 发布调用规则。该规则仍为逐行检测，但涉及消息可靠性语义，应单独迁移并重点回归 HIGH 风险 finding 与消息可靠性维度。

### 2026-06-22 架构优化 OPT-ARCH-007-H RabbitMQ 发布调用逐行规则插件化

已完成：
- 按 `OPT-ARCH-007-G` 的下一步建议继续迁移消息可靠性类逐行规则，处理 `RG-MQ-001` RabbitMQ 发布调用规则。
- 新增 `RabbitMessagePublishRule`，保持原有检测语义不变：同一行包含 `rabbitTemplate.convertAndSend` 或 `amqpTemplate.convertAndSend` 时触发。
- `RuleBasedPullRequestReviewer` 的 `lineRules` 插件列表加入 `RabbitMessagePublishRule`，删除内联 `RG-MQ-001` 判断，扫描流程、HIGH 风险等级、finding 字段语义和 `MESSAGE_RELIABILITY_RULE` 维度保持不变。
- 新增 `RabbitMessagePublishRuleTest`，独立覆盖 `rabbitTemplate` 发布、`amqpTemplate` 发布、非发布调用不命中、禁用规则和文件模式不匹配路径。
- 本轮不迁移 `RG-AUTH-001`，因为该规则依赖整段 patch 授权 guard；不迁移外部调用、数据库和 GitHub 回写规则，继续保持一轮一条规则的小步边界。

验证记录：
- `mvn "-Dtest=RuleBasedPullRequestReviewerTest,RabbitMessagePublishRuleTest,TaskStatusStringRuleTest,SensitiveLoggingRuleTest,SensitiveLiteralRuleTest,BroadExceptionCatchRuleTest,StandardOutputLoggingRuleTest,FixedSleepRuleTest,TodoCommentRuleTest,LlmRuleReviewMergerTest,LlmPullRequestReviewerTest,ReviewRuleProviderTest,ReviewRuleConfigServiceImplTest,SpringBeanConstructorSelectionTest" test` 通过；14 个相关测试类共 60 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- `RG-JAVA-001`、`RG-JAVA-002`、`RG-JAVA-003`、`RG-GEN-001`、`RG-SECRET-001`、`RG-LOG-001`、`RG-STATE-001`、`RG-MQ-001` 已完成插件化，`RuleBasedPullRequestReviewer` 剩余内联逐行规则继续减少。
- 下一步建议继续 `OPT-ARCH-007-I`：迁移 `RG-EXT-001` 外部调用治理规则。该规则仍为逐行检测，但涉及外部依赖治理语义，迁移时需要重点回归 MEDIUM 风险 finding 和 `EXTERNAL_CALL_RULE` 维度。

### 2026-06-22 架构优化 OPT-ARCH-007-I 外部调用治理逐行规则插件化

已完成：
- 按 `OPT-ARCH-007-H` 的下一步建议继续迁移外部依赖治理类逐行规则，处理 `RG-EXT-001` 原始外部调用规则。
- 新增 `RawExternalCallRule`，保持原有检测语义不变：同一行包含 `restClient.`、`webClient.`、`RestTemplate`、`HttpClient.newHttpClient` 任一调用方关键词，且同时包含 `.retrieve()`、`.exchange(`、`.send(`、`.body(` 任一终止动作关键词时触发。
- `RuleBasedPullRequestReviewer` 的 `lineRules` 插件列表加入 `RawExternalCallRule`，删除内联 `RG-EXT-001` 判断，扫描流程、MEDIUM 风险等级、finding 字段语义和 `EXTERNAL_CALL_RULE` 维度保持不变。
- 新增 `RawExternalCallRuleTest`，独立覆盖 `restClient`、`webClient`、`RestTemplate`、`HttpClient` 多类调用形态，调用方/终止动作不同时存在不命中，禁用规则和文件模式不匹配路径。
- 本轮不迁移 `RG-AUTH-001`，因为该规则依赖整段 patch 授权 guard；不迁移数据库和 GitHub 回写规则，继续保持一轮一条规则的小步边界。

验证记录：
- `mvn "-Dtest=RuleBasedPullRequestReviewerTest,RawExternalCallRuleTest,RabbitMessagePublishRuleTest,TaskStatusStringRuleTest,SensitiveLoggingRuleTest,SensitiveLiteralRuleTest,BroadExceptionCatchRuleTest,StandardOutputLoggingRuleTest,FixedSleepRuleTest,TodoCommentRuleTest,LlmRuleReviewMergerTest,LlmPullRequestReviewerTest,ReviewRuleProviderTest,ReviewRuleConfigServiceImplTest,SpringBeanConstructorSelectionTest" test` 通过；15 个相关测试类共 65 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- `RG-JAVA-001`、`RG-JAVA-002`、`RG-JAVA-003`、`RG-GEN-001`、`RG-SECRET-001`、`RG-LOG-001`、`RG-STATE-001`、`RG-MQ-001`、`RG-EXT-001` 已完成插件化，`RuleBasedPullRequestReviewer` 剩余内联逐行规则已收敛到授权、数据库和 GitHub 回写相关规则。
- 下一步建议继续 `OPT-ARCH-007-J`：迁移 `RG-DB-002` 破坏性数据库迁移规则。该规则仍为逐行检测，但依赖 SQL 文件类型和 DDL 正则，应单独迁移并重点回归 `DATABASE_COMPATIBILITY_RULE` 维度。

### 2026-06-22 架构优化 OPT-ARCH-007-J 破坏性数据库迁移逐行规则插件化

已完成：
- 按 `OPT-ARCH-007-I` 的下一步建议继续迁移数据库兼容性类逐行规则，处理 `RG-DB-002` 破坏性数据库迁移规则。
- 新增 `DestructiveMigrationRule`，保持原有检测语义不变：仅在 `.sql` 文件中匹配 `drop table`、`drop column`、`truncate table` 三类 DDL 正则。
- `RuleBasedPullRequestReviewer` 的 `lineRules` 插件列表加入 `DestructiveMigrationRule`，删除内联 `RG-DB-002` 判断，扫描流程、HIGH 风险等级、finding 字段语义和 `DATABASE_COMPATIBILITY_RULE` 维度保持不变。
- 新增 `DestructiveMigrationRuleTest`，独立覆盖 `drop table`、`drop column`、`truncate table` 命中，非 SQL 文件不命中，非破坏性 DDL 不命中，禁用规则和文件模式不匹配路径。
- 本轮不迁移 `RG-AUTH-001`，因为该规则依赖整段 patch 授权 guard；不迁移 `RG-DB-003` 和 GitHub 回写规则，继续保持一轮一条规则的小步边界。

验证记录：
- `mvn "-Dtest=RuleBasedPullRequestReviewerTest,DestructiveMigrationRuleTest,RawExternalCallRuleTest,RabbitMessagePublishRuleTest,TaskStatusStringRuleTest,SensitiveLoggingRuleTest,SensitiveLiteralRuleTest,BroadExceptionCatchRuleTest,StandardOutputLoggingRuleTest,FixedSleepRuleTest,TodoCommentRuleTest,LlmRuleReviewMergerTest,LlmPullRequestReviewerTest,ReviewRuleProviderTest,ReviewRuleConfigServiceImplTest,SpringBeanConstructorSelectionTest" test` 通过；16 个相关测试类共 70 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- `RG-JAVA-001`、`RG-JAVA-002`、`RG-JAVA-003`、`RG-GEN-001`、`RG-SECRET-001`、`RG-LOG-001`、`RG-STATE-001`、`RG-MQ-001`、`RG-EXT-001`、`RG-DB-002` 已完成插件化，数据库兼容性规则已开始从 reviewer 内联逻辑迁出。
- 下一步建议继续 `OPT-ARCH-007-K`：迁移 `RG-DB-003` 新增非空字段缺少默认值规则。该规则同样依赖 SQL 文件类型和 DDL 正则，适合单独迁移并重点回归兼容性 finding 语义。

### 2026-06-22 架构优化 OPT-ARCH-007-K 新增非空字段逐行规则插件化

已完成：
- 按 `OPT-ARCH-007-J` 的下一步建议继续迁移数据库兼容性类逐行规则，处理 `RG-DB-003` 新增非空字段缺少默认值规则。
- 新增 `RequiredColumnWithoutDefaultRule`，保持原有检测语义不变：仅在 `.sql` 文件中匹配 `add column ... not null` 或 `add ... not null` 形式，且同一行不包含 ` default ` 时触发。
- `RuleBasedPullRequestReviewer` 的 `lineRules` 插件列表加入 `RequiredColumnWithoutDefaultRule`，删除内联 `RG-DB-003` 判断，扫描流程、HIGH 风险等级、finding 字段语义和 `DATABASE_COMPATIBILITY_RULE` 维度保持不变。
- 新增 `RequiredColumnWithoutDefaultRuleTest`，独立覆盖新增非空字段命中、省略 `column` 关键字命中、带默认值不命中、可空字段不命中、非 SQL 文件不命中、禁用规则和文件模式不匹配路径。
- 本轮不迁移 `RG-AUTH-001`，因为该规则依赖整段 patch 授权 guard；不迁移 GitHub 回写规则，继续保持一轮一条规则的小步边界。

验证记录：
- `mvn "-Dtest=RuleBasedPullRequestReviewerTest,RequiredColumnWithoutDefaultRuleTest,DestructiveMigrationRuleTest,RawExternalCallRuleTest,RabbitMessagePublishRuleTest,TaskStatusStringRuleTest,SensitiveLoggingRuleTest,SensitiveLiteralRuleTest,BroadExceptionCatchRuleTest,StandardOutputLoggingRuleTest,FixedSleepRuleTest,TodoCommentRuleTest,LlmRuleReviewMergerTest,LlmPullRequestReviewerTest,ReviewRuleProviderTest,ReviewRuleConfigServiceImplTest,SpringBeanConstructorSelectionTest" test` 通过；17 个相关测试类共 75 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- `RG-JAVA-001`、`RG-JAVA-002`、`RG-JAVA-003`、`RG-GEN-001`、`RG-SECRET-001`、`RG-LOG-001`、`RG-STATE-001`、`RG-MQ-001`、`RG-EXT-001`、`RG-DB-002`、`RG-DB-003` 已完成插件化，数据库兼容性逐行规则已完成从 reviewer 内联逻辑迁出。
- 下一步建议继续 `OPT-ARCH-007-L`：迁移 `RG-GH-001` GitHub 评论发布幂等规则。该规则仍为逐行检测，但涉及 GitHub 回写边界语义，应单独迁移并重点回归 `GITHUB_WRITEBACK_RULE` 维度。

### 2026-06-22 架构优化 OPT-ARCH-007-L GitHub 评论发布逐行规则插件化

已完成：
- 按 `OPT-ARCH-007-K` 的下一步建议继续迁移 GitHub 回写类逐行规则，处理 `RG-GH-001` GitHub 评论发布幂等规则。
- 新增 `GithubCommentDirectPublishRule`，保持原有检测语义不变：同一行包含 `publishPullRequestComments(`、`publishPullRequestComment(`、`publishLineComment(`、`/pulls/{pullNumber}/comments`、`/issues/{pullNumber}/comments` 任一直接发布调用或 API 路径时触发。
- `RuleBasedPullRequestReviewer` 的 `lineRules` 插件列表加入 `GithubCommentDirectPublishRule`，删除内联 `RG-GH-001` 判断，扫描流程、HIGH 风险等级、finding 字段语义和 `GITHUB_WRITEBACK_RULE` 维度保持不变。
- 新增 `GithubCommentDirectPublishRuleTest`，独立覆盖 PR 评论发布、单条 PR 评论发布、行评论发布、pulls/issues 评论 API 路径，非直接发布流程不命中，禁用规则和文件模式不匹配路径。
- 本轮不迁移 `RG-AUTH-001`，因为该规则依赖整段 patch 授权 guard，不属于前面已迁移的纯逐行局部判断。

验证记录：
- `mvn "-Dtest=RuleBasedPullRequestReviewerTest,GithubCommentDirectPublishRuleTest,RequiredColumnWithoutDefaultRuleTest,DestructiveMigrationRuleTest,RawExternalCallRuleTest,RabbitMessagePublishRuleTest,TaskStatusStringRuleTest,SensitiveLoggingRuleTest,SensitiveLiteralRuleTest,BroadExceptionCatchRuleTest,StandardOutputLoggingRuleTest,FixedSleepRuleTest,TodoCommentRuleTest,LlmRuleReviewMergerTest,LlmPullRequestReviewerTest,ReviewRuleProviderTest,ReviewRuleConfigServiceImplTest,SpringBeanConstructorSelectionTest" test` 通过；18 个相关测试类共 80 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- `RG-JAVA-001`、`RG-JAVA-002`、`RG-JAVA-003`、`RG-GEN-001`、`RG-SECRET-001`、`RG-LOG-001`、`RG-STATE-001`、`RG-MQ-001`、`RG-EXT-001`、`RG-DB-002`、`RG-DB-003`、`RG-GH-001` 已完成插件化，`RuleBasedPullRequestReviewer` 中的纯逐行局部规则已基本迁出。
- 下一步建议进行 `OPT-ARCH-007 阶段收口复核`：重点评估 `RG-AUTH-001` 是否应保持在 reviewer 中，或另行设计带 patch 级上下文的规则接口；同时复核 PR 级 `RG-API-001` 是否应进入后续独立 PR 级规则插件接口，避免把 patch 级/PR 级规则强行塞进逐行接口。

### 2026-06-22 架构优化 OPT-ARCH-007 阶段收口复核

已完成：
- 复核 `RuleBasedPullRequestReviewer` 当前剩余职责，确认纯逐行局部规则已迁入 `ReviewRule` 插件列表。
- 当前已插件化规则包括：`RG-JAVA-001`、`RG-JAVA-002`、`RG-JAVA-003`、`RG-GEN-001`、`RG-SECRET-001`、`RG-LOG-001`、`RG-STATE-001`、`RG-MQ-001`、`RG-EXT-001`、`RG-DB-002`、`RG-DB-003`、`RG-GH-001`。
- `RuleBasedPullRequestReviewer` 当前仍保留 diff 扫描编排、hunk 行号解析、风险聚合、`RG-AUTH-001` patch 级授权 guard 判断、`RG-API-001` PR 级测试缺口判断和少量路径/模式匹配辅助逻辑。
- 评估 `RG-AUTH-001` 不适合强行塞入当前 `ReviewRuleLineContext`：该规则不仅依赖当前新增行是否为 Controller 写接口，还依赖整段 patch 是否存在 `@RequireRole`、`@RequirePermission`、`@PreAuthorize`、`@Secured`、`@RolesAllowed` 等授权 guard。
- 评估 `RG-API-001` 同样不适合进入逐行接口：该规则需要 PR 级文件集合上下文，判断是否存在 Controller/API 变更以及同一个 PR 是否包含测试文件。
- 本轮不新增 patch 级或 PR 级规则接口，避免在纯逐行插件化刚完成后立即引入低收益抽象；后续如继续扩展，应先设计独立的 `PatchReviewRule` / `PullRequestReviewRule` 上下文，而不是扩胖 `ReviewRuleLineContext`。

验证记录：
- `mvn "-Dtest=RuleBasedPullRequestReviewerTest,GithubCommentDirectPublishRuleTest,RequiredColumnWithoutDefaultRuleTest,DestructiveMigrationRuleTest,RawExternalCallRuleTest,RabbitMessagePublishRuleTest,TaskStatusStringRuleTest,SensitiveLoggingRuleTest,SensitiveLiteralRuleTest,BroadExceptionCatchRuleTest,StandardOutputLoggingRuleTest,FixedSleepRuleTest,TodoCommentRuleTest,LlmRuleReviewMergerTest,LlmPullRequestReviewerTest,ReviewRuleProviderTest,ReviewRuleConfigServiceImplTest,SpringBeanConstructorSelectionTest" test` 通过；18 个相关测试类共 80 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- `OPT-ARCH-007` 达成“规则审查器插件化”的阶段目标：所有纯逐行局部规则已拆成独立规则类并具备单独测试，`RuleBasedPullRequestReviewer` 已从规则判断集合收敛为扫描编排器和跨行/跨文件规则承载点。
- 下一步建议进入 `OPT-ARCH-008`：按主方案顺序推进前端继续瘦身大页面。若后续继续规则体系演进，应另起一个专门阶段设计 patch 级 / PR 级规则插件接口，不在本轮继续扩张。

### 2026-06-22 架构优化 OPT-ARCH-008-A 登录页品牌展示面板组件化

已完成：
- 按 `OPT-ARCH-007` 阶段收口结论进入 `OPT-ARCH-008` 前端大页面瘦身，优先选择风险最低的 `LoginPage.vue` 左侧品牌展示区作为第一轮切口。
- 新增 `LoginHeroPanel.vue`，承载登录页左侧品牌标题、平台介绍、特性列表和相关响应式样式。
- `LoginPage.vue` 改为组合 `LoginHeroPanel`，页面层继续保留登录/注册模式切换、表单状态、接口调用、提示消息和路由跳转逻辑。
- 从 `LoginPage.vue` 移除左侧展示区模板、`features` 数据、`Shield` 图标依赖和对应品牌展示样式，避免页面层继续混合纯展示内容和认证流程。
- `LoginPage.vue` 行数由 608 行降至 407 行；新增 `LoginHeroPanel.vue` 215 行。本轮不拆登录表单/注册表单，不改变接口调用、表单字段、路由路径和视觉文案。

验证记录：
- `npm run build` 因当前 PowerShell 环境无法识别 `npm` 命令未执行成功；随后使用当前项目可用 Node 入口分别执行类型检查和构建。
- `& 'C:\Users\admin\AppData\Roaming\npm\node_modules\node\bin\node.exe' node_modules\vue-tsc\bin\vue-tsc.js -b` 通过。
- `& 'C:\Users\admin\AppData\Roaming\npm\node_modules\node\bin\node.exe' node_modules\vite\bin\vite.js build` 通过；仅有第三方 `@vueuse/core` PURE 注释位置提示，不影响构建产物生成。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- `OPT-ARCH-008` 已开始推进，`LoginPage.vue` 的纯展示品牌面板已从页面层迁出，页面职责向认证流程编排收敛。
- 下一步建议继续 `OPT-ARCH-008-B`：在不改变认证行为的前提下，继续拆分 `LoginForm` 或 `RegisterForm`，进一步让登录页只保留模式选择和组合编排；或者转向 `ReviewDetailPage.vue` 的确认弹窗/状态展示逻辑下沉，需按风险选择一轮一个切口。

### 2026-06-22 架构优化 OPT-ARCH-008-B 登录表单组件化

已完成：
- 按 `OPT-ARCH-008-A` 的下一步建议继续登录页小步瘦身，优先抽取登录表单而不触碰注册流程。
- 新增 `LoginForm.vue`，承载登录账号、密码、记住登录状态、忘记密码入口、登录按钮和切换注册按钮，以及登录表单相关 scoped 样式。
- `LoginPage.vue` 改为组合 `LoginForm`，页面层继续负责 `loginForm` 状态、登录校验、登录 API 调用、消息提示和路由跳转，认证行为保持不变。
- `LoginForm` 通过 `v-model` 接收登录表单模型，通过 `submit`、`forgot-password`、`switch-register` 事件回传用户动作，避免子组件直接依赖路由、消息组件或 API。
- `LoginPage.vue` 行数由 407 行降至 305 行；新增 `LoginForm.vue` 180 行。本轮不拆注册表单，不改变接口调用、表单字段、路由路径和视觉文案。

验证记录：
- `& 'C:\Users\admin\AppData\Roaming\npm\node_modules\node\bin\node.exe' node_modules\vue-tsc\bin\vue-tsc.js -b` 通过。
- `& 'C:\Users\admin\AppData\Roaming\npm\node_modules\node\bin\node.exe' node_modules\vite\bin\vite.js build` 通过；仅有第三方 `@vueuse/core` PURE 注释位置提示，不影响构建产物生成。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- `LoginPage.vue` 已完成品牌展示区和登录表单两轮组件化，页面职责进一步收敛为认证模式切换、表单状态、接口调用和组合编排。
- 下一步建议继续 `OPT-ARCH-008-C`：在不改变注册行为的前提下抽取 `RegisterForm`，让登录页两种认证表单都进入独立组件；完成后再评估是否进入 `ReviewDetailPage.vue` 的状态展示/确认流程下沉。

### 2026-06-22 架构优化 OPT-ARCH-008-C 注册表单组件化

已完成：
- 按 `OPT-ARCH-008-B` 的下一步建议继续登录页小步瘦身，抽取注册表单而不改变注册校验和注册 API 调用位置。
- 新增 `RegisterForm.vue`，承载用户名、邮箱、密码、确认密码、注册按钮和返回登录按钮，以及注册表单相关 scoped 样式。
- `LoginPage.vue` 改为组合 `RegisterForm`，页面层继续负责 `registerForm` 状态、注册校验、注册 API 调用、消息提示和路由跳转，认证行为保持不变。
- `RegisterForm` 通过 `v-model` 接收注册表单模型，通过 `submit`、`switch-login` 事件回传用户动作，避免子组件直接依赖路由、消息组件或 API。
- 清理 `LoginPage.vue` 中注册表单模板、图标依赖和剩余表单按钮/分割线样式，页面行数由 305 行降至 215 行；新增 `RegisterForm.vue` 165 行。本轮不改变接口调用、表单字段、路由路径和视觉文案。

验证记录：
- `& 'C:\Users\admin\AppData\Roaming\npm\node_modules\node\bin\node.exe' node_modules\vue-tsc\bin\vue-tsc.js -b` 通过。
- `& 'C:\Users\admin\AppData\Roaming\npm\node_modules\node\bin\node.exe' node_modules\vite\bin\vite.js build` 通过；仅有第三方 `@vueuse/core` PURE 注释位置提示，不影响构建产物生成。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- `LoginPage.vue` 已完成品牌展示区、登录表单和注册表单三轮组件化，页面职责收敛为认证模式切换、表单状态、接口调用和组合编排，行数由优化前 608 行降至 215 行。
- 下一步建议对 `OPT-ARCH-008` 做登录页阶段收口复核：评估是否继续将认证校验/API 调用下沉到 composable；若收益有限，则按主方案转向 `ReviewDetailPage.vue` 或 `NotificationOpsPage.vue` 的大页面瘦身。

### 2026-06-22 架构优化 OPT-ARCH-008 登录页阶段收口复核

已完成：
- 复核 `LoginPage.vue`、`LoginHeroPanel.vue`、`LoginForm.vue`、`RegisterForm.vue` 当前职责边界，确认登录页已经完成主方案要求的 `LoginForm`、`RegisterForm`、`AuthHeroPanel` 类拆分目标。
- `LoginPage.vue` 当前仅保留认证模式切换、登录/注册表单状态、提交防重、基础校验、认证 API 调用、消息提示、路由跳转和页面组合编排；纯展示面板、登录表单和注册表单均已迁入独立组件。
- 评估暂不继续将认证校验和 API 调用下沉到 composable：当前登录页剩余逻辑仍属于页面级认证流程编排，继续拆分会增加间接层和跨文件跳转成本，收益低于转向更复杂页面的瘦身收益。
- 本轮不新增代码变更，仅做阶段复核和进度沉淀；不改变接口调用、路由路径、表单字段、视觉文案和认证行为。

验证记录：
- `& 'C:\Users\admin\AppData\Roaming\npm\node_modules\node\bin\node.exe' node_modules\vue-tsc\bin\vue-tsc.js -b` 通过。
- `& 'C:\Users\admin\AppData\Roaming\npm\node_modules\node\bin\node.exe' node_modules\vite\bin\vite.js build` 通过；仅有第三方 `@vueuse/core` PURE 注释位置提示，不影响构建产物生成。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- 登录页阶段达到 `OPT-ARCH-008` 的局部收口标准：页面行数由 608 行降至 215 行，页面层职责已经从“大模板 + 表单 + 展示 + 认证流程”收敛为“认证流程编排 + 组件组合”。
- 下一步建议按主方案继续推进大页面瘦身，优先转向 `ReviewDetailPage.vue`：先选择确认弹窗流程下沉 composable 或状态文案/tag class/失败说明 display mapper 下沉中的一个小切口，继续遵守“一轮一个清晰边界”的优化规则。

### 2026-06-22 架构优化 OPT-ARCH-008-D ReviewDetail GitHub 评论回写确认流程下沉

已完成：
- 按 `OPT-ARCH-008` 大页面瘦身顺序，从登录页阶段收口后转向 `ReviewDetailPage.vue`，优先选择页面内 GitHub 评论回写确认流程作为单一小切口。
- 新增 `useReviewDetailGithubCommentPublishConfirm` composable，集中承载回写前置条件判断、确认弹窗文案、writeback check 警告提示、确认后发布调用和发布后预览/历史刷新编排。
- `ReviewDetailPage.vue` 移除 `ElMessageBox` 直接依赖和内联 `confirmPublishGithubComments` 实现，页面层改为通过 composable 获取 `confirmPublishGithubComments` 并继续传给 `ReviewDetailGithubCommentsCard`。
- `useReviewDetailGithubComments` 的加载、发布、成功/失败消息语义保持不变；本轮不改变 GitHub 评论预览、发布 API、发布后刷新范围、按钮禁用规则和 DTO 契约。
- `ReviewDetailPage.vue` 行数由 545 行降至 525 行，确认弹窗流程从页面层迁入领域 composable。

验证记录：
- `& 'C:\Users\admin\AppData\Roaming\npm\node_modules\node\bin\node.exe' node_modules\vue-tsc\bin\vue-tsc.js -b` 通过。
- `& 'C:\Users\admin\AppData\Roaming\npm\node_modules\node\bin\node.exe' node_modules\vite\bin\vite.js build` 通过；仅有第三方 `@vueuse/core` PURE 注释位置提示，不影响构建产物生成。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- `ReviewDetailPage.vue` 已开始从“页面内确认流程”向“页面组合编排 + 领域 composable”收敛，GitHub 评论回写确认弹窗流程具备独立边界。
- 下一步建议继续 `OPT-ARCH-008-E`：优先在 `ReviewDetailPage.vue` 内选择一个展示派生边界继续下沉，例如 `writebackCheckStatusClass`、LLM 状态文本/样式 computed 或 finding/file/timeline 派生集合；每轮仍保持一个清晰边界，避免一次性搬空详情页造成行为回归风险。

### 2026-06-22 架构优化 OPT-ARCH-008-E ReviewDetail finding/file/timeline 派生集合下沉

已完成：
- 按 `OPT-ARCH-008-D` 的下一步建议继续瘦身 `ReviewDetailPage.vue`，选择 finding/file/timeline 派生集合作为本轮唯一边界。
- 新增 `useReviewDetailDerivedCollections` composable，集中承载 `reviewFindings`、`missingTests`、`changedFiles`、`reviewTimeline`、`riskProfile`、风险等级统计、文件 finding 计数、带 finding 计数的 changed files、本地化 timeline 和 `statusReason` 派生逻辑。
- `ReviewDetailPage.vue` 移除上述派生 computed、`findingCountByFile` 中间聚合、`ChangedFileWithFindingCount` 页面内类型，以及 `statusReasonText`、`timelineLabelText` 直接依赖。
- 页面层继续保留路由、加载、轮询、重试、人工复核、GitHub 评论发布和组件组合编排；本轮不改变 DTO、组件 props、展示文案和行为语义。
- `ReviewDetailPage.vue` 行数由 525 行降至 493 行，详情页派生集合逻辑迁入领域 composable。

验证记录：
- `& 'C:\Users\admin\AppData\Roaming\npm\node_modules\node\bin\node.exe' node_modules\vue-tsc\bin\vue-tsc.js -b` 通过。
- `& 'C:\Users\admin\AppData\Roaming\npm\node_modules\node\bin\node.exe' node_modules\vite\bin\vite.js build` 通过；仅有第三方 `@vueuse/core` PURE 注释位置提示，不影响构建产物生成。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- `ReviewDetailPage.vue` 的展示派生集合已形成独立 composable，页面层进一步收敛为详情加载、操作流程和组件编排。
- 下一步建议继续 `OPT-ARCH-008-F`：优先选择剩余的轻量展示派生边界下沉，例如 LLM 展示文本/状态 class 或 `writebackCheckStatusClass`，继续保持一轮一个边界；若详情页继续下降收益变低，再转向 `NotificationOpsPage.vue` 的 settings/events/deliveries 区域拆分。

### 2026-06-22 架构优化 OPT-ARCH-008-F ReviewDetail LLM 展示派生下沉

已完成：
- 按 `OPT-ARCH-008-E` 的下一步建议继续瘦身 `ReviewDetailPage.vue`，选择 LLM 展示文本和解析状态 class 派生作为本轮唯一边界。
- 新增 `useReviewDetailLlmDisplay` composable，集中承载 LLM provider/model 展示、耗时展示、token 使用量展示、成本展示、解析状态文案和解析状态样式 class 派生。
- `ReviewDetailPage.vue` 移除 `llmModelText`、`llmDurationText`、`llmTokenUsageText`、`llmCostText`、`llmParseStatusText`、`llmParseStatusClass` 六个内联 computed，以及对 LLM display mapper 的直接依赖。
- 页面层继续把派生结果传递给 `ReviewDetailSidePanel`，不改变侧栏 props、展示文案、LLM DTO 契约和任何加载/轮询/发布行为。
- `ReviewDetailPage.vue` 行数由 493 行降至 474 行，LLM 展示派生逻辑迁入领域 composable。

验证记录：
- `& 'C:\Users\admin\AppData\Roaming\npm\node_modules\node\bin\node.exe' node_modules\vue-tsc\bin\vue-tsc.js -b` 通过。
- `& 'C:\Users\admin\AppData\Roaming\npm\node_modules\node\bin\node.exe' node_modules\vite\bin\vite.js build` 通过；仅有第三方 `@vueuse/core` PURE 注释位置提示，不影响构建产物生成。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- `ReviewDetailPage.vue` 中 LLM 展示派生已形成独立 composable，详情页页面层继续向“加载/操作/组件编排”收敛。
- 下一步建议继续 `OPT-ARCH-008-G`：复核详情页剩余展示派生是否还有高收益小切口，例如 `writebackCheckStatusClass` 和人工复核状态派生；若剩余收益有限，则对 `ReviewDetailPage.vue` 做阶段收口复核，并按主方案转向 `NotificationOpsPage.vue` 的 settings/events/deliveries 区域拆分。

### 2026-06-22 架构优化 OPT-ARCH-008-G ReviewDetail 人工复核展示与回写门禁派生下沉

已完成：
- 按 `OPT-ARCH-008-F` 的下一步建议复核 `ReviewDetailPage.vue` 剩余 computed，选择人工复核状态展示和 GitHub 评论回写门禁派生作为本轮唯一边界。
- 新增 `useReviewDetailHumanReviewDisplay` composable，集中承载 `humanReviewStatus`、`isHumanReviewPublishAllowed`、`canSubmitHumanReview`、`humanReviewPublishBlockReason`、`humanReviewStatusText` 和 `humanReviewStatusClass` 派生。
- `ReviewDetailPage.vue` 移除人工复核相关内联 computed，以及对 `humanReviewPublishBlockReasonText`、`humanReviewStatusText`、`humanReviewStatusClass` mapper 的直接依赖。
- 页面层继续将派生结果传给 `ReviewDetailHumanReviewCard`、`ReviewDetailGithubCommentsCard`、`useReviewDetailHumanReview` 和 GitHub 评论发布确认 composable；本轮不改变人工复核动作、GitHub 评论按钮禁用条件、阻断文案和 API 契约。
- `ReviewDetailPage.vue` 行数由 474 行降至 453 行，人工复核展示与回写门禁派生迁入领域 composable。

验证记录：
- `& 'C:\Users\admin\AppData\Roaming\npm\node_modules\node\bin\node.exe' node_modules\vue-tsc\bin\vue-tsc.js -b` 通过。
- `& 'C:\Users\admin\AppData\Roaming\npm\node_modules\node\bin\node.exe' node_modules\vite\bin\vite.js build` 通过；仅有第三方 `@vueuse/core` PURE 注释位置提示，不影响构建产物生成。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- `ReviewDetailPage.vue` 中人工复核状态展示和 GitHub 回写门禁派生已形成独立 composable，详情页页面层继续收敛为路由、加载、轮询、操作流程和组件编排。
- 下一步建议进行 `OPT-ARCH-008 ReviewDetail 阶段收口复核`：评估剩余 `refreshStatusText`、`retryTooltip`、`writebackCheckStatusClass`、轮询间隔等 computed 是否仍值得继续拆分；若收益有限，则按主方案转向 `NotificationOpsPage.vue` 的 settings/events/deliveries 区域拆分。

### 2026-06-22 架构优化 OPT-ARCH-008 ReviewDetail 阶段收口复核

已完成：
- 复核 `ReviewDetailPage.vue` 当前剩余职责，确认 `OPT-ARCH-008-D` 至 `OPT-ARCH-008-G` 已完成 GitHub 评论确认流程、finding/file/timeline 派生集合、LLM 展示派生、人工复核展示与回写门禁派生下沉。
- `ReviewDetailPage.vue` 当前仍保留路由返回与外链打开、详情加载编排、轮询间隔与刷新状态、重试 tooltip、GitHub writeback check 小标签 class、各领域 composable 组合和详情组件 props 连接。
- 评估暂不继续拆分剩余 `refreshStatusText`、`retryTooltip`、`writebackCheckStatusClass` 和轮询间隔 computed：这些逻辑要么与页面级轮询/操作编排强相关，要么体量很小，继续抽取会增加间接层，收益低于转向下一个大页面。
- 本轮不新增代码变更，仅做阶段复核和进度沉淀；不改变路由、轮询、重试、人工复核、GitHub 评论回写、组件 props 和展示语义。

验证记录：
- `& 'C:\Users\admin\AppData\Roaming\npm\node_modules\node\bin\node.exe' node_modules\vue-tsc\bin\vue-tsc.js -b` 通过。
- `& 'C:\Users\admin\AppData\Roaming\npm\node_modules\node\bin\node.exe' node_modules\vite\bin\vite.js build` 通过；仅有第三方 `@vueuse/core` PURE 注释位置提示，不影响构建产物生成。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- `ReviewDetailPage.vue` 阶段达到 `OPT-ARCH-008` 的局部收口标准：页面行数由 545 行降至 453 行，确认弹窗流程和主要展示派生逻辑已迁入领域 composable，页面层职责收敛为路由、加载、轮询、操作流程和组件编排。
- 下一步建议按主方案转向 `NotificationOpsPage.vue`：优先从 settings、events、deliveries 三个区域中选择一个低风险区域组件化或 composable 化，继续遵守“一轮一个清晰边界”的优化规则。

### 2026-06-22 架构优化 OPT-ARCH-008-H NotificationOps 通知事件区域组件化

已完成：
- 按 `ReviewDetailPage.vue` 阶段收口结论转向 `NotificationOpsPage.vue`，优先选择模板最重且边界清晰的通知事件 events 标签页作为本轮切口。
- 新增 `NotificationEventsPanel.vue`，集中承载通知事件筛选条、事件表格、状态/渠道/投递展示、失败投递标记和单条事件重试按钮。
- `NotificationOpsPage.vue` 的 `events` 标签页改为组合 `NotificationEventsPanel`，页面层继续持有 `eventFilter`、`notificationEvents`、`eventsLoading`、`retryingEventId`、`loadNotificationEvents` 和 `retryEvent`，不改变数据来源、筛选条件、重试行为和 API 调用位置。
- `NotificationEventsPanel` 复用现有 `notificationOpsDisplayMappers` 中的 `eventTypeText`、`channelIcon`、`channelText`、`notificationStatusClass`、`notificationStatusText` 和 `canRetryNotificationEvent`，保持展示文案和状态样式不变。
- `NotificationOpsPage.vue` 行数由 530 行降至 475 行；新增 `NotificationEventsPanel.vue` 105 行。本轮不触碰 settings、bindings、deliveries 区域，不改变路由、标签页名称、接口契约和筛选语义。

验证记录：
- `& 'C:\Users\admin\AppData\Roaming\npm\node_modules\node\bin\node.exe' node_modules\vue-tsc\bin\vue-tsc.js -b` 通过。
- `& 'C:\Users\admin\AppData\Roaming\npm\node_modules\node\bin\node.exe' node_modules\vite\bin\vite.js build` 通过；仅有第三方 `@vueuse/core` PURE 注释位置提示，不影响构建产物生成。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- `NotificationOpsPage.vue` 已开始按主方案拆分 settings、events、deliveries 区域，通知事件标签页已形成独立领域组件，页面层职责向数据编排和区域组合收敛。
- 下一步建议继续 `OPT-ARCH-008-I`：优先抽取投递记录 deliveries 区域为 `NotificationDeliveriesPanel`，因为其表格和筛选结构与 events 相似，适合保持同一拆分节奏；settings 区域已有 `NotificationSettingsPanel`，可稍后做阶段复核。

### 2026-06-22 架构优化 OPT-ARCH-008-I NotificationOps 投递记录区域组件化

已完成：
- 按 `OPT-ARCH-008-H` 的下一步建议继续拆分 `NotificationOpsPage.vue`，选择与 events 区域结构相近且边界清晰的 deliveries 标签页作为本轮切口。
- 新增 `NotificationDeliveriesPanel.vue`，集中承载投递记录筛选条、投递记录表格、平台展示、投递状态展示、失败原因和发送时间列。
- `NotificationOpsPage.vue` 的 `deliveries` 标签页改为组合 `NotificationDeliveriesPanel`，页面层继续持有 `deliveryFilter`、`notificationDeliveries`、`deliveriesLoading` 和 `loadNotificationDeliveries`，不改变数据来源、筛选条件和 API 调用位置。
- `NotificationDeliveriesPanel` 复用现有 `notificationOpsDisplayMappers` 中的 `providerText`、`notificationStatusClass` 和 `notificationStatusText`，保持展示文案和状态样式不变。
- `NotificationOpsPage.vue` 行数由 475 行降至 451 行；新增 `NotificationDeliveriesPanel.vue` 60 行。本轮不触碰 settings、bindings、events 区域，不改变标签页名称、接口契约和筛选语义。

验证记录：
- `& 'C:\Users\admin\AppData\Roaming\npm\node_modules\node\bin\node.exe' node_modules\vue-tsc\bin\vue-tsc.js -b` 通过。
- `& 'C:\Users\admin\AppData\Roaming\npm\node_modules\node\bin\node.exe' node_modules\vite\bin\vite.js build` 通过；仅有第三方 `@vueuse/core` PURE 注释位置提示，不影响构建产物生成。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- `NotificationOpsPage.vue` 的 events 和 deliveries 两个表格区域均已组件化，页面层进一步收敛为标签页组合、数据加载编排和弹窗/绑定操作入口。
- 下一步建议继续 `OPT-ARCH-008 NotificationOps 阶段收口复核`：复核 settings 区域已有 `NotificationSettingsPanel`、bindings 区域已有 `NotificationBindingTable` 和 `NotificationBindingDialog` 后，判断是否仍需继续拆分最近通知事件摘要表或测试发送弹窗；若收益有限，则对 `OPT-ARCH-008` 前端大页面瘦身做阶段收口。

### 2026-06-22 架构优化 OPT-ARCH-008 NotificationOps 与前端大页面瘦身阶段收口复核

已完成：
- 复核 `NotificationOpsPage.vue` 当前剩余职责，确认 settings 区域已有 `NotificationSettingsPanel`，bindings 区域已有 `NotificationBindingTable` 与 `NotificationBindingDialog`，events 区域已抽为 `NotificationEventsPanel`，deliveries 区域已抽为 `NotificationDeliveriesPanel`。
- `NotificationOpsPage.vue` 当前仍保留页面标题与操作入口、settings 首页指标卡片、最近通知事件摘要表、测试发送弹窗模板、页面级 loading、标签页状态和各 composable 的组合编排。
- 评估暂不继续拆分最近通知事件摘要表和测试发送弹窗：前者属于 settings 首页概览且与指标卡片共享当前页面上下文，后者状态已由 `useNotificationOpsTestDialog` 承载，模板体量较小；继续拆分会增加间接层，收益低于阶段收口。
- 本轮不新增代码变更，仅做阶段复核和进度沉淀；不改变 settings、bindings、events、deliveries 标签页行为、接口调用、筛选语义、弹窗行为和展示文案。
- 复核 `OPT-ARCH-008` 主方案目标：`LoginPage.vue` 已完成品牌区/登录表单/注册表单组件化并收口；`ReviewDetailPage.vue` 已完成确认流程与主要展示派生下沉并收口；`NotificationOpsPage.vue` 已完成 events/deliveries 区域组件化，且 settings/bindings 既有组件边界可复用。

验证记录：
- `& 'C:\Users\admin\AppData\Roaming\npm\node_modules\node\bin\node.exe' node_modules\vue-tsc\bin\vue-tsc.js -b` 通过。
- `& 'C:\Users\admin\AppData\Roaming\npm\node_modules\node\bin\node.exe' node_modules\vite\bin\vite.js build` 通过；仅有第三方 `@vueuse/core` PURE 注释位置提示，不影响构建产物生成。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- `OPT-ARCH-008` 前端大页面瘦身达到阶段目标：登录页、详情页、通知运维页均已完成主要高收益拆分，页面层职责整体向路由、加载、操作流程和组件组合收敛。
- 下一步建议从 `OPT-ARCH-008` 收口，回到主方案后续优先级做新一轮架构复核：优先检查是否进入前端类型按领域维护、状态/文案 mapper 继续收敛，或转回后端中优先级项如 `ReviewTaskQueryServiceImpl`、通知投递链路、Dashboard SQL 可验证性等；新一轮开始前应先重新读取主方案和最新进度，避免越过优化顺序。

### 2026-06-22 架构优化 OPT-ARCH-009-A ReviewTaskQuery 查询项上下文边界抽取

已完成：
- 按 `OPT-ARCH-008` 收口后的下一步建议，重新读取主方案和最新进度后进入新一轮架构复核，优先选择后端中优先级项 `ReviewTaskQueryServiceImpl`。
- 复核 `ReviewTaskQueryServiceImpl` 当前职责，确认服务本身已较轻，但 `listReviews`、`getReviewDetail`、`getReviewStatus` 三个只读路径仍重复维护任务必取、timeline labels、failure summary 和 list item 上下文组装。
- 新增 `ReviewTaskQueryItemLoader`，集中承载查询任务必取、timeline 加载、最新 timeline item 派生、基于 timeline 的失败摘要解析和 `ReviewTaskListItem` 组装。
- `ReviewTaskQueryServiceImpl` 改为委托 `ReviewTaskQueryItemLoader` 处理上述查询项上下文，使页面列表、详情和轻量状态接口只保留分页查询、详情数据加载、状态响应组装等用例编排。
- 新增 `ReviewTaskQueryItemLoaderTest`，独立覆盖任务不存在异常、失败 timeline 分类摘要组装、状态快照最新 timeline item 派生。
- 本轮仅调整只读查询组装边界，不改变 Controller/API 响应结构、分页查询条件、详情 DTO、轻量状态接口和任何写路径行为。

验证记录：
- `mvn "-Dtest=ReviewServiceImplTest,ReviewTaskListItemAssemblerTest,ReviewTaskStatusAssemblerTest,ReviewTaskDetailDataLoaderTest,SpringBeanConstructorSelectionTest" test` 通过；5 个相关测试类共 42 个用例全部成功。
- `mvn "-Dtest=ReviewServiceImplTest,ReviewTaskQueryItemLoaderTest,ReviewTaskListItemAssemblerTest,ReviewTaskStatusAssemblerTest,ReviewTaskDetailDataLoaderTest,SpringBeanConstructorSelectionTest" test` 通过；6 个相关测试类共 45 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- `ReviewTaskQueryServiceImpl` 已进一步收敛为只读查询用例编排层，任务项上下文和失败摘要派生有了独立可测试边界。
- 下一步建议继续 `OPT-ARCH-009-B`：复核 `ReviewTaskDetailDataLoader` 中 finding/missing test 分类、反馈状态默认值和 DTO 映射是否值得继续拆出 assembler；若收益有限，则转向通知投递链路或 Dashboard SQL 可验证性复核。

### 2026-06-22 架构优化 OPT-ARCH-009-B ReviewTaskDetail finding 映射边界抽取

已完成：
- 按 `OPT-ARCH-009-A` 的下一步建议继续复核 `ReviewTaskDetailDataLoader`，确认其同时承担详情查库、changed file DTO 映射、finding/missing test 分类、反馈状态默认值和 finding DTO 组装。
- 新增 `ReviewTaskDetailFindingAssembler`，集中承载 `ChangedFile -> ChangedFileDto`、`ReviewFinding -> ReviewFindingDto`、`ReviewFinding -> MissingTestDto` 映射，以及 `FINDING`/`MISSING_TEST` 分类、反馈状态默认值、文本空值兜底、时间格式化和大小写归一化。
- `ReviewTaskDetailDataLoader` 改为只负责 changed files、review findings、timeline 的查询加载，并委托 `ReviewTaskDetailFindingAssembler` 完成详情 DTO 映射，使查库和展示 DTO 组装边界分离。
- 新增 `ReviewTaskDetailFindingAssemblerTest`，独立覆盖 changed file 映射、finding/missing test 分类、反馈状态默认值、显式反馈状态归一化、空文本字段兜底和阻断标记兜底。
- 本轮仅调整详情只读映射边界，不改变详情接口响应结构、字段名称、排序规则、finding/missing test 分类语义和任何写路径行为。

验证记录：
- `mvn "-Dtest=ReviewServiceImplTest,ReviewTaskQueryItemLoaderTest,ReviewTaskDetailDataLoaderTest,ReviewTaskDetailFindingAssemblerTest,ReviewTaskListItemAssemblerTest,ReviewTaskStatusAssemblerTest,SpringBeanConstructorSelectionTest" test` 通过；7 个相关测试类共 47 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- `ReviewTaskDetailDataLoader` 已收敛为详情查询数据加载器，finding/missing test 分类和 DTO 展示映射具备独立可测试边界。
- `OPT-ARCH-009` 的 `ReviewTaskQueryServiceImpl` 查询编排和详情映射两个高收益小切口已完成；下一步建议先做 `OPT-ARCH-009` 阶段复核，判断是否继续拆 `ReviewTaskDetailDataLoader`，若剩余收益有限则按主方案转向通知投递链路或 Dashboard SQL 可验证性。

### 2026-06-22 架构优化 OPT-ARCH-009 查询链路阶段收口复核

已完成：
- 复核 `ReviewTaskQueryServiceImpl`、`ReviewTaskQueryItemLoader`、`ReviewTaskDetailDataLoader`、`ReviewTaskDetailFindingAssembler` 和 `ReviewTaskDetailAssembler` 当前职责边界。
- 确认 `OPT-ARCH-009-A` 已将任务必取、timeline 加载、失败摘要和 list item 上下文从 `ReviewTaskQueryServiceImpl` 迁入 `ReviewTaskQueryItemLoader`。
- 确认 `OPT-ARCH-009-B` 已将 changed file/finding/missing test 展示 DTO 映射从 `ReviewTaskDetailDataLoader` 迁入 `ReviewTaskDetailFindingAssembler`。
- 当前 `ReviewTaskQueryServiceImpl` 仅保留分页查询、详情查询和轻量状态查询三个用例编排；`ReviewTaskDetailDataLoader` 仅保留详情子表和 timeline 的查询加载；`ReviewTaskDetailAssembler` 继续负责完整详情响应聚合、风险画像、摘要、LLM/RabbitMQ 子 DTO 组装。
- 评估暂不继续拆分查询链路剩余职责：继续拆 `ReviewTaskDetailAssembler` 会触碰风险画像、PR 摘要和 LLM chunked review 聚合语义，收益低于转向下一条主线；继续拆 `ReviewTaskQueryServiceImpl` 或 `ReviewTaskDetailDataLoader` 会增加间接层而非降低复杂度。
- 本轮不新增代码变更，仅做阶段复核和进度沉淀；不改变查询接口、详情 DTO、状态接口、排序规则和任何写路径行为。

验证记录：
- `mvn "-Dtest=ReviewServiceImplTest,ReviewTaskQueryItemLoaderTest,ReviewTaskDetailDataLoaderTest,ReviewTaskDetailFindingAssemblerTest,ReviewTaskListItemAssemblerTest,ReviewTaskStatusAssemblerTest,SpringBeanConstructorSelectionTest" test` 通过；7 个相关测试类共 47 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- `OPT-ARCH-009` 查询链路阶段达到收口标准：查询服务、查询项上下文加载、详情子表加载和详情 finding 映射均已有清晰边界，且相关回归测试通过。
- 下一步建议按主方案中优先级转向通知投递链路复核：优先检查 `NotificationDispatchServiceImpl`、事件发布/投递补偿、失败策略、状态更新器和 delivery log 工厂是否还有重复状态派生、DTO 组装或落库边界可收敛。

### 2026-06-22 架构优化 OPT-ARCH-010-A 通知派发请求组装边界抽取

已完成：
- 按 `OPT-ARCH-009` 收口后的下一步建议转向通知投递链路复核，优先检查 `NotificationDispatchServiceImpl`、投递 worker、失败策略、状态更新器和 delivery log 工厂。
- 确认投递执行链路已经拆分为 deliverable query、payload parser、binding batch delivery、completion service、failure policy、state updater 和 log factory，当前不适合继续从 worker 主流程硬拆。
- 选择 `NotificationDispatchServiceImpl` 中仍直接维护的事件类型选择和 GitHub 评论回写计数归一化作为本轮小切口。
- 新增 `NotificationDispatchRequest` 和 `NotificationDispatchRequestFactory`，集中承载 review finished/review failed/GitHub comments published 三类通知派发请求的 event type、batch id、finding count、评论成功/失败/跳过计数归一化。
- `NotificationDispatchServiceImpl` 改为委托 `NotificationDispatchRequestFactory` 构建派发请求，服务层保留 outbox event 创建、payload 构建和事务后发布编排。
- 新增 `NotificationDispatchRequestFactoryTest`，独立覆盖人工复核事件类型、普通完成事件类型、审查失败事件类型和 GitHub 评论计数 null 归零。
- 本轮不改变通知事件表结构、event key、payload JSON、RabbitMQ 发布、补偿、投递 worker、状态机和前端 DTO 契约。

验证记录：
- 首次执行 `mvn "-Dtest=NotificationDispatchServiceImplTest,NotificationDispatchRequestFactoryTest,NotificationDeliveryWorkerTest,NotificationDeliveryCompletionServiceTest,NotificationDeliveryFailurePolicyTest,NotificationDeliveryEventStateUpdaterTest,NotificationBindingBatchDeliveryServiceTest,NotificationBindingDeliveryServiceTest,NotificationDeliveryLogFactoryTest,NotificationDeliveryResultSummaryTest,NotificationStatusTest,SpringBeanConstructorSelectionTest" test` 时因新增测试少传 `GithubCommentPublishResponse#batchId` 参数导致 testCompile 失败，已修正测试构造器参数。
- 修正后再次执行上述命令通过；12 个相关测试类共 33 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- 通知派发入口已从“服务层直接拼 event type 和计数”收敛为“派发请求工厂 + 服务层编排”，后续新增通知事件时可先扩展请求工厂并独立测试派发请求语义。
- 下一步建议继续 `OPT-ARCH-010-B`：复核通知投递完成链路，优先判断 `NotificationDeliveryCompletionService` 是否需要承接更明确的“投递结果 -> 事件终态决策”边界；若当前 failure policy 和 updater 已足够清晰，则做通知投递链路阶段收口复核。

### 2026-06-22 架构优化 OPT-ARCH-010-B 通知投递完成决策边界抽取

已完成：
- 按 `OPT-ARCH-010-A` 的下一步建议复核通知投递完成链路，重点检查 `NotificationDeliveryCompletionService`、`NotificationDeliveryFailurePolicy`、`NotificationDeliveryEventStateUpdater`、`NotificationDeliveryResultSummary` 和 `NotificationDeliveryWorker`。
- 确认 worker 主流程已保持“加载可投递事件 -> 解析 payload -> 标记 delivering -> 批量投递 -> 完成处理”的清晰编排，不适合继续硬拆。
- 识别 `NotificationDeliveryCompletionService` 中仍直接维护“投递结果 summary -> 事件终态动作”的判断，即无失败标记 delivered，有失败则调用 failure policy 生成失败/死亡决策。
- 新增 `NotificationDeliveryCompletionDecision` 和 `NotificationDeliveryCompletionDecider`，集中承载投递完成后的 delivered/failed 终态决策，以及失败场景下对 `NotificationDeliveryFailurePolicy` 的委托。
- `NotificationDeliveryCompletionService` 改为只执行 completion decision：delivered 时标记 delivered，failed 时按 failure decision 标记 failed/dead。
- 新增 `NotificationDeliveryCompletionDeciderTest`，独立覆盖无失败投递结果不调用失败策略、有失败投递结果调用失败策略并返回失败决策。
- 调整 `NotificationDeliveryCompletionServiceTest`，改为 mock completion decider，验证 service 只负责执行决策而不再直接判断 result summary。
- 本轮不改变投递 worker、失败退避策略、事件状态值、delivery log、RabbitMQ ack/reject 语义和前端 DTO 契约。

验证记录：
- 首次执行 `mvn "-Dtest=NotificationDeliveryCompletionServiceTest,NotificationDeliveryCompletionDeciderTest,NotificationDeliveryWorkerTest,NotificationDeliveryFailurePolicyTest,NotificationDeliveryEventStateUpdaterTest,NotificationBindingBatchDeliveryServiceTest,NotificationBindingDeliveryServiceTest,NotificationDeliveryLogFactoryTest,NotificationDeliveryResultSummaryTest,NotificationDispatchServiceImplTest,NotificationDispatchRequestFactoryTest,NotificationStatusTest,SpringBeanConstructorSelectionTest" test` 时因 `NotificationDeliveryCompletionDecision` record 组件名 `delivered` 与静态工厂方法 `delivered()` 冲突导致编译失败，已将静态工厂方法改为 `markDelivered()`。
- 修正后再次执行上述命令通过；13 个相关测试类共 35 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- 通知投递完成链路已形成“completion decider 决定终态 + completion service 执行状态更新”的边界，失败策略和状态更新器职责保持独立。
- 下一步建议做 `OPT-ARCH-010` 通知投递链路阶段收口复核：复核 dispatch request factory、delivery worker、completion decider、failure policy、state updater、log factory 和补偿查询是否还存在高收益拆分点；若收益有限，则转向 Dashboard SQL 可验证性复核。

### 2026-06-22 架构优化 OPT-ARCH-010 通知投递链路阶段收口复核

已完成：
- 复核通知派发、发布补偿、投递 worker、投递完成、失败策略、状态更新、delivery log 和 outbox event store 当前职责边界。
- 确认 `OPT-ARCH-010-A` 已将通知事件类型选择和 GitHub 评论计数归一化从 `NotificationDispatchServiceImpl` 迁入 `NotificationDispatchRequestFactory`。
- 确认 `OPT-ARCH-010-B` 已将“投递结果 summary -> 事件终态决策”从 `NotificationDeliveryCompletionService` 迁入 `NotificationDeliveryCompletionDecider`。
- 当前通知链路边界清晰：`NotificationDispatchServiceImpl` 负责派发入口编排，`NotificationEventPublishCoordinator` 负责事务后发布和发布失败处理，`NotificationEventPublishCompensator` 负责补偿调度，`NotificationDeliveryWorker` 负责投递流程编排，`NotificationBindingBatchDeliveryService` 负责候选绑定批量投递，`NotificationDeliveryCompletionDecider` 负责投递终态决策，failure policy/state updater/log factory 分别负责退避决策、状态落库和投递日志创建。
- 评估暂不继续拆分通知链路：补偿查询、发布状态更新器、投递状态更新器、失败策略和 log factory 均已是小职责类；继续拆分会增加间接层，不如转向 Dashboard SQL 可验证性。
- 本轮不新增代码变更，仅做阶段复核和进度沉淀；不改变通知事件表结构、RabbitMQ 发布/消费、补偿条件、投递状态、delivery log、接口 DTO 和前端展示语义。

验证记录：
- `mvn "-Dtest=NotificationDispatchServiceImplTest,NotificationDispatchRequestFactoryTest,NotificationEventPublishCoordinatorTest,NotificationEventPublishCompensatorTest,NotificationPublishCompensationQueryTest,NotificationPublishEventStateUpdaterTest,NotificationPublishFailurePolicyTest,NotificationDeliveryCompletionServiceTest,NotificationDeliveryCompletionDeciderTest,NotificationDeliveryWorkerTest,NotificationDeliveryFailurePolicyTest,NotificationDeliveryEventStateUpdaterTest,NotificationBindingBatchDeliveryServiceTest,NotificationBindingDeliveryServiceTest,NotificationDeliveryLogFactoryTest,NotificationDeliveryResultSummaryTest,NotificationStatusTest,NotificationOutboxEventStoreTest,SpringBeanConstructorSelectionTest" test` 通过；19 个相关测试类共 52 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- `OPT-ARCH-010` 通知投递链路达到阶段收口标准：派发、发布、补偿、投递、完成决策、失败策略、状态更新和投递日志均已有明确边界，并具备相关回归测试。
- 下一步建议按主方案转向 Dashboard SQL 可验证性复核：优先检查 `DashboardMapper`、`DashboardServiceImpl`、趋势窗口、LLM 质量趋势 builder 和现有索引/migration，明确时间窗口、查询假设和可验证 SQL 边界。

### 2026-06-22 架构优化 OPT-ARCH-011-A Dashboard SQL 验证计划边界沉淀

已完成：
- 按 `OPT-ARCH-010` 收口后的下一步建议转向 Dashboard SQL 可验证性复核，检查 `DashboardMapper`、`DashboardServiceImpl`、`DashboardReviewTrendWindow`、`DashboardLlmQualityTrendBuilder` 和 Flyway migration 中的索引。
- 确认 Dashboard 当前使用注解 SQL，没有独立 XML mapper；聚合查询已通过 `DashboardMapperSqlContractTest` 覆盖查询形状、时间窗口过滤、group/order/limit 和关键 migration 索引名。
- 识别当前不足：SQL 合同测试中已有“查询形状 + 索引名”校验，但缺少一个稳定边界集中描述每个 Dashboard 聚合查询的验证范围和索引假设，容易让代码、测试和文档语义分散。
- 新增 `DashboardSqlVerificationPlan`，集中列出 Dashboard 8 个聚合查询方法、验证范围说明和支持索引假设，包括 7 日审查窗口、7/30/90 日 LLM 趋势窗口、finding join、风险分布、高风险列表、LLM model/repository breakdown 等。
- 新增 `DashboardSqlVerificationPlanTest`，校验每个计划项引用的 `DashboardMapper` 方法真实存在并保留 `@Select` SQL、每个支持索引名真实存在于 Flyway migration、计划覆盖全部 Dashboard 聚合查询方法。
- 本轮不改变 Dashboard SQL、接口响应、缓存策略、时间窗口、前端 DTO 和数据库 migration，仅沉淀 SQL 可验证性边界并加测试防漂移。

验证记录：
- `mvn "-Dtest=DashboardServiceImplTest,DashboardMapperSqlContractTest,DashboardSqlVerificationPlanTest,DashboardLlmQualityTrendBuilderTest,DashboardLlmQualityFormatterTest,DashboardStatusMapperTest,DashboardRuleDisplayMapperTest,DashboardOverviewDisplayMapperTest,DashboardSystemHealthProbeTest,DashboardControllerTest,SpringBeanConstructorSelectionTest" test` 通过；11 个相关测试类共 42 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- Dashboard SQL 可验证性已有明确计划边界：查询方法、验证范围和索引假设不再只散落在 Mapper SQL 与测试断言里。
- 下一步建议继续 `OPT-ARCH-011-B`：在不改变接口语义的前提下，复核 Dashboard 查询是否存在“计划索引与 SQL 过滤列顺序不匹配”的问题；如需优化，优先补 migration 索引或调整可验证计划，而不是先改聚合语义。

### 2026-06-22 后续优化价值排序文档沉淀

已完成：
- 根据当前已完成的 `OPT-ARCH-001` 至 `OPT-ARCH-011-A` 阶段结果，复核后续仍值得投入的优化方向。
- 在主方案新增“7.7 当前阶段后续优化建议”，明确当前阶段优先转向生产准出、SQL 可验证性和契约稳定性，不继续在已收口模块上做低收益拆分。
- 明确后续优先级：`OPT-ARCH-011-B Dashboard SQL 索引匹配复核`、Dashboard `EXPLAIN` / SQL 验证说明、API 契约测试、生产准出检查自动化。
- 明确暂不建议继续投入方向：不继续硬拆 `ReviewTaskQueryServiceImpl`、`ReviewTaskDetailDataLoader`、通知投递链路和已收口前端大页面；不为了行数继续抽小类；不直接修改 Dashboard 聚合 SQL 语义。

验证记录：
- 本轮为文档沉淀，无代码变更和测试命令。
- 已同步更新主方案与进度索引中的 2026-06-22 记录数量。

阶段结论：
- 后续优化排序已写入主方案，下一轮如继续执行，应优先从 `OPT-ARCH-011-B Dashboard SQL 索引匹配复核` 开始。

### 2026-06-22 架构优化 OPT-ARCH-011-B Dashboard SQL 索引匹配复核

已完成：
- 按主方案后续优化顺序继续 `OPT-ARCH-011-B`，复核 `DashboardMapper` 的时间窗口过滤、聚合/分组列、排序列与 `DashboardSqlVerificationPlan` 中索引假设的匹配关系。
- 识别 LLM 质量趋势、model breakdown、repository breakdown 查询均以 `created_at >= #{startDate}` 作为主要窗口约束，而原有 `idx_review_task_llm_model_window`、`idx_review_task_llm_repository_window` 以 `llm_status` 作为左前缀；当前 SQL 中 `llm_status` 是非空/不等值过滤，不适合作为时间窗口查询的稳定等值前缀。
- 识别 Dashboard finding 相关查询从任务窗口或高风险任务进入 `review_finding` 后，还需要按 `task_id + category` 过滤并按 `rule_id` 聚合；原有 `idx_review_finding_task` 与 `idx_review_finding_category` 为单列索引，缺少组合边界。
- 新增 migration `V34__dashboard_sql_index_alignment.sql`，补充 `idx_review_task_dashboard_created_risk`、`idx_review_task_dashboard_created_llm_model`、`idx_review_task_dashboard_created_llm_repo` 和 `idx_review_finding_task_category_rule`，优先让 Dashboard 时间窗口与 finding join 具备匹配的左前缀索引。
- 更新 `DashboardSqlVerificationPlan`，将 Dashboard 聚合查询的支持索引切换到新索引，并新增 `IndexAlignment` 记录索引名、左前缀列和使用理由。
- 更新 `DashboardSqlVerificationPlanTest`，校验索引对齐项在 migration 中保留期望左前缀列，防止后续 migration 或计划说明漂移。
- 更新 `DashboardMapperSqlContractTest`，将 Dashboard migration 索引假设同步到新索引名。
- 本轮不改变 Dashboard 聚合 SQL、接口响应、缓存策略、时间窗口、前端 DTO 和既有旧索引，仅补充更匹配当前查询形态的索引与验证计划。

验证记录：
- `mvn "-Dtest=DashboardServiceImplTest,DashboardMapperSqlContractTest,DashboardSqlVerificationPlanTest,DashboardLlmQualityTrendBuilderTest,DashboardLlmQualityFormatterTest,DashboardStatusMapperTest,DashboardRuleDisplayMapperTest,DashboardOverviewDisplayMapperTest,DashboardSystemHealthProbeTest,DashboardControllerTest,SpringBeanConstructorSelectionTest" test` 通过；11 个相关测试类共 43 个用例全部成功。

阶段结论：
- `OPT-ARCH-011-B` 已完成 Dashboard SQL 索引匹配复核：当前计划索引与 SQL 时间窗口、LLM 过滤、risk group 和 finding join 的主要访问路径更一致，并有合同测试保护索引左前缀。
- 下一步建议继续 Dashboard `EXPLAIN` / SQL 验证说明：沉淀每个 Dashboard 聚合 SQL 在预发/压测中应观察的 `key`、`type`、`rows`、`Extra` 等指标，先形成可复用验证材料，再决定是否需要进一步调整查询或索引。

### 2026-06-22 架构优化 Dashboard EXPLAIN / SQL 验证说明沉淀

已完成：
- 按 `OPT-ARCH-011-B` 后的下一步建议，继续沉淀 Dashboard `EXPLAIN` / SQL 验证说明。
- 在 `DashboardSqlVerificationPlan` 中新增 `ExplainObservation`，为 8 个 Dashboard 聚合查询逐一记录预发/压测执行计划应观察的候选 `key`、可接受访问类型、`rows` 预期和 `Extra` 风险提示。
- 明确 `selectReviewTrendCounts`、LLM 趋势和 LLM breakdown 查询中 `date_format`、派生表 group/order 可能产生临时表或 filesort；这类现象不是单独阻断项，但必须建立在 `rows` 已被时间窗口有效裁剪的前提下。
- 明确 finding join 查询应优先观察 `review_finding` 是否使用 `task_id/category` 组合索引，避免全表扫描 finding。
- 新增测试覆盖 `ExplainObservation`：校验说明覆盖全部 Dashboard 聚合查询、候选 key 均存在于 migration、访问类型落在 `range/ref/eq_ref/const` 等可接受范围、`rows` 预期和 `Extra` 观察项不为空。
- 本轮不连接真实数据库、不改 Dashboard SQL 语义、不改接口响应，仅沉淀可复用执行计划观察说明和防漂移测试。

验证记录：
- `mvn "-Dtest=DashboardServiceImplTest,DashboardMapperSqlContractTest,DashboardSqlVerificationPlanTest,DashboardLlmQualityTrendBuilderTest,DashboardLlmQualityFormatterTest,DashboardStatusMapperTest,DashboardRuleDisplayMapperTest,DashboardOverviewDisplayMapperTest,DashboardSystemHealthProbeTest,DashboardControllerTest,SpringBeanConstructorSelectionTest" test` 通过；11 个相关测试类共 44 个用例全部成功。

阶段结论：
- Dashboard `EXPLAIN` 验证说明已进入代码级计划边界，后续预发或压测时可按 query method 对照观察 `key`、`type`、`rows`、`Extra`，避免仅凭“有索引名”判断 SQL 已经可生产化。
- 下一步建议进入 API 契约测试方向，优先覆盖审查任务列表/详情/状态、GitHub 评论预览/发布、通知运维和 Dashboard overview 等核心接口。

### 2026-06-22 架构优化 API 契约测试-A Dashboard overview 契约加固

已完成：
- 按主方案后续优化顺序进入 API 契约测试方向，先选择刚完成 SQL 可验证性复核的 Dashboard overview 作为低风险首个切口。
- 将 `DashboardControllerTest` 从轻量 smoke test 升级为接口契约测试，覆盖 `ApiResponse` envelope 的 `success`、`code`、`message`、`timestamp`。
- 固化 `/api/v1/dashboard/overview` 响应中的 `overviewMetrics`、`reviewTrend`、`riskDistribution`、`ruleHits`、`highRiskReviews`、`failedRules`、`systemHealth`、`llmQualityByModel`、`llmQualityByRepository` 和 `llmQualityTrend` 字段结构。
- 新增对 `llmTrendDays` 查询参数透传到 `DashboardService#getOverview` 的断言，避免后续 Controller 重构丢失前端筛选契约。
- 本轮不改变 Controller 路由、响应字段、服务接口和前端 DTO，仅补充契约防线。

验证记录：
- `mvn "-Dtest=DashboardControllerTest,SpringBeanConstructorSelectionTest" test` 通过；2 个相关测试用例全部成功。

阶段结论：
- Dashboard overview 已有首个明确 API 契约测试，可防止后续 Dashboard 架构优化误伤前端消费字段和 `llmTrendDays` 参数语义。
- 下一步建议继续 API 契约测试-B：优先覆盖审查任务列表/详情/轻量状态接口，因其是前端详情页和任务列表的核心调用面。

### 2026-06-22 架构优化 API 契约测试-B 审查任务列表详情状态契约加固

已完成：
- 按 API 契约测试顺序继续覆盖审查任务列表、详情和轻量状态接口。
- 增强 `ReviewControllerTest#listReviewsReturnsPagedItems`，固化分页 envelope、任务摘要字段、source/triggerSource、人审字段，以及 `page/pageSize/status/source/triggerSource` 查询参数传递。
- 增强 `ReviewControllerTest#getReviewDetailReturnsTaskDetail`，固化详情接口中的任务基础字段、失败分类、PR URL、finding、missing test、changed file、timeline、risk profile、PR summary、LLM、chunked review、RabbitMQ 和人审字段。
- 增强 `ReviewControllerTest#getReviewStatusReturnsLightweightSnapshot`，固化轻量状态接口中的最新 timeline 和人审状态字段，保护详情页轮询契约。
- 本轮不改变 Controller 路由、DTO 字段、服务接口和任何写路径，仅补充前端核心调用面的契约防线。

验证记录：
- 首次执行 `mvn "-Dtest=ReviewControllerTest,SpringBeanConstructorSelectionTest" test` 时因测试构造 `ReviewTaskStatusResponse` 少传 `failureSuggestion` 参数导致 testCompile 失败，已修正测试构造数据。
- 修正后再次执行上述命令通过；`ReviewControllerTest` 11 个用例和 `SpringBeanConstructorSelectionTest` 1 个用例全部成功。

阶段结论：
- 审查任务列表、详情和轻量状态接口已有更明确的 API 契约测试，可防止后续服务拆分、DTO 组装或状态派生调整误伤前端任务列表与详情页轮询。
- 下一步建议继续 API 契约测试-C：覆盖 GitHub 评论预览/发布/历史接口，因这些接口承载真实回写前的安全门禁和幂等展示。

### 2026-06-22 架构优化 API 契约测试-C GitHub 评论回写接口契约加固

已完成：
- 按 API 契约测试顺序继续覆盖 GitHub 评论预览、发布和历史接口。
- 增强 `ReviewControllerTest#getGithubCommentPreviewReturnsCommentDrafts`，固化预览接口 envelope、任务/PR 字段、回写前配置校验字段、finding 草稿字段、commentable、targetType、published 和 feedbackStatus。
- 增强 `ReviewControllerTest#publishGithubCommentsReturnsPublishSummary`，固化发布接口的汇总计数、逐条发布结果、GitHub comment URL、comment id 和发布时间字段。
- 增强 `ReviewControllerTest#getGithubCommentPublicationHistoryReturnsBatches`，固化发布历史接口的分页字段、状态筛选、批次汇总、逐条历史结果字段，并断言 `page/pageSize/status` 参数透传。
- 本轮不改变 GitHub 评论 Controller 路由、DTO 字段、发布逻辑、幂等逻辑和真实 GitHub 调用行为，仅补充回写链路契约防线。

验证记录：
- `mvn "-Dtest=ReviewControllerTest,SpringBeanConstructorSelectionTest" test` 通过；`ReviewControllerTest` 11 个用例和 `SpringBeanConstructorSelectionTest` 1 个用例全部成功。

阶段结论：
- GitHub 评论预览、发布和历史接口已有更完整的字段级契约测试，可防止后续评论写回链路重构误伤安全门禁、发布计数、历史分页和幂等展示。
- 下一步建议继续 API 契约测试-D：覆盖通知运维接口，优先保护通知事件、投递记录和重试操作的前端契约。

### 2026-06-22 架构优化 API 契约测试-D 通知运维接口契约加固

已完成：
- 按 API 契约测试顺序继续覆盖通知运维接口。
- 新增 `NotificationIntegrationControllerTest`，覆盖 `/api/v1/config/notification-bindings`、`/api/v1/notification-events`、`/api/v1/notification-events/{id}/retry` 和 `/api/v1/notification-deliveries`。
- 固化通知绑定列表接口的 envelope、分页、绑定基础字段、通知开关、状态、脱敏 secret、检查时间和更新时间，并断言 `page/pageSize/organization/repository/provider` 参数透传。
- 固化通知事件列表接口的事件 key/type、任务与批次 id、状态、重试信息、错误信息、投递摘要、创建和更新时间，并断言 `page/pageSize/status/taskId` 参数透传。
- 固化事件重试接口的事件 id、重试后状态和 retryCount。
- 固化投递记录列表接口的 event/binding/task id、provider、状态、尝试次数、失败原因、requestId、发送时间和创建时间，并断言分页与筛选参数透传。
- 本轮不改变通知运维 Controller 路由、DTO 字段、服务接口和写路径行为，仅补充前端运维页契约防线。

验证记录：
- `mvn "-Dtest=NotificationIntegrationControllerTest,SpringBeanConstructorSelectionTest" test` 通过；`NotificationIntegrationControllerTest` 4 个用例和 `SpringBeanConstructorSelectionTest` 1 个用例全部成功。

阶段结论：
- 通知运维 bindings/events/deliveries/retry 接口已有字段级契约测试，可防止后续通知链路重构误伤前端运维页筛选、列表展示和重试操作。
- 下一步建议做 API 契约测试阶段收口复核：确认 Dashboard、审查任务、GitHub 评论和通知运维核心接口均已有契约防线后，再进入生产准出检查自动化方向。

### 2026-06-22 架构优化 API 契约测试阶段收口复核

已完成：
- 复核 API 契约测试阶段目标，确认本阶段已覆盖主方案要求的核心接口面：Dashboard overview、审查任务列表/详情/轻量状态、GitHub 评论预览/发布/历史、通知运维 bindings/events/deliveries/retry。
- 确认 Dashboard overview 契约测试已覆盖 `ApiResponse` envelope、核心图表/列表 DTO、LLM 质量 DTO 和 `llmTrendDays` 参数透传。
- 确认审查任务契约测试已覆盖任务摘要、详情、finding、missing test、changed file、timeline、风险画像、PR 摘要、LLM、chunked review、RabbitMQ、人审字段和轻量状态轮询字段。
- 确认 GitHub 评论契约测试已覆盖回写前安全校验、评论草稿、发布汇总、逐条发布结果、历史批次、分页和状态筛选。
- 确认通知运维契约测试已覆盖通知绑定、事件列表、事件重试和投递记录，以及前端筛选参数透传。
- 本轮不新增生产代码变更，仅做阶段复核和进度沉淀；后续 API 契约测试可按新接口增量补充，不再阻塞进入生产准出检查自动化。

验证记录：
- `mvn "-Dtest=DashboardControllerTest,ReviewControllerTest,NotificationIntegrationControllerTest,SpringBeanConstructorSelectionTest" test` 通过；4 个测试类共 17 个用例全部成功。

阶段结论：
- API 契约测试阶段达到当前收口标准，已经能保护后续架构优化中最容易被误伤的前端核心调用面和 GitHub/通知运维外部集成展示契约。
- 下一步建议按主方案进入生产准出检查自动化：沉淀后端关键测试集合、前端类型检查和构建、`git diff --check`、migration 检查、密钥/token 泄漏检查等可重复执行门禁。
