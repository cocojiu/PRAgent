# 2026-06-12 优化进度记录

### 2026-06-12 P1-04 可观测性指标与结构化日志

已完成：
- 新增请求 traceId 过滤器，为请求链路生成并透传 `X-Trace-Id`。
- 全局异常日志补充 traceId、异常类型、脱敏消息和定位信息。
- 新增审查任务、RabbitMQ 消费、GitHub 评论回写、外部调用等业务指标。
- 审查执行、消息消费、异常处理等关键链路输出结构化日志字段，便于按 taskId、repository、operation、result 聚合排查。

验证记录：
- `mvn test` 后端全量测试通过。

### 2026-06-12 P0-04 RabbitMQ 手动确认与失败治理基础

已完成：
- RabbitMQ 消费改为手动 ack/nack，明确成功确认、失败拒绝和是否重入队语义。
- Worker 消费日志补充 deliveryTag、durationMs、result、exceptionType 等结构化字段。
- 消费失败时任务状态和时间线保留可读失败信息，避免失败消息静默丢失。

验证记录：
- `mvn test` 后端全量测试通过。

### 2026-06-12 P1-01 外部调用治理与错误分类

已完成：
- 建立 GitHub/LLM 外部调用错误分类，覆盖 token 缺失/失效、权限不足、目标不存在、限流、校验失败、服务端错误等场景。
- 增加轻量熔断、超时、重试与并发保护能力，避免外部服务连续异常时持续放大故障。
- GitHub 评论回写、审查失败、历史回写记录均复用统一失败摘要，前端展示中文可读原因和建议。

验证记录：
- `mvn test` 后端全量测试通过。

### 2026-06-12 P1-05 数据库索引、归档与历史分页治理

已完成：
- 增加数据保留清理能力，支持按保留天数清理审查任务关联数据。
- GitHub 评论回写历史支持分页与状态筛选，避免详情页一次性加载过多历史批次。
- 相关管理接口、服务和前端详情页完成分页参数接入。

验证记录：
- `mvn test` 后端全量测试通过。
- `vue-tsc -b` 前端类型检查通过。
- `vite build` 前端生产构建通过。

### 2026-06-12 P2-01 详情页轻量状态接口与轮询退避

已完成：
- 后端新增 `GET /api/v1/reviews/{id}/status` 轻量状态接口。
- 状态接口返回任务状态、风险等级、LLM 状态、耗时、更新时间、失败摘要和最新时间线节点。
- 前端详情页自动轮询改为调用轻量状态接口，任务完成或失败后再拉取完整详情。
- 保留轮询失败退避和连续失败暂停机制，减少高频完整详情请求。

验证记录：
- `mvn "-Dtest=ReviewServiceImplTest,ReviewControllerTest" test` 通过。
- `mvn test` 后端全量测试通过，184 个用例全部成功。
- `vue-tsc -b` 前端类型检查通过。
- `vite build` 前端生产构建通过。

### 2026-06-12 P2-02 前端错误处理和操作防重

已完成：
- 前端请求层新增 `RequestError`，统一携带 HTTP 状态码、业务错误码和时间戳。
- 新增 `getErrorMessage()`，统一处理 401、403、网络异常和普通业务错误提示。
- 登录、注册、仪表盘、任务列表、任务详情、集成配置、系统设置、规则配置、用户管理、MQ 健康页接入统一错误提示。
- 对高风险操作增加入口防重：
  - 登录/注册提交。
  - 手动创建审查任务。
  - 审查任务重试。
  - GitHub 评论回写。
  - 集成配置保存与连接测试。
  - 系统设置保存。
  - 规则保存与启停。
  - 用户角色/状态更新。
  - MQ 异常任务重新入队。

验证记录：
- `vue-tsc -b` 前端类型检查通过。
- `vite build` 前端生产构建通过。

记录约定：
- 自本条起，每完成一项优化，均在本文档追加对应完成内容、验证记录和遗留事项。

### 2026-06-12 P2-03 接口版本化与契约测试

已完成：
- 新增后端 `ApiContractTest`，集中约束所有 Controller 的基础契约。
- 约束所有 Controller 类级路径必须使用 `/api/v1/` 版本前缀。
- 约束所有接口处理方法必须返回统一 `ApiResponse` 信封，避免新增接口绕开前后端统一错误处理。
- 增加成功响应 JSON 信封测试，固定 `success/code/message/data/timestamp` 字段。
- 增加失败响应 JSON 信封测试，固定业务异常的 HTTP 状态码、错误码、消息、空 data 和 timestamp 字段。

验证记录：
- `mvn "-Dtest=ApiContractTest,ReviewControllerTest,SystemConfigControllerTest,AuthControllerTest,UserManagementControllerTest" test` 通过，37 个用例全部成功。

遗留事项：
- 后续可继续补充 OpenAPI/JSON Schema 快照或前端自动生成类型，进一步减少手写 DTO 与前端类型不一致风险。

### 2026-06-12 P2-04 构建产物与前端性能继续验证

已完成：
- 将前端全局 Element Plus 注册从根包导入调整为组件级 ESM 入口导入，降低 UI 库被整体打入业务入口的风险。
- 将页面内 `ElMessage`、`ElMessageBox` 服务导入调整为组件级 ESM 入口，保持构建拆包路径一致。
- 将 Vite `manualChunks` 调整为函数式拆包，分别拆出 `vue-vendor`、`element-plus`、`echarts-core`、`zrender` 和通用 `vendor`。
- 验证路由页面仍保持按页面懒加载，生产构建最大 JS chunk 收敛到 `echarts-core` 约 374 kB、`element-plus` 约 268 kB，未再出现超过 500 kB 的 chunk 警告或循环拆包警告。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

遗留事项：
- 当前仍保留完整 `element-plus/dist/index.css`，后续如需继续压缩 CSS 体积，可评估按组件样式导入或引入自动导入插件。
- 构建阶段仍会输出 `@vueuse/core` 的 PURE 注释位置提示，Rollup 会移除该注释，不影响当前产物。
