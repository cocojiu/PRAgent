# 2026-06-19 优化进度记录

### 2026-06-19 架构优化 A45 Notification 候选绑定批量投递编排拆分

已完成：
- 新增 `NotificationBindingBatchDeliveryService`，集中候选通知绑定加载、逐个绑定投递调用和 `NotificationDeliveryResultSummary` 汇总。
- `NotificationDeliveryWorker` 不再直接依赖 `NotificationCandidateBindingQuery` 和 `NotificationBindingDeliveryService`，主流程只负责可投递事件加载、payload 解析、事件状态推进、批量投递结果分流和最终状态写回。
- 保持候选绑定查询条件、单绑定投递语义、跳过项不计入实际投递结果、任一实际投递失败触发失败补偿、无实际投递失败则标记事件已投递的原有行为不变。
- 新增 `NotificationBindingBatchDeliveryServiceTest`，覆盖多候选绑定成功/失败/跳过混合汇总和无候选绑定空汇总。
- 调整 `NotificationDeliveryWorkerTest` 的测试装配，使其通过批量投递服务验证 worker 事件级编排不变。

验证记录：
- `mvn "-Dtest=NotificationDeliveryWorkerTest,NotificationBindingBatchDeliveryServiceTest,NotificationBindingDeliveryServiceTest,NotificationDeliverableEventQueryTest,NotificationDeliveryResultSummaryTest,NotificationSuccessfulDeliveryQueryTest,NotificationDeliveryEventStateUpdaterTest,NotificationCandidateBindingQueryTest,NotificationBindingMatcherTest,NotificationEventPayloadParserTest,NotificationDeliveryFailurePolicyTest,NotificationDeliveryLogFactoryTest,NotificationStatusTest,SpringBeanConstructorSelectionTest" test` 通过，41 个用例全部成功。
- `mvn test` 后端全量测试通过，425 个用例全部成功。

阶段结论：
- `NotificationDeliveryWorker` 已从候选绑定批量投递细节中解耦，当前职责进一步收敛为事件级投递编排和最终状态分流。
- 下一步建议检查 worker 是否还需要抽出“事件级投递完成决策”边界，集中处理 `NotificationDeliveryResultSummary` 到成功/失败状态写回的映射；若收益有限，可转向下一个架构热点继续按小步规则推进。

### 2026-06-19 架构优化 A46 Notification 投递完成状态分流拆分

已完成：
- 新增 `NotificationDeliveryCompletionService`，集中 `NotificationDeliveryResultSummary` 到 outbox 事件最终状态的分流处理。
- `NotificationDeliveryWorker` 不再直接依赖 `NotificationDeliveryFailurePolicy`，也不再内联成功标记和失败补偿决策；worker 主流程进一步收敛为事件加载、payload 解析、投递中状态推进、批量投递调用和完成处理委托。
- 保持任一实际投递失败时调用失败补偿策略、无失败时标记 `DELIVERED`、失败补偿字段和状态写回语义不变。
- 新增 `NotificationDeliveryCompletionServiceTest`，覆盖无失败标记已投递和有失败时应用补偿决策两类完成分流。
- 调整 `NotificationDeliveryWorkerTest` 的测试装配，使 worker 通过完成处理服务验证事件级投递流程不变。

验证记录：
- `mvn "-Dtest=NotificationDeliveryWorkerTest,NotificationDeliveryCompletionServiceTest,NotificationBindingBatchDeliveryServiceTest,NotificationBindingDeliveryServiceTest,NotificationDeliverableEventQueryTest,NotificationDeliveryResultSummaryTest,NotificationSuccessfulDeliveryQueryTest,NotificationDeliveryEventStateUpdaterTest,NotificationCandidateBindingQueryTest,NotificationBindingMatcherTest,NotificationEventPayloadParserTest,NotificationDeliveryFailurePolicyTest,NotificationDeliveryLogFactoryTest,NotificationStatusTest,SpringBeanConstructorSelectionTest" test` 通过，43 个用例全部成功。
- `mvn test` 后端全量测试通过，427 个用例全部成功。

阶段结论：
- `NotificationDeliveryWorker` 已基本收敛为 Rabbit 消费入口和事件级投递编排，投递准入、payload 解析、批量绑定投递、完成状态分流、失败补偿和状态写回均已独立成可测试边界。
- 下一步建议暂停继续细拆 `NotificationDeliveryWorker`，转向下一个架构热点；可优先复查通知模块剩余大类如 `NotificationDispatchServiceImpl`、`NotificationIntegrationServiceImpl` 或回到 Dashboard SQL 可验证性、前端大页面拆分等既定目标。

### 2026-06-19 架构优化 A47 Notification 发布状态写回边界拆分

已完成：
- 新增 `NotificationPublishEventStateUpdater`，集中通知 outbox 发布成功和发布失败两类状态写回。
- `NotificationDispatchServiceImpl` 不再直接内联发布成功的 `PUBLISHED` 写回和发布失败的 `PUBLISH_FAILED`/`DEAD` 补偿字段写回，改为委托状态更新器。
- 保持发布成功不覆盖 `DELIVERED` 事件、清空 `last_error`、发布失败保留失败策略决策字段、更新 `retry_count`/`next_retry_at`/`last_error` 的原有语义不变。
- 新增 `NotificationPublishEventStateUpdaterTest`，覆盖发布成功写回和发布失败决策写回。
- 调整 `NotificationDispatchServiceImplTest` 的测试装配，使其通过发布状态更新器验证 dispatch 服务外部行为不变。

验证记录：
- `mvn "-Dtest=NotificationDispatchServiceImplTest,NotificationPublishEventStateUpdaterTest,NotificationPublishFailurePolicyTest,NotificationEventPublishCompensatorTest,NotificationEventPayloadBuilderTest,NotificationStatusTest,SpringBeanConstructorSelectionTest" test` 通过，19 个用例全部成功。
- `mvn test` 后端全量测试通过，429 个用例全部成功。

阶段结论：
- `NotificationDispatchServiceImpl` 的发布状态写回职责已独立成可测试边界，服务本身更聚焦 outbox 事件创建、事务后发布和失败策略编排。
- 下一步建议继续收敛 dispatch 服务中的 outbox 事件创建与重复事件回读边界，或评估 `NotificationEventPublishCompensator` 的补偿查询是否需要独立查询对象；仍保持小步拆分和测试门禁。

### 2026-06-19 架构优化 A48 Notification Outbox 事件存储边界拆分

已完成：
- 新增 `NotificationOutboxEventStore`，集中通知 outbox 事件创建、初始 `PENDING` 状态字段、时间字段、重复 eventKey 插入冲突后的既有事件回读，以及按 id 加载事件。
- `NotificationDispatchServiceImpl` 不再直接依赖 `NotificationEventMapper`，也不再内联 outbox 事件实体组装、插入和重复键回读逻辑。
- 保持 eventKey、eventType、taskId、batchId、payload、`PENDING`、`retryCount = 0`、`nextRetryAt`、`createdAt`、`updatedAt` 初始化语义不变。
- 保持重复 eventKey 时复用既有 outbox 事件并继续按原流程发布的语义不变。
- 新增 `NotificationOutboxEventStoreTest`，覆盖新建待发布事件、重复事件回读和按 id 加载。
- 调整 `NotificationDispatchServiceImplTest` 的测试装配，使 dispatch 服务通过 outbox store 验证事件创建和发布行为不变。

验证记录：
- `mvn "-Dtest=NotificationDispatchServiceImplTest,NotificationOutboxEventStoreTest,NotificationPublishEventStateUpdaterTest,NotificationPublishFailurePolicyTest,NotificationEventPublishCompensatorTest,NotificationEventPayloadBuilderTest,NotificationStatusTest,SpringBeanConstructorSelectionTest" test` 通过，22 个用例全部成功。
- `mvn test` 后端全量测试通过，432 个用例全部成功。

阶段结论：
- `NotificationDispatchServiceImpl` 已进一步收敛为通知事件类型选择、payload 构建、事务后发布和发布失败策略编排，outbox 持久化细节已独立成可测试边界。
- 下一步建议评估 `NotificationEventPublishCompensator` 的补偿查询是否需要独立查询对象，或转向 Dashboard SQL 可验证性、前端大页面拆分等下一批架构热点；仍保持小步拆分和测试门禁。

### 2026-06-19 架构优化 A49 Notification 发布补偿查询边界拆分

已完成：
- 新增 `NotificationPublishCompensationQuery`，集中通知 outbox 发布补偿候选事件查询规则。
- `NotificationEventPublishCompensator` 不再直接依赖 `NotificationEventMapper` 和 `RabbitNotificationQueueProperties`，只负责定时触发、读取候选事件并调用 `NotificationDispatchService#publishExistingEvent`。
- 保持补偿候选状态集合 `PENDING`、`PUBLISH_FAILED`、`DELIVERY_FAILED` 不变。
- 保持 `nextRetryAt <= now`、`retryCount < maxAttempts`、按 `nextRetryAt` 升序、按 batch size limit 查询的原有语义不变。
- 新增 `NotificationPublishCompensationQueryTest`，覆盖候选事件查询委托和非正 maxAttempts/batchSize 归一化。
- 调整 `NotificationEventPublishCompensatorTest` 的测试装配，使 compensator 通过查询对象验证补偿发布委托行为不变。

验证记录：
- `mvn "-Dtest=NotificationEventPublishCompensatorTest,NotificationPublishCompensationQueryTest,NotificationDispatchServiceImplTest,NotificationOutboxEventStoreTest,NotificationPublishEventStateUpdaterTest,NotificationPublishFailurePolicyTest,NotificationStatusTest,SpringBeanConstructorSelectionTest" test` 通过，22 个用例全部成功。
- `mvn test` 后端全量测试通过，434 个用例全部成功。

阶段结论：
- 通知发布补偿链路已完成查询、事件存储、发布状态写回和失败策略等关键边界拆分；`NotificationEventPublishCompensator` 已收敛为轻量调度编排。
- 下一步建议转向下一批架构热点，优先评估 Dashboard SQL 可验证性或前端大页面拆分；通知模块后续可按需继续检查配置查询和运维查询边界，但不建议继续围绕同一 worker/compensator 做低收益细拆。

### 2026-06-19 架构优化 A50 Dashboard LLM 趋势窗口与补零构建边界拆分

已完成：
- 新增 `DashboardLlmQualityTrendBuilder`，集中 Dashboard LLM 质量趋势的天数归一化、SQL 查询起始日期计算、日期序列补零、空 dayKey 过滤和成功率/回退率 DTO 组装。
- `DashboardServiceImpl` 不再直接依赖 `LocalDate.now()` 计算趋势窗口，也不再内联 LLM 趋势补零和安全计数逻辑；主服务仅负责获取窗口、调用 `DashboardMapper#selectLlmQualityTrendCounts(startDate)` 并委托 builder 构建响应。
- 保持 Dashboard Controller/API、DTO 字段、缓存 key、`DashboardMapper` SQL 文本和 LLM 趋势 7/30/90 天语义不变。
- 新增 `DashboardLlmQualityTrendBuilderTest`，通过固定时钟覆盖默认 7 天、支持 30/90 天窗口、缺失日期补零、空 dayKey 忽略和比例格式化。
- 调整 `DashboardServiceImplTest` 使用固定时钟的趋势 builder，并验证 7 天窗口会向 SQL 查询传入稳定的 `2026-06-13` 起始日期。

验证记录：
- `mvn "-Dtest=DashboardServiceImplTest,DashboardLlmQualityTrendBuilderTest,DashboardLlmQualityFormatterTest,DashboardStatusMapperTest,DashboardOverviewDisplayMapperTest,DashboardRuleDisplayMapperTest,SpringBeanConstructorSelectionTest" test` 通过，27 个 Dashboard 服务、趋势构建、格式化、展示映射和构造器守卫用例全部成功。
- `mvn test` 后端全量测试通过，437 个用例全部成功。

阶段结论：
- Dashboard LLM 趋势的“查询窗口”和“展示序列补零”已经从主服务中拆出，SQL 入参可通过单元测试稳定验证，后续调整时间窗口或演进为按日汇总读模型时不需要继续扩大 `DashboardServiceImpl`。
- 下一步建议继续围绕 Dashboard SQL 可验证性推进：优先补充关键聚合查询的索引假设、`date_format(created_at, ...)` 分组的性能风险说明，或将其它时间趋势查询的窗口入参边界显式化；也可以转向前端大页面拆分。

### 2026-06-19 架构优化 A51 Dashboard 聚合 SQL 契约守卫补强

已完成：
- 新增 `DashboardMapperSqlContractTest`，通过反射读取 `DashboardMapper` 上的 `@Select` SQL，将关键 Dashboard 聚合查询约束转为自动化测试。
- 覆盖 LLM 质量趋势查询的 `created_at >= #{startDate}` 时间窗口、非空/非 `PENDING` LLM 状态过滤、按日 `date_format(created_at, '%Y-%m-%d')` 聚合契约。
- 覆盖高风险最近审查查询的 `risk_level in ('HIGH', 'CRITICAL')` 过滤、`review_finding` 左连接、`created_at desc` 排序和 `limit 5` 契约。
- 覆盖规则命中查询的 `category = 'FINDING'` 过滤、`coalesce(rule_id, 'LLM')` 兜底分组契约。
- 覆盖 LLM 按模型/仓库质量统计查询中的 LLM 状态过滤、finding 反馈 join、反馈状态统计、按任务量排序和 `limit 6` 契约。
- 读取 Flyway 迁移 SQL，守卫 Dashboard 聚合依赖的关键索引假设：`idx_review_task_created_at`、`idx_review_task_risk_created`、`idx_review_finding_category`、`idx_review_finding_task`、`idx_review_task_llm_quality`。
- 不改变 Dashboard Controller/API、Mapper SQL 文本、DTO 字段、缓存 key 和运行时业务行为，仅补强 SQL 可验证性门禁。

验证记录：
- `mvn "-Dtest=DashboardMapperSqlContractTest,DashboardServiceImplTest,DashboardLlmQualityTrendBuilderTest,DashboardLlmQualityFormatterTest,DashboardStatusMapperTest,DashboardOverviewDisplayMapperTest,DashboardRuleDisplayMapperTest,SpringBeanConstructorSelectionTest" test` 通过，32 个 Dashboard SQL 契约、服务、趋势构建、格式化、展示映射和构造器守卫用例全部成功。
- `mvn test` 后端全量测试通过，442 个用例全部成功。

阶段结论：
- Dashboard 聚合 SQL 的关键过滤、排序、限制、反馈 join 和索引迁移假设已有自动化守卫，后续调整 `DashboardMapper` 或 Flyway 迁移时更容易及时发现契约漂移。
- 下一步建议继续做一个更实质的 SQL 演进小步：优先将 `selectReviewTrendCounts` 的全量 `date_format(created_at, '%m-%d')` 聚合改为显式时间窗口，或补充 Dashboard 聚合 SQL 的索引/汇总表演进说明；也可以转向前端大页面拆分。

### 2026-06-19 架构优化 A52 Dashboard 审查趋势 SQL 时间窗口显式化

已完成：
- 新增 `DashboardReviewTrendWindow`，集中 Dashboard 审查趋势的默认 7 天窗口和起始日期计算，生产环境使用系统时钟，测试通过固定时钟稳定验证。
- `DashboardMapper#selectReviewTrendCounts` 从无参全量聚合改为接收 `startDate`，SQL 增加 `where created_at >= #{startDate}`，避免 Dashboard 审查趋势每次冷查都扫描全量历史任务。
- `DashboardServiceImpl` 改为通过 `DashboardReviewTrendWindow#startDate()` 调用审查趋势聚合查询，Controller/API、DTO 字段、缓存 key 和返回结构保持不变。
- `DashboardServiceImplTest` 补充固定时钟验证，确认默认窗口会向 SQL 查询传入 `2026-06-13` 起始日期。
- `DashboardMapperSqlContractTest` 补充审查趋势 SQL 契约守卫，覆盖 `created_at >= #{startDate}`、`date_format(created_at, '%m-%d')` 日粒度聚合和 `order by dayLabel`。

验证记录：
- `mvn "-Dtest=DashboardMapperSqlContractTest,DashboardServiceImplTest,DashboardLlmQualityTrendBuilderTest,DashboardLlmQualityFormatterTest,DashboardStatusMapperTest,DashboardOverviewDisplayMapperTest,DashboardRuleDisplayMapperTest,SpringBeanConstructorSelectionTest" test` 通过，33 个 Dashboard SQL 契约、服务、趋势构建、格式化、展示映射和构造器守卫用例全部成功。
- `mvn test` 后端全量测试通过，443 个用例全部成功。

阶段结论：
- Dashboard 审查趋势已从全量历史聚合推进到显式近期窗口查询，查询范围可测试、可演进，并与既有 `idx_review_task_created_at` 索引假设对齐。
- 下一步建议继续检查 Dashboard 其它聚合查询的冷热路径：优先评估风险分布和指标总览是否需要时间窗口或汇总表演进说明；也可以转向前端大页面拆分。

### 2026-06-19 架构优化 A53 Dashboard 指标总览 SQL 时间窗口显式化

已完成：
- `DashboardMapper#selectMetricStat` 从无参全量聚合改为接收 `startDate`，SQL 增加 `where created_at >= #{startDate}`，让 Dashboard 顶部指标不再每次冷查扫描全量历史任务。
- `DashboardServiceImpl` 复用 `DashboardReviewTrendWindow` 计算出的默认 7 天窗口，将指标总览与审查趋势聚合统一到同一近期窗口边界。
- 保持 Dashboard Controller/API、DTO 字段、缓存 key 和返回结构不变，仅收敛后端聚合查询范围。
- `DashboardServiceImplTest` 通过固定时钟验证，确认默认窗口会向 `selectMetricStat` 传入 `2026-06-13` 起始日期。
- `DashboardMapperSqlContractTest` 补充指标总览 SQL 契约守卫，覆盖 `created_at >= #{startDate}`、总量、高风险、失败数和平均耗时聚合字段。

验证记录：
- `mvn "-Dtest=DashboardMapperSqlContractTest,DashboardServiceImplTest,DashboardLlmQualityTrendBuilderTest,DashboardLlmQualityFormatterTest,DashboardStatusMapperTest,DashboardOverviewDisplayMapperTest,DashboardRuleDisplayMapperTest,SpringBeanConstructorSelectionTest" test` 通过，34 个 Dashboard SQL 契约、服务、趋势构建、格式化、展示映射和构造器守卫用例全部成功。
- `mvn test` 后端全量测试通过，444 个用例全部成功。

阶段结论：
- Dashboard “本周审查/高风险/失败/平均耗时” 已从全量历史聚合推进到显式近期窗口查询，指标文案、审查趋势窗口和 `idx_review_task_created_at` 索引假设完成对齐。
- 下一步建议继续评估风险分布或规则命中聚合是否需要同样的时间窗口约束，或者转向前端大页面拆分；仍按小步、契约不变、测试先行的规则推进。

### 2026-06-19 架构优化 A54 Dashboard 风险分布 SQL 时间窗口显式化

已完成：
- `DashboardMapper#selectRiskLevelCounts` 从无参全量风险分布聚合改为接收 `startDate`，SQL 增加 `where created_at >= #{startDate}`，避免 Dashboard 风险分布冷查扫描全量历史任务。
- `DashboardServiceImpl` 复用 `DashboardReviewTrendWindow` 计算出的默认 7 天窗口，将风险分布、指标总览和审查趋势统一到同一个近期窗口边界。
- 保持 Dashboard Controller/API、DTO 字段、缓存 key、风险等级展示顺序、颜色和百分比计算逻辑不变，仅收敛后端聚合查询范围。
- `DashboardServiceImplTest` 通过固定时钟验证，确认默认窗口会向 `selectRiskLevelCounts` 传入 `2026-06-13` 起始日期。
- `DashboardMapperSqlContractTest` 补充风险分布 SQL 契约守卫，覆盖 `created_at >= #{startDate}`、`risk_level` 分组和计数字段。

验证记录：
- `mvn "-Dtest=DashboardMapperSqlContractTest,DashboardServiceImplTest,DashboardLlmQualityTrendBuilderTest,DashboardLlmQualityFormatterTest,DashboardStatusMapperTest,DashboardOverviewDisplayMapperTest,DashboardRuleDisplayMapperTest,SpringBeanConstructorSelectionTest" test` 通过，35 个 Dashboard SQL 契约、服务、趋势构建、格式化、展示映射和构造器守卫用例全部成功。
- `mvn test` 后端全量测试通过，445 个用例全部成功。

阶段结论：
- Dashboard 风险分布已从全量历史聚合推进到显式近期窗口查询，与顶部指标和审查趋势的 7 天窗口保持一致，并继续对齐 `idx_review_task_created_at` 索引假设。
- 下一步建议继续评估规则命中/失败规则聚合是否需要按任务时间窗口约束，或为 LLM 模型/仓库质量统计设计独立窗口与索引演进方案；仍保持 Controller/API 契约不变和测试门禁。

### 2026-06-19 架构优化 A55 Dashboard 规则命中 SQL 时间窗口显式化

已完成：
- `DashboardMapper#selectRuleHitCounts` 从无参全量 `review_finding` 聚合改为接收 `startDate`，通过 `review_finding f join review_task t on t.id = f.task_id` 使用任务创建时间收敛查询窗口。
- SQL 保持 `f.category = 'FINDING'` 过滤和 `coalesce(f.rule_id, 'LLM')` 兜底分组语义不变，同时增加 `t.created_at >= #{startDate}`，让规则命中和失败规则统计不再扫描全量发现项。
- `DashboardServiceImpl` 复用 `DashboardReviewTrendWindow` 计算出的默认 7 天窗口，将规则命中、失败规则、风险分布、指标总览和审查趋势统一到同一个近期窗口边界。
- 保持 Dashboard Controller/API、DTO 字段、缓存 key、规则展示名称、颜色、排序和百分比计算逻辑不变，仅收敛后端聚合查询范围。
- `DashboardServiceImplTest` 通过固定时钟验证，确认默认窗口会向 `selectRuleHitCounts` 传入 `2026-06-13` 起始日期。
- `DashboardMapperSqlContractTest` 补充规则命中 SQL 契约守卫，覆盖 finding/task join、任务时间窗口、`FINDING` 类别过滤和 `LLM` 兜底分组。

验证记录：
- `mvn "-Dtest=DashboardMapperSqlContractTest,DashboardServiceImplTest,DashboardLlmQualityTrendBuilderTest,DashboardLlmQualityFormatterTest,DashboardStatusMapperTest,DashboardOverviewDisplayMapperTest,DashboardRuleDisplayMapperTest,SpringBeanConstructorSelectionTest" test` 通过，35 个 Dashboard SQL 契约、服务、趋势构建、格式化、展示映射和构造器守卫用例全部成功。
- `mvn test` 后端全量测试通过，445 个用例全部成功。

阶段结论：
- Dashboard 规则命中和失败规则聚合已从全量发现项扫描推进到按任务近期窗口聚合，并复用既有 `review_finding.task_id` 与 `review_task.created_at` 索引假设。
- 下一步建议评估 LLM 模型/仓库质量统计是否需要独立时间窗口、索引或汇总表演进方案；这两类查询包含子查询和 feedback join，建议继续按小步拆分并保持契约测试先行。

### 2026-06-19 架构优化 A56 Dashboard LLM 质量统计 SQL 时间窗口显式化

已完成：
- `DashboardMapper#selectLlmQualityByModelStats` 和 `DashboardMapper#selectLlmQualityByRepositoryStats` 从无参全量聚合改为接收 `startDate`，模型与仓库质量统计的 `task_stats` 子查询都增加 `created_at >= #{startDate}`。
- 两个 feedback 统计子查询同步增加 `t.created_at >= #{startDate}`，保证任务数、回退率、部分回退率、有效反馈率和误报率使用同一近期任务窗口，避免只过滤主统计或只过滤反馈统计造成口径漂移。
- `DashboardServiceImpl` 复用 `DashboardReviewTrendWindow` 的默认 7 天窗口，把 LLM 模型/仓库质量统计与指标总览、风险分布、审查趋势、规则命中统一到同一个近期窗口边界。
- 保持 Dashboard Controller/API、DTO 字段、缓存 key、模型/仓库展示格式、排序、`limit 6` 和反馈 join 语义不变，仅收敛后端聚合查询范围。
- `DashboardMapperSqlContractTest` 补强模型/仓库质量 SQL 合约，覆盖方法入参、主统计窗口、feedback join 窗口、LLM 状态过滤、反馈状态统计、排序和限制条数。
- `DashboardServiceImplTest` 通过固定时钟验证默认窗口会向模型/仓库质量统计传入 `2026-06-13` 起始日期。

验证记录：
- `mvn "-Dtest=DashboardMapperSqlContractTest,DashboardServiceImplTest,DashboardLlmQualityTrendBuilderTest,DashboardLlmQualityFormatterTest,DashboardStatusMapperTest,DashboardOverviewDisplayMapperTest,DashboardRuleDisplayMapperTest,SpringBeanConstructorSelectionTest" test` 通过：35 个 Dashboard SQL 合约、服务、趋势构建、格式化、展示映射和构造器守卫用例全部成功。
- `mvn test` 后端全量测试通过：445 个用例全部成功。

阶段结论：
- Dashboard LLM 模型/仓库质量统计已从全量历史扫描推进到显式近期窗口聚合，并且主任务统计与 feedback 统计共享同一窗口边界，继续对齐 `idx_review_task_created_at` 与 `idx_review_task_llm_quality` 索引假设。
- 下一步建议评估剩余 Dashboard 冷路径：`selectRecentHighRiskReviews` 是否需要独立窗口、是否应增加更贴近 LLM breakdown 的复合索引，或转向前端大页面拆分；仍保持小步、契约不变、测试先行。

### 2026-06-19 架构优化 A57 Dashboard 最近高风险列表 SQL 时间窗口显式化

已完成：
- `DashboardMapper#selectRecentHighRiskReviews` 从无参全量高风险历史查询改为接收 `startDate`，SQL 在 `risk_level in ('HIGH', 'CRITICAL')` 基础上增加 `t.created_at >= #{startDate}`。
- `DashboardServiceImpl` 复用 `DashboardReviewTrendWindow` 的默认 7 天窗口，把最近高风险列表与指标总览、风险分布、审查趋势、规则命中、LLM 模型/仓库质量统计统一到同一个近期窗口边界。
- 保持 Dashboard Controller/API、DTO 字段、缓存 key、最近高风险列表 `limit 5`、`created_at desc` 排序、finding 左连接和规则命中计数语义不变，仅收敛后端查询范围。
- `DashboardMapperSqlContractTest` 补强最近高风险 SQL 合约，覆盖方法入参、风险等级过滤、finding 左连接、任务时间窗口、排序和限制条数。
- `DashboardServiceImplTest` 通过固定时钟验证默认窗口会向最近高风险查询传入 `2026-06-13` 起始日期。

验证记录：
- `mvn "-Dtest=DashboardMapperSqlContractTest,DashboardServiceImplTest,DashboardLlmQualityTrendBuilderTest,DashboardLlmQualityFormatterTest,DashboardStatusMapperTest,DashboardOverviewDisplayMapperTest,DashboardRuleDisplayMapperTest,SpringBeanConstructorSelectionTest" test` 通过：35 个 Dashboard SQL 合约、服务、趋势构建、格式化、展示映射和构造器守卫用例全部成功。
- `mvn test` 后端全量测试通过：445 个用例全部成功。

阶段结论：
- Dashboard 总览页主要实时聚合和最近高风险冷路径已统一收敛到显式近期窗口，查询范围、排序、限制和 join 契约均有自动化守卫。
- 下一步建议评估 Dashboard 聚合索引是否需要针对 LLM breakdown 增加更贴近 `llm_status`、`created_at`、`llm_model`/`repository` 的复合索引，或转向前端大页面拆分；仍保持小步、契约不变、测试先行。

### 2026-06-19 架构优化 A58 Dashboard LLM breakdown 近期窗口索引补齐

已完成：
- 新增 Flyway 迁移 `V27__dashboard_llm_breakdown_indexes.sql`，为 Dashboard LLM 模型/仓库质量统计补齐近期窗口访问路径。
- 新增 `idx_review_task_llm_model_window (llm_status, created_at, llm_provider, llm_model)`，对齐模型质量统计中的 LLM 状态过滤、`created_at` 时间窗口和 provider/model 分组标签。
- 新增 `idx_review_task_llm_repository_window (llm_status, created_at, organization, repository)`，对齐仓库质量统计中的 LLM 状态过滤、`created_at` 时间窗口和 organization/repository 分组标签。
- 保持 Dashboard Mapper SQL、Controller/API、DTO 字段、缓存 key 和返回语义不变，仅补齐数据库索引演进。
- `DashboardMapperSqlContractTest` 补充迁移索引假设守卫，确保 Dashboard 聚合依赖的 LLM breakdown 窗口索引不会在后续迁移中被遗漏。

验证记录：
- `mvn "-Dtest=DashboardMapperSqlContractTest" test` 通过：8 个 Dashboard SQL 合约与索引迁移假设用例全部成功。
- `mvn test` 后端全量测试通过：445 个用例全部成功。

阶段结论：
- Dashboard LLM breakdown 已完成“查询窗口显式化 + 索引路径补齐”的闭环，后续如果数据量继续增长，可进一步评估按日/按模型/按仓库汇总读模型，而不是继续堆叠实时聚合 SQL。
- 下一步建议转向前端 Dashboard 大页面拆分，或评估 Dashboard 聚合是否需要独立 read-model/summary-table 方案；仍保持小步、契约不变、测试先行。

### 2026-06-19 架构优化 A59 前端 Dashboard 图表配置边界拆分

已完成：
- 新增 `repoguard-frontend/src/pages/overviewChartOptions.ts`，集中 Dashboard Overview 中审查趋势、风险分布、规则命中和 LLM 质量趋势 4 类 ECharts option 构建逻辑。
- `OverviewPage.vue` 不再直接维护大段 ECharts 配置，仅保留页面数据加载、状态维护、布局和 computed 调用，图表配置演进从页面主文件中剥离。
- 保持 Dashboard API、路由、组件结构、图表视觉参数、表格展示和交互语义不变，仅调整前端代码边界。
- 抽取过程中将 LLM 质量趋势百分比转换 helper 收敛到图表配置模块内部，避免页面脚本继续承载图表内部数据转换细节。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- Dashboard Overview 页面已经从“页面 + 图表配置大段内联”推进到“页面编排 + 图表配置模块”分层，后续拆分高风险表格、LLM 质量表格或数据加载 composable 时风险更低。
- 下一步建议继续按前端大页面拆分顺序推进：优先评估是否抽出 LLM 质量区域或底部高风险/失败规则/系统健康区域为独立展示组件；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A60 前端 Dashboard LLM 质量区域组件化

已完成：
- 新增 `repoguard-frontend/src/components/LlmQualitySection.vue`，集中承载 Dashboard Overview 中 LLM 质量趋势图、时间窗口分段控件、模型质量表格和仓库质量表格。
- `OverviewPage.vue` 不再直接维护 LLM 质量区域的大段模板和表格列定义，仅负责传入数据、趋势窗口、加载态、图表 option，并接收窗口切换事件触发重新加载。
- 保持 Dashboard API、路由、样式类名、表格字段、空态文案、图表 option 构建逻辑和窗口参数语义不变，仅调整前端展示组件边界。
- LLM 趋势窗口切换从页面内联 `@change` 收敛为组件事件 `trendDaysChange`，页面仍以 `llmTrendDays` 作为唯一状态源，避免组件内部耦合数据请求。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- Dashboard Overview 页已从“页面编排 + 图表配置模块”继续推进到“页面编排 + LLM 质量展示组件 + 图表配置模块”的分层，页面主文件模板体积和表格细节负担进一步下降。
- 下一步建议继续按前端大页面拆分顺序推进：优先评估底部高风险审查、失败规则和系统健康区域是否抽成独立概览组件；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A61 前端 Dashboard 底部概览区域组件化

已完成：
- 新增 `repoguard-frontend/src/components/DashboardBottomSection.vue`，集中承载 Dashboard Overview 底部近期高风险审查、高频失败规则和系统健康三类展示卡片。
- `OverviewPage.vue` 不再直接维护底部三张卡片的大段模板、表格列定义、风险等级文案转换和跳转链接，仅负责传入数据、加载态、最后健康检查时间，并接收刷新事件。
- 保持 Dashboard API、路由目标、样式类名、表格字段、空态文案、风险等级展示和系统健康刷新语义不变，仅调整前端展示组件边界。
- 底部系统健康刷新从页面按钮内联调用收敛为组件 `refresh` 事件，页面仍统一复用 `loadOverview`，避免展示组件耦合数据请求实现。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- Dashboard Overview 页已完成图表配置、LLM 质量区域和底部概览区域的连续边界拆分，页面主文件进一步收敛为数据加载、状态维护和模块编排。
- 下一步建议继续按前端大页面拆分顺序推进：评估顶部审查趋势/风险分布/规则命中图表区域是否抽成独立图表概览组件，或进一步将 Dashboard 数据加载与状态维护抽成 composable；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A62 前端 Dashboard 顶部图表区域组件化

已完成：
- 新增 `repoguard-frontend/src/components/DashboardChartSection.vue`，集中承载 Dashboard Overview 顶部审查趋势、风险分布和规则命中三类图表展示。
- `OverviewPage.vue` 不再直接维护顶部三张图表的大段模板、空态文案和规则命中图例，仅负责传入图表数据与已构建的 ECharts option。
- 保持 Dashboard API、图表 option 构建模块、样式类名、图表视觉参数、规则图例字段和空态文案不变，仅调整前端展示组件边界。
- 顶部图表区域与 A59 的 `overviewChartOptions.ts` 形成“图表展示组件 + option 构建模块”的分层，避免页面主文件重新承载图表细节。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- Dashboard Overview 页的主要展示区域已拆分为指标网格、顶部图表、LLM 质量和底部概览四个展示边界，页面主文件进一步聚焦数据加载、状态维护和模块编排。
- 下一步建议评估将 Dashboard 数据加载、错误处理、时间窗口状态和派生 computed 抽成 `useDashboardOverview` composable；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A63 前端 Dashboard 数据状态 composable 抽取

已完成：
- 新增 `repoguard-frontend/src/composables/useDashboardOverview.ts`，集中管理 Dashboard Overview 的加载态、错误消息、最后健康检查时间、LLM 趋势窗口、接口请求和派生数据。
- `OverviewPage.vue` 不再直接维护 `DashboardOverview` 空对象、接口调用、错误提示、窗口切换逻辑、指标项映射和各类 computed 数据，只保留页面组件编排、图标映射和图表 option 组装。
- 保持 Dashboard API 请求参数、错误文案、Element Plus 提示、健康检查时间格式、LLM 窗口选项、指标趋势文案和所有展示组件入参语义不变，仅调整前端状态管理边界。
- `useDashboardOverview` 暴露 `loadOverview` 与 `updateLlmTrendDays`，页面仍统一触发加载，展示组件继续通过事件通知页面/composable，不反向耦合 API 实现。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- Dashboard Overview 页已从“大页面承担模板、图表配置、数据加载、派生状态”推进到“页面编排 + 展示组件 + 图表配置模块 + 数据状态 composable”的分层。
- 下一步建议评估是否将 Dashboard 相关展示组件、图表 option 和 composable 归并到 `features/dashboard` 或 `pages/overview` 子目录，降低全局 `components` 目录膨胀；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A64 前端 Dashboard feature 目录归并

已完成：
- 新增 `repoguard-frontend/src/features/dashboard/`，将 Dashboard Overview 专属展示组件、数据状态 composable 和图表 option 构建模块归并到该 feature 目录。
- `DashboardChartSection.vue`、`LlmQualitySection.vue`、`DashboardBottomSection.vue` 从全局 `components` 目录移动到 `features/dashboard/components/`，避免业务专属组件继续膨胀公共组件层。
- `useDashboardOverview.ts` 从全局 `composables` 目录移动到 `features/dashboard/composables/`，`overviewChartOptions.ts` 从 `pages` 目录移动到 `features/dashboard/`，与 Dashboard 展示组件形成同一业务边界。
- `OverviewPage.vue` 更新为从 `@/features/dashboard/...` 引入 Dashboard 专属能力，公共的 `MetricGrid`、`EChartPanel`、`useMetricIcon` 仍保留在全局公共目录。
- 保持 Dashboard API、页面路由、展示组件 props/events、图表 option 内容、状态管理逻辑和视觉表现不变，仅调整前端文件归属与 import 边界。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- Dashboard Overview 前端已形成“页面入口 + feature/dashboard 业务模块 + 公共基础组件”的目录结构，业务专属展示、状态和图表配置不再散落在全局 `components`、`composables` 和 `pages` 下。
- 下一步建议评估是否为 `features/dashboard` 增加 barrel export 或轻量目录 README/边界说明，或转向下一个高复杂度页面的同类拆分；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A65 前端 Dashboard feature 统一出口

已完成：
- 新增 `repoguard-frontend/src/features/dashboard/index.ts`，统一导出 Dashboard Overview 的展示组件、数据状态 composable 和图表 option 构建函数。
- `OverviewPage.vue` 从多条 `@/features/dashboard/components/...`、`@/features/dashboard/composables/...`、`@/features/dashboard/overviewChartOptions` 深层路径导入，收敛为单一 `@/features/dashboard` feature 入口。
- 保持 Dashboard API、页面路由、组件 props/events、图表 option 内容、状态管理逻辑和视觉表现不变，仅调整前端模块公开边界。
- 通过搜索确认 `src` 下没有外部代码继续直接依赖 `@/features/dashboard/` 子路径，Dashboard feature 内部目录结构可以独立演进。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- Dashboard feature 已从“目录归并”进一步推进到“统一公开出口”，页面入口不再感知 feature 内部组件、composable 和 option 文件布局。
- 下一步建议转向下一个高复杂度前端页面做同类拆分，优先评估 `ReviewDetailPage.vue` 或 `NotificationOpsPage.vue` 的页面状态、展示区块与业务组件边界；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A66 前端 ReviewDetail KPI 区域组件化

已完成：
- 新增 `repoguard-frontend/src/features/review-detail/components/ReviewDetailKpiGrid.vue`，集中承载审查详情页顶部仓库、Commit、耗时和审查发现四个 KPI 卡片。
- 新增 `repoguard-frontend/src/features/review-detail/index.ts`，作为 ReviewDetail feature 的统一出口，页面从 `@/features/review-detail` 引入详情页专属展示组件。
- `ReviewDetailPage.vue` 不再直接维护顶部 KPI 网格的大段模板和 KPI 专用图标 import，仅负责传入当前任务、开始时间、发现数和变更文件数。
- 保持审查详情 API、路由、状态计算、KPI 文案、样式类名、图标语义和页面交互不变，仅调整前端展示组件边界。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- ReviewDetail 页已开始形成 feature 化拆分入口，先从低风险、纯展示的 KPI 网格切入，避免一次性搬动详情页大量状态和回写逻辑。
- 下一步建议继续按小步拆分 ReviewDetail：优先评估摘要/风险画像区域或缺失测试/变更文件表格区域组件化；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A67 前端 ReviewDetail 摘要风险区域组件化

已完成：
- 新增 `repoguard-frontend/src/features/review-detail/components/ReviewDetailSummaryCard.vue`，集中承载审查详情页审查摘要、严重级别计数、PR 风险画像和 PR 总评展示。
- `ReviewDetailPage.vue` 不再直接维护摘要/风险画像区域的大段模板，仅负责传入当前任务、发现计数、风险画像和既有文案转换函数。
- `repoguard-frontend/src/features/review-detail/index.ts` 继续作为 ReviewDetail feature 统一出口，导出新增的 `ReviewDetailSummaryCard`。
- 保持审查详情 API、路由、风险计数 computed、风险/状态文案、变更类型文案、样式类名和页面交互不变，仅调整前端展示组件边界。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- ReviewDetail 页已完成顶部 KPI 与摘要/风险画像两个纯展示区块的组件化，页面主文件进一步从“大段展示模板”向“状态/动作 + 展示组件编排”收敛。
- 下一步建议继续按小步拆分 ReviewDetail：优先评估缺失测试/变更文件表格区域或 LLM/RabbitMQ 侧栏状态卡组件化；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A68 前端 ReviewDetail 文件表格区域组件化

已完成：
- 新增 `repoguard-frontend/src/features/review-detail/components/ReviewDetailFilesSection.vue`，集中承载审查详情页缺失测试表格和变更文件表格。
- `ReviewDetailPage.vue` 不再直接维护缺失测试、变更文件两张表格的大段模板，仅负责传入缺失测试数据、带问题数的变更文件列表和既有变更类型文案函数。
- `repoguard-frontend/src/features/review-detail/index.ts` 继续作为 ReviewDetail feature 统一出口，导出新增的 `ReviewDetailFilesSection`。
- 保持审查详情 API、变更文件 finding 计数 computed、变更类型文案、表格列、空态文案、样式类名和页面交互不变，仅调整前端展示组件边界。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- ReviewDetail 页的 KPI、摘要风险、文件表格三个纯展示区块已完成组件化，页面主文件继续向状态/动作编排层收敛。
- 下一步建议继续按小步拆分 ReviewDetail：优先评估 LLM/RabbitMQ 侧栏状态卡或任务时间线组件化；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A69 前端 ReviewDetail 侧栏状态区域组件化

已完成：
- 新增 `repoguard-frontend/src/features/review-detail/components/ReviewDetailSidePanel.vue`，集中承载审查详情页任务时间线、LLM 状态和 RabbitMQ 状态三类侧栏卡片。
- `ReviewDetailPage.vue` 不再直接维护侧栏时间线、LLM 状态、分片审查和 RabbitMQ 状态的大段模板，仅负责传入当前任务、已本地化时间线和既有文案/状态转换函数。
- `repoguard-frontend/src/features/review-detail/index.ts` 继续作为 ReviewDetail feature 统一出口，导出新增的 `ReviewDetailSidePanel`。
- 保持审查详情 API、轮询逻辑、LLM/RabbitMQ 状态 computed、时间线本地化、文案转换、样式类名和页面交互不变，仅调整前端展示组件边界。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- ReviewDetail 页的 KPI、摘要风险、文件表格和侧栏状态四个纯展示区块已完成组件化，主页面模板体积继续下降，状态与动作逻辑仍集中在页面侧可控。
- 下一步建议继续按小步拆分 ReviewDetail：优先评估 GitHub 评论预览/回写历史区域，或再将详情页状态与轮询逻辑抽成 composable；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A70 前端 ReviewDetail GitHub 评论区域组件化

已完成：
- 新增 `repoguard-frontend/src/features/review-detail/components/ReviewDetailGithubCommentsCard.vue`，集中承载审查详情页 GitHub 评论预览、回写检查、发布结果和回写历史展示。
- `ReviewDetailPage.vue` 不再直接维护 GitHub 评论预览/回写历史的大段模板，仅负责传入预览数据、发布结果、历史批次、状态文案函数和发布事件处理。
- `ReviewDetailGithubCommentsCard` 通过 `publish` 事件触发页面原有 `confirmPublishGithubComments`，保留 GitHub 回写确认、发布、刷新预览和错误处理逻辑在页面侧。
- `repoguard-frontend/src/features/review-detail/index.ts` 继续作为 ReviewDetail feature 统一出口，导出新增的 `ReviewDetailGithubCommentsCard`。
- 保持审查详情 API、GitHub 回写流程、人工审查门禁、回写检查文案、发布状态文案、历史展示、路由跳转和样式类名不变，仅调整前端展示组件边界。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- ReviewDetail 页的主要展示区块已基本完成组件化，页面模板进一步收敛为动作/状态编排与 feature 组件组合。
- 下一步建议评估将 ReviewDetail 的加载、轮询、GitHub 评论预览/发布和人工审查动作抽成 composable，或继续拆人工审查门禁与审查发现列表区域；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A71 前端 ReviewDetail Findings 区域组件化

已完成：
- 新增 `repoguard-frontend/src/features/review-detail/components/ReviewDetailFindingsCard.vue`，集中承载审查详情页 LLM Findings 列表、严重级别、文件行号、反馈状态、反馈备注和反馈按钮展示。
- `ReviewDetailPage.vue` 不再直接维护 Findings 列表的大段模板，仅负责传入 findings 数据、反馈保存态、风险/反馈状态文案函数，并接收 `feedback` 事件触发原有 `submitFindingFeedback`。
- `repoguard-frontend/src/features/review-detail/index.ts` 继续作为 ReviewDetail feature 统一出口，导出新增的 `ReviewDetailFindingsCard`。
- 保持审查详情 API、finding 反馈弹窗、反馈提交、局部状态回填、GitHub 评论预览刷新、文案、按钮禁用规则和样式类名不变，仅调整前端展示组件边界。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- ReviewDetail 页的 KPI、摘要风险、侧栏状态、GitHub 评论、文件表格和 Findings 主要展示区块已完成 feature 组件化，页面模板继续收敛为状态/动作编排层。
- 下一步建议评估将 ReviewDetail 的人工审查门禁抽成展示组件，或开始拆详情加载、轮询、GitHub 评论预览/发布和反馈动作 composable；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A72 前端 ReviewDetail 人工审查门禁组件化

已完成：
- 新增 `repoguard-frontend/src/features/review-detail/components/ReviewDetailHumanReviewCard.vue`，集中承载审查详情页人工审查门禁、审查状态、审查备注、审查人/时间和三类人工决策按钮展示。
- `ReviewDetailPage.vue` 不再直接维护人工审查门禁模板，仅负责传入当前任务、状态文案、可提交态和提交中状态，并接收 `submit` 事件触发原有 `submitHumanReviewDecision`。
- `repoguard-frontend/src/features/review-detail/index.ts` 继续作为 ReviewDetail feature 统一出口，导出新增的 `ReviewDetailHumanReviewCard`。
- 保持审查详情 API、人工审查弹窗、必填校验、状态回填、GitHub 评论预览刷新、按钮禁用规则、文案和样式类名不变，仅调整前端展示组件边界。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- ReviewDetail 页的主要展示块已进一步沉淀到 `features/review-detail`，页面模板已基本只负责 header、失败提示、页面布局和 feature 组件编排。
- 下一步建议开始评估将 ReviewDetail 的加载/轮询/GitHub 评论预览与发布/finding 反馈/人工审查动作抽成 composable；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A73 前端 ReviewDetail 轮询机制 composable 抽取

已完成：
- 新增 `repoguard-frontend/src/features/review-detail/composables/useReviewDetailPolling.ts`，集中管理审查详情页自动刷新 timer、轮询启停、失败次数上限暂停和按当前间隔调度下一次状态刷新。
- `ReviewDetailPage.vue` 移除本地 `pollTimer`、`startPolling`、`stopPolling`、`syncPolling`、`cleanupPolling` 实现，改为从 `useReviewDetailPolling` 获取轮询控制函数。
- `repoguard-frontend/src/features/review-detail/index.ts` 继续作为 ReviewDetail feature 统一出口，导出新增的 `useReviewDetailPolling`。
- 保持审查详情 API、状态快照应用、失败文案、刷新按钮、终态详情加载、GitHub 评论预览/历史加载和生命周期清理语义不变，仅调整轮询机制的前端状态边界。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- ReviewDetail 页已开始从“展示组件化”推进到“状态机制 composable 化”，轮询 timer 与页面业务状态更新解耦，后续可继续拆加载、GitHub 评论发布、finding 反馈和人工审查动作。
- 下一步建议评估抽取 ReviewDetail 详情加载/终态附属数据加载 composable，或将 GitHub 评论预览/发布动作独立成 composable；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A74 前端 ReviewDetail GitHub 评论数据 composable 抽取

已完成：
- 新增 `repoguard-frontend/src/features/review-detail/composables/useReviewDetailGithubComments.ts`，集中管理 GitHub 评论预览、回写历史、发布结果、预览错误、历史错误及衍生的已发布数量、历史批次和回写检查状态。
- `ReviewDetailPage.vue` 移除本地 GitHub 评论预览/历史 ref、computed 和预览/历史加载函数，改为从 `useReviewDetailGithubComments` 获取状态、加载函数和清理函数。
- `repoguard-frontend/src/features/review-detail/index.ts` 继续作为 ReviewDetail feature 统一出口，导出新增的 `useReviewDetailGithubComments`。
- 保持审查详情 API、GitHub 评论发布确认、发布接口调用、finding 反馈后刷新预览、任务重试清理、终态预览/历史加载、展示组件 props 和样式类名不变，仅调整 GitHub 评论附属数据的前端状态边界。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- ReviewDetail 页中的轮询机制与 GitHub 评论附属数据已先后沉淀到 feature composable，页面继续向详情加载、动作编排和组件组合层收敛。
- 下一步建议评估抽取 GitHub 评论发布动作 composable，或继续拆 ReviewDetail 详情主加载/终态附属数据加载边界；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A75 前端 ReviewDetail GitHub 评论发布动作收敛

已完成：
- `useReviewDetailGithubComments` 新增 `publishingComments` 与 `publishGithubCommentsForTask`，集中承载 GitHub 评论发布 loading、发布接口调用、发布结果写入、成功/部分失败提示和异常提示。
- `ReviewDetailPage.vue` 移除本地 `publishingComments` 状态和 `publishGithubComments` API 直接依赖，确认弹窗通过后调用 composable 的发布动作，并继续在发布成功后刷新预览与回写历史。
- 保持 GitHub 评论发布前置门禁、确认弹窗文案、发布后预览/历史刷新、发布结果展示、错误提示文案、展示组件 props 和 API 契约不变，仅调整发布执行动作的前端状态边界。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- ReviewDetail 页的 GitHub 评论预览、历史、发布 loading、发布结果和发布执行动作已集中到同一 feature composable，页面侧保留确认、门禁和动作编排。
- 下一步建议继续拆 ReviewDetail 详情主加载/终态附属数据加载边界，或抽取 finding 反馈与人工审查提交动作 composable；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A76 前端 ReviewDetail Finding 反馈动作收敛

已完成：
- 新增 `repoguard-frontend/src/features/review-detail/composables/useReviewDetailFindingFeedback.ts`，集中承载 finding 反馈保存态、反馈弹窗、输入校验、反馈 API 调用、局部 finding 状态回填、发布结果清理和终态预览刷新。
- `ReviewDetailPage.vue` 移除本地 `feedbackSavingId`、`applyFindingFeedback`、`submitFindingFeedback` 和 `updateFindingFeedback` 直接依赖，改为从 `useReviewDetailFindingFeedback` 获取反馈保存态与提交动作。
- `repoguard-frontend/src/features/review-detail/index.ts` 继续作为 ReviewDetail feature 统一出口，导出新增的 `useReviewDetailFindingFeedback`。
- 保持 finding 反馈按钮、弹窗文案、必填规则、API 请求体、局部回填字段、终态预览刷新、GitHub 发布结果清理和展示组件 props 不变，仅调整反馈动作的前端状态边界。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- ReviewDetail 页的 GitHub 评论数据/发布动作与 finding 反馈动作均已沉淀到 feature composable，页面侧进一步收敛为任务详情加载、人工审查/重试动作和组件编排。
- 下一步建议继续抽取人工审查提交动作 composable，或拆 ReviewDetail 详情主加载/终态附属数据加载边界；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A77 前端 ReviewDetail 人工审查动作收敛

已完成：
- 新增 `repoguard-frontend/src/features/review-detail/composables/useReviewDetailHumanReview.ts`，集中承载人工审查提交态、人工审查弹窗、输入校验、提交 API 调用、任务人工审查字段回填、成功提示和异常提示。
- `ReviewDetailPage.vue` 移除本地 `submittingHumanReview`、`submitHumanReviewDecision` 和 `submitHumanReview` 直接依赖，改为从 `useReviewDetailHumanReview` 获取提交态与提交动作。
- `repoguard-frontend/src/features/review-detail/index.ts` 继续作为 ReviewDetail feature 统一出口，导出新增的 `useReviewDetailHumanReview`。
- 保持人工审查按钮、弹窗文案、必填规则、API 请求体、状态回填字段、提交后详情刷新、GitHub 发布结果重置和展示组件 props 不变，仅调整人工审查动作的前端状态边界。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- ReviewDetail 页的 GitHub 评论、finding 反馈和人工审查动作已完成 feature composable 收敛，页面侧主要剩余详情主加载、轮询状态快照、重试动作和展示组件编排。
- 下一步建议继续拆 ReviewDetail 详情主加载/终态附属数据加载边界，或抽取任务重试动作 composable；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A78 前端 ReviewDetail 任务重试动作收敛

已完成：
- 新增 `repoguard-frontend/src/features/review-detail/composables/useReviewDetailRetry.ts`，集中承载任务重试 loading、确认弹窗、重试 API 调用、成功提示、异常提示、GitHub 评论状态清理和轮询失败状态重置。
- `ReviewDetailPage.vue` 移除本地 `retryingTask`、`confirmRetryReview` 和 `retryReview` 直接依赖，改为从 `useReviewDetailRetry` 获取重试状态与重试动作。
- `repoguard-frontend/src/features/review-detail/index.ts` 继续作为 ReviewDetail feature 统一出口，导出新增的 `useReviewDetailRetry`。
- 保持失败任务重试门禁、确认弹窗文案、失败原因提示、API 调用、成功提示、GitHub 评论状态清理、轮询失败重置和重试后详情刷新不变，仅调整重试动作的前端状态边界。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- ReviewDetail 页的 GitHub 评论、finding 反馈、人工审查和任务重试动作均已沉淀到 feature composable，页面侧主要剩余详情主加载、轮询状态快照、状态/文案映射和展示组件编排。
- 下一步建议继续拆 ReviewDetail 详情主加载/终态附属数据加载边界，或将状态快照归并/状态文案映射迁入 feature 工具模块；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A79 前端 ReviewDetail 状态快照映射归并

已完成：
- 新增 `repoguard-frontend/src/features/review-detail/reviewDetailTaskMappers.ts`，集中承载审查详情数据默认值规范化、timeline 最新节点合并和轮询状态快照应用。
- `ReviewDetailPage.vue` 移除本地 `normalizeStatusFields`、`normalizeTimelineItem`、`mergeLatestTimeline` 和 `applyStatusSnapshot`，改为调用 `normalizeReviewTaskDetail` 与 `applyReviewStatusSnapshot`。
- `repoguard-frontend/src/features/review-detail/index.ts` 继续作为 ReviewDetail feature 统一出口，导出新增的状态映射函数。
- 保持详情 API 响应默认值、风险画像/PR 总评兜底文案、LLM token/cost 默认值、分片审查默认值、轮询状态字段回填和 timeline 合并语义不变，仅调整状态映射的前端边界。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- ReviewDetail 页的展示组件、动作 composable、轮询机制和状态映射已大部分沉淀到 `features/review-detail`，页面侧继续收敛为主加载、轮询编排、状态文案函数和组件组合。
- 下一步建议继续拆 ReviewDetail 详情主加载/终态附属数据加载边界，或将状态/文案映射进一步迁入 feature 工具模块；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A80 前端 ReviewDetail 展示文案映射归并

已完成：
- 新增 `repoguard-frontend/src/features/review-detail/reviewDetailDisplayMappers.ts`，集中承载 ReviewDetail 的 timeline、变更类型、分片原因、消费状态、GitHub 评论回写状态、人工审查状态和 finding 反馈状态等展示映射。
- `ReviewDetailPage.vue` 移除本地纯文案/样式映射函数，改为从 `features/review-detail` 统一出口导入；仅保留少量 computed 作为现有组件 props 的适配层。
- `repoguard-frontend/src/features/review-detail/index.ts` 导出新增展示映射，保持 feature 目录作为页面组件、composable 与工具函数的统一边界。
- 保持详情页 API、组件 props、文案内容、状态 class、评论 key 生成、人工审核提交动作文案和 GitHub 发布历史展示语义不变，仅调整展示映射归属。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- ReviewDetail 页进一步减少展示规则内联，页面主脚本更聚焦任务加载、轮询、刷新和动作编排。
- 下一步建议继续拆 ReviewDetail 详情主加载/终态附属数据加载边界，或评估将 LLM 状态文案和刷新状态文案迁入更小的 feature 工具模块；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A81 前端 ReviewDetail 详情加载 composable 抽取

已完成：
- 新增 `repoguard-frontend/src/features/review-detail/composables/useReviewDetailLoader.ts`，集中承载审查详情加载、轮询状态快照请求、刷新状态、错误状态和最后刷新时间。
- 详情加载 composable 复用 `normalizeReviewTaskDetail` 与 `applyReviewStatusSnapshot`，并在终态任务时统一加载 GitHub 评论预览和回写历史，非终态时清理附属数据。
- `ReviewDetailPage.vue` 不再直接依赖 `fetchReviewDetail`、`fetchReviewStatus`、`getErrorMessage` 和详情默认值归一化逻辑，仅保留页面路由、组件组合、动作确认和轮询桥接。
- 保持详情 API、轮询间隔、连续失败暂停阈值、手动刷新语义、终态附属数据加载、错误提示文案和 GitHub 评论回写刷新语义不变。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- ReviewDetail 的主加载和终态附属数据加载边界已从页面脚本抽出，页面进一步收敛为路由驱动的展示与动作编排。
- 下一步建议继续评估 LLM 状态文案/刷新状态文案是否迁入 feature 工具模块，或转向下一个高复杂度页面 `NotificationOpsPage.vue` 的状态与展示边界拆分；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A82 前端 ReviewDetail 剩余状态文案映射归并

已完成：
- `reviewDetailDisplayMappers.ts` 继续承载 ReviewDetail 剩余展示文案，新增人工审核回写阻断原因、刷新状态、状态原因、LLM 模型、耗时、Token、成本、解析状态文案和解析状态 class 映射。
- `ReviewDetailPage.vue` 移除上述本地文案分支，改为在 computed 中组装 mapper 入参，模板 props、组件结构和交互入口保持不变。
- `features/review-detail/index.ts` 导出新增映射函数，维持 feature 统一出口边界。
- 保持刷新提示、人工审核阻断提示、LLM 侧栏展示、状态原因提取、风险/状态 class 和所有已有中文文案不变。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- ReviewDetail 页面内的主要展示文案和状态 class 规则已基本归并到 feature 工具层，页面职责进一步收敛到数据流和组件编排。
- 下一步建议转向下一个高复杂度页面 `NotificationOpsPage.vue` 的状态与展示边界拆分，或先为 ReviewDetail feature 补充轻量边界说明；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A83 前端 NotificationOps 展示映射边界拆分

已完成：
- 新增 `repoguard-frontend/src/features/notification-ops/notificationOpsDisplayMappers.ts`，集中承载通知运维页的通知状态 class/文案、事件类型文案、投递次数文案、provider 文案、渠道文案/图标、失败事件判断和可重试判断。
- 新增 `repoguard-frontend/src/features/notification-ops/index.ts` 作为通知运维 feature 统一出口。
- `NotificationOpsPage.vue` 移除本地展示映射函数，改为从 feature 导入；页面继续保留通知设置、事件、投递记录和绑定操作的数据编排。
- 保持通知事件列表、投递记录、渠道绑定、测试发送、可重试按钮、失败/待重试指标口径、状态文案和渠道图标语义不变。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- NotificationOps 页的展示映射规则已从页面脚本抽出，页面职责开始从“状态/文案 + 数据加载 + 操作编排”向数据加载和操作编排收敛。
- 下一步建议继续拆 NotificationOps 的数据加载状态 composable，优先抽取通知事件/投递记录加载与重试动作；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A84 前端 NotificationOps 事件记录 composable 抽取

已完成：
- 新增 `repoguard-frontend/src/features/notification-ops/composables/useNotificationOpsRecords.ts`，集中承载通知事件列表、投递记录列表、分页筛选、加载状态、总数和事件重试状态。
- `NotificationOpsPage.vue` 移除本地 `loadNotificationEvents`、`loadNotificationDeliveries`、`refreshNotificationData` 和 `retryEvent` 实现，改为调用 feature composable 返回的状态与动作。
- `features/notification-ops/index.ts` 导出新增 composable，保持通知运维 feature 的统一访问入口。
- 保持通知事件/投递记录 API 请求参数、分页字段、错误提示、重试成功提示、重试后刷新事件/投递/绑定数据的语义不变。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- NotificationOps 页的事件和投递记录数据边界已从页面脚本抽出，页面继续收敛为系统设置、绑定弹窗和页面组合编排。
- 下一步建议继续拆 NotificationOps 的通知设置读取/保存边界，或将顶部指标计算迁入 feature 工具层；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A85 前端 NotificationOps 通知设置 composable 抽取

已完成：
- 新增 `repoguard-frontend/src/features/notification-ops/composables/useNotificationOpsSettings.ts`，集中承载通知设置表单、系统设置加载、通知设置保存和保存中防重复提交状态。
- `NotificationOpsPage.vue` 移除本地系统设置状态、通知设置表单初始化、设置加载和保存实现，改为调用 feature composable。
- `features/notification-ops/index.ts` 导出新增设置 composable，继续保持通知运维 feature 统一访问入口。
- 保持系统设置 API 请求体结构、通知表单默认值、email 兜底、保存成功/失败提示和无权限/重复保存拦截语义不变。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- NotificationOps 页的通知设置读取/保存边界已从页面脚本抽出，页面继续收敛为标签页布局、绑定弹窗和页面级组合。
- 下一步建议将顶部指标计算迁入 feature 工具层，或继续拆测试发送弹窗状态；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A86 前端 NotificationOps 顶部指标映射归并

已完成：
- `notificationOpsDisplayMappers.ts` 新增 `buildNotificationMetricItems`，集中承载顶部指标的数量计算、主题和图标映射。
- `NotificationOpsPage.vue` 移除本地失败事件、待重试事件和指标卡数组计算，改为传入事件、事件总数和绑定列表给 feature mapper。
- `features/notification-ops/index.ts` 导出新增指标构建函数，继续保持通知运维 feature 统一访问入口。
- 保持今日通知、失败、待重试、启用渠道四个指标的文案、图标、主题和统计口径不变。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- NotificationOps 的主要展示映射、记录数据流和设置数据流已沉淀到 feature 层，页面进一步聚焦布局组合、绑定弹窗和测试发送交互。
- 下一步建议继续拆测试发送弹窗状态，或评估是否将通知绑定 composable 迁入 `features/notification-ops` 统一边界；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A87 前端 NotificationOps 测试发送弹窗状态抽取

已完成：
- 新增 `repoguard-frontend/src/features/notification-ops/composables/useNotificationOpsTestDialog.ts`，集中承载测试发送弹窗显隐、默认选中启用绑定、当前测试绑定 ID 和发送测试后的关闭逻辑。
- `NotificationOpsPage.vue` 移除本地测试发送弹窗状态与动作实现，改为调用 feature composable。
- `features/notification-ops/index.ts` 导出新增测试弹窗 composable，继续保持通知运维 feature 统一访问入口。
- 保持启用渠道过滤、打开弹窗默认选择第一个启用绑定、无选择时不发送、测试成功/失败提示由绑定测试动作处理、测试后关闭弹窗的语义不变。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- NotificationOps 页的测试发送状态也已沉淀到 feature 层，页面进一步收敛为布局组合和绑定表单入口。
- 下一步建议评估是否将通知绑定 composable 从全局 `composables` 迁入 `features/notification-ops` 统一边界，或继续拆绑定弹窗展示组件；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A88 前端 NotificationOps 绑定 composable 归并

已完成：
- 将 `useNotificationBindings` 从全局 `repoguard-frontend/src/composables` 迁入 `repoguard-frontend/src/features/notification-ops/composables`。
- `features/notification-ops/index.ts` 导出 `useNotificationBindings`，`NotificationOpsPage.vue` 和 `NotificationBindingManager.vue` 改为从 feature 统一出口引用。
- 删除旧的全局 `useNotificationBindings.ts`，避免通知运维领域状态继续散落在全局 composables 目录。
- 保持渠道绑定列表加载、新增/编辑、保存、测试、启停、删除、表单默认值、成功/失败提示和权限拦截语义不变，仅调整模块归属。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- NotificationOps 的展示映射、记录数据流、通知设置、测试弹窗和绑定操作 composable 已统一沉淀到 feature 目录。
- 下一步建议评估是否拆出绑定弹窗展示组件，或转向其它高复杂度页面继续按同样的小步规则收敛；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A89 前端 NotificationOps 绑定弹窗组件化

已完成：
- 新增 `repoguard-frontend/src/features/notification-ops/components/NotificationBindingDialog.vue`，集中承载消息通知绑定新增/编辑弹窗表单。
- `NotificationOpsPage.vue` 和 `NotificationBindingManager.vue` 移除重复弹窗模板，改为复用 `NotificationBindingDialog`。
- `features/notification-ops/index.ts` 导出新增弹窗组件，保持通知运维 feature 统一访问入口。
- 保持绑定弹窗标题、字段、默认 v-model 绑定、取消关闭、保存按钮 loading、权限禁用和保存动作语义不变。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- NotificationOps 的绑定表单展示和绑定操作状态进一步归入 feature 层，页面与旧管理组件都复用同一份弹窗展示。
- 下一步建议评估是否将绑定表格也组件化，或转向其它高复杂度页面继续按小步规则收敛；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A90 前端 NotificationOps 绑定表格组件化

已完成：
- 新增 `repoguard-frontend/src/features/notification-ops/components/NotificationBindingTable.vue`，集中承载通知绑定表格列、状态展示、操作按钮和事件出口。
- `NotificationOpsPage.vue` 和 `NotificationBindingManager.vue` 移除重复绑定表格模板，改为复用 `NotificationBindingTable`。
- `features/notification-ops/index.ts` 导出新增绑定表格组件，保持通知运维 feature 统一访问入口。
- 保持绑定表格字段、平台文案、仓库展示、启停状态、最近检测/错误、编辑/测试/启停/删除按钮、loading 和权限禁用语义不变；仅通过组件参数保留两处原有样式差异。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- NotificationOps 的绑定列表展示与绑定弹窗展示都已沉淀到 feature 组件层，页面和旧管理组件进一步减少重复模板。
- 下一步建议转向其它高复杂度页面继续按小步规则收敛，或评估 NotificationOps 是否还需拆分通知设置面板组件；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A91 前端 NotificationOps 通知设置面板组件化

已完成：
- 新增 `repoguard-frontend/src/features/notification-ops/components/NotificationSettingsPanel.vue`，集中承载通知设置页左侧触发策略、默认通知范围和失败重试展示。
- `NotificationOpsPage.vue` 移除通知设置面板的大段模板和面板专属样式，改为传入通知设置表单并监听保存事件。
- `features/notification-ops/index.ts` 导出新增设置面板组件，保持通知运维 feature 统一访问入口。
- 保持 GitHub 评论通知、失败任务通知、高风险 PR 通知开关保存触发、默认通知范围控件、失败重试控件、重试间隔 chips 和原有文案/视觉样式不变。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- NotificationOps 页的主要面板、绑定表格、绑定弹窗、记录数据流和设置数据流都已沉淀到 feature 层，页面进一步收敛为标签页布局和跨模块编排。
- 下一步建议转向其它高复杂度页面继续按小步规则收敛，优先评估 `ReviewTasksPage.vue` 或 `IntegrationsPage.vue`；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A92 前端 ReviewTasks PR 选择弹窗组件化

已完成：
- 新增 `repoguard-frontend/src/features/review-tasks/components/ReviewTaskPullRequestDialog.vue`，集中承载创建审查任务时的 GitHub PR 选择弹窗、PR 表格、刷新入口和创建按钮。
- 新增 `repoguard-frontend/src/features/review-tasks/index.ts`，建立 ReviewTasks feature 统一出口。
- `ReviewTasksPage.vue` 移除内联 PR 选择弹窗模板，改为向 feature 组件传入加载状态、PR 列表、仓库文案、选中 PR 和创建可用性，并监听刷新、选择、创建事件。
- 保持 PR 预加载提示、刷新 PR、单选 PR、Commit 短 hash 展示、空态、取消关闭、创建按钮 loading/禁用和创建动作语义不变；页面仍负责 API、路由、权限和错误提示。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- ReviewTasks 页开始建立 feature 边界，创建任务弹窗的展示层已从页面下沉到 `features/review-tasks`。
- 下一步建议继续在 `ReviewTasksPage.vue` 内拆分任务列表展示或筛选/分页数据流；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A93 前端 ReviewTasks 任务表格组件化

已完成：
- 新增 `repoguard-frontend/src/features/review-tasks/components/ReviewTaskTable.vue`，集中承载审查任务表格列、失败原因提示、风险/状态展示、操作按钮和底部分页。
- 新增 `repoguard-frontend/src/features/review-tasks/reviewTaskDisplayMappers.ts`，沉淀任务来源文案/样式、任务可重试判断和重试提示文案。
- `ReviewTasksPage.vue` 移除内联任务表格和分页模板，改为传入任务列表、总数、分页状态、权限状态和重试中任务 ID，并监听查看、重试事件。
- `features/review-tasks/index.ts` 导出新增表格组件和 display mapper，保持 ReviewTasks feature 统一访问入口。
- 保持 PR 链接、仓库/Commit 展示、来源/状态/风险样式、失败原因 tooltip、空态、分页参数、查看跳转、重试按钮禁用/loading 和重试前置校验语义不变。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- ReviewTasks 页的弹窗展示、任务表格展示和表格 display mapper 已沉淀到 feature 层，页面进一步收敛为筛选、数据加载、路由与 API 编排。
- 下一步建议继续评估筛选栏组件化或分页/筛选数据流 composable；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A94 前端 ReviewTasks 筛选栏组件化

已完成：
- 新增 `repoguard-frontend/src/features/review-tasks/components/ReviewTaskFilterBar.vue`，集中承载审查任务仓库、状态、风险、来源、关键词筛选控件和刷新按钮。
- `ReviewTasksPage.vue` 移除内联筛选栏模板，改为向 feature 组件传入仓库列表、筛选值、关键词和 loading 状态，并监听刷新事件。
- `features/review-tasks/index.ts` 导出新增筛选栏组件，保持 ReviewTasks feature 统一访问入口。
- 保持筛选项文案和值、关键词输入 placeholder、搜索图标、刷新按钮样式/loading、clearable 行为、筛选 debounce、分页重置和刷新加载语义不变。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- ReviewTasks 页的筛选栏、任务表格和 PR 选择弹窗展示均已沉淀到 feature 层，页面进一步收敛为数据加载、权限、路由和 API 编排。
- 下一步建议评估是否将 ReviewTasks 的列表加载/筛选分页状态抽为 composable，或转向 `IntegrationsPage.vue` 继续拆高复杂度页面；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A95 前端 ReviewTasks 列表数据流 composable 化

已完成：
- 新增 `repoguard-frontend/src/features/review-tasks/composables/useReviewTasksList.ts`，集中承载审查任务列表 loading、错误信息、任务数据、仓库列表、筛选条件、分页状态、筛选 debounce、请求序号防乱序和汇总指标计算。
- `ReviewTasksPage.vue` 移除本地列表加载、仓库加载、筛选监听、分页监听、筛选刷新和指标计算实现，改为调用 `useReviewTasksList`。
- `features/review-tasks/index.ts` 导出新增列表 composable，保持 ReviewTasks feature 统一访问入口。
- 保持列表 API 参数、仓库加载口径、筛选 debounce 350ms、当前页重置、分页触发加载、加载失败清空列表、错误文案、汇总指标口径和手动刷新语义不变；页面仍负责重试确认、PR 创建弹窗和路由跳转。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- ReviewTasks 页的展示组件和列表数据流均已沉淀到 feature 层，页面进一步收敛为权限、路由、重试动作和 PR 创建动作编排。
- 下一步建议评估是否继续抽 PR 选择弹窗数据流，或转向 `IntegrationsPage.vue` 继续拆高复杂度页面；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A96 前端 ReviewTasks PR 选择数据流 composable 化

已完成：
- 新增 `repoguard-frontend/src/features/review-tasks/composables/useReviewTaskPullRequestPicker.ts`，集中承载 GitHub PR 选择弹窗的数据加载、loading、错误信息、仓库信息、选中 PR、默认选中、刷新、选择和请求序号防乱序。
- `ReviewTasksPage.vue` 移除本地 PR 列表加载、仓库文案、默认选中、刷新和选择实现，改为调用 `useReviewTaskPullRequestPicker`。
- `features/review-tasks/index.ts` 导出新增 PR picker composable 和 `resolvePullRequestHeadSha`，保持 ReviewTasks feature 统一访问入口。
- 保持首次进入预加载但不默认选择、打开弹窗时默认选择首个 PR、未加载时触发加载、刷新时清空选中、加载失败清空列表、错误文案、仓库文案和创建审查任务参数语义不变；页面仍负责创建提交、成功提示和路由跳转。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- ReviewTasks 页的列表数据流、PR 选择数据流和主要展示组件均已下沉到 feature 层，页面进一步收敛为权限、路由、重试确认和创建审查任务编排。
- 下一步建议评估是否抽取 ReviewTasks 的创建审查任务动作，或转向 `IntegrationsPage.vue` 继续拆高复杂度页面；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A97 前端 ReviewTasks 创建任务动作 composable 化

已完成：
- 新增 `repoguard-frontend/src/features/review-tasks/composables/useReviewTaskCreation.ts`，集中承载从选中 GitHub PR 创建审查任务的 loading、权限拦截、有效 PR 校验、API 提交、成功/复用提示和失败提示。
- `ReviewTasksPage.vue` 移除本地创建审查任务提交实现，改为调用 `useReviewTaskCreation`，页面仅保留创建成功后的关闭弹窗和路由跳转回调。
- `features/review-tasks/index.ts` 导出新增创建动作 composable，保持 ReviewTasks feature 统一访问入口。
- 保持未授权直接返回、未选择有效 PR 警告、提交参数、创建中 loading、已有任务提示、创建成功提示、失败提示、创建后关闭弹窗和跳转详情页语义不变。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- ReviewTasks 页的展示层、列表数据流、PR 选择数据流和创建动作都已沉淀到 feature 层，页面剩余职责主要是权限入口、路由回调、重试确认和模块编排。
- 下一步建议评估是否抽取重试动作，或转向 `IntegrationsPage.vue` 继续拆高复杂度页面；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A98 前端 ReviewTasks 重试动作 composable 化

已完成：
- 新增 `repoguard-frontend/src/features/review-tasks/composables/useReviewTaskRetry.ts`，集中承载审查任务重试的权限拦截、失败任务判断、确认弹窗、重试中任务 ID、API 调用、成功提示、失败提示和完成后回调。
- `ReviewTasksPage.vue` 移除本地重试确认和重试 API 实现，改为调用 `useReviewTaskRetry`，页面仅传入重试成功后的列表刷新回调。
- `features/review-tasks/index.ts` 导出新增重试 composable，保持 ReviewTasks feature 统一访问入口。
- 保持仅失败任务可重试、已有重试中任务时阻止重复提交、确认弹窗文案、失败原因拼接、成功提示、失败提示、重试按钮 loading 和成功后刷新列表语义不变。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- ReviewTasks 页的展示、列表数据流、PR 选择、创建动作和重试动作均已沉淀到 feature 层，页面基本收敛为页面级布局、权限入口和路由回调。
- 下一步建议转向 `IntegrationsPage.vue` 继续拆高复杂度页面；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A99 前端 Integrations 默认配置元数据下沉

已完成：
- 新增 `repoguard-frontend/src/features/integrations/integrationDefaults.ts`，集中承载集成页默认卡片、默认字段、服务图标映射、Provider 映射、集成 ID 类型和默认卡片克隆函数。
- 新增 `repoguard-frontend/src/features/integrations/index.ts`，建立 Integrations feature 统一出口。
- `IntegrationsPage.vue` 移除大段默认集成卡片和静态映射定义，改为从 `features/integrations` 引用。
- 保持默认卡片文案、字段 label/value/type/placeholder/options、图标、Provider 正反向映射、表单初始化和本地默认值语义不变；保存、加载、连接检测和配置应用流程不变。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- Integrations 页开始建立 feature 边界，静态元数据已先从页面下沉，页面体积和认知负担下降。
- 下一步建议继续拆分 Integrations 的配置应用映射或连接检测动作；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A100 前端 Integrations 配置映射下沉

已完成：
- 新增 `repoguard-frontend/src/features/integrations/integrationConfigMappers.ts`，集中承载 GitHub、MySQL、RabbitMQ、Spring AI 配置应用补丁、连接检测结果补丁、服务字段映射和诊断文案/状态映射。
- `IntegrationsPage.vue` 移除本地配置应用大段映射逻辑，改为调用 feature mapper 生成补丁，再由页面统一写入当前卡片和表单状态。
- `features/integrations/index.ts` 导出新增配置映射函数，保持 Integrations feature 统一访问入口。
- 保持 GitHub/服务/LLM 状态文案、字段 label/value/type/placeholder/options、诊断项、检测结果展示、表单回填和保存后应用配置语义不变；请求加载、保存、连接检测动作仍由页面原流程控制。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- Integrations 页的静态元数据和配置展示映射已进入 feature 层，页面进一步收敛为 API 编排、表单取值和状态写入。
- 下一步建议继续拆分连接检测动作或配置加载/保存数据流；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A101 前端 Integrations Payload 组装下沉

已完成：
- 新增 `repoguard-frontend/src/features/integrations/integrationPayloadBuilders.ts`，集中承载 GitHub、MySQL、RabbitMQ、Spring AI 表单值到 API payload 的组装逻辑。
- `IntegrationsPage.vue` 移除本地字段取值、数字字段解析、Provider 反向映射和四类 payload 细节实现，改为调用 feature payload builder。
- `features/integrations/index.ts` 导出新增 payload builder 和 `IntegrationFormState` 类型，保持 Integrations feature 统一访问入口。
- 保持 GitHub 默认 API Base URL、MySQL/RabbitMQ 字段映射、Spring AI provider/model/baseUrl/apiKey/chunk/价格字段、数字 fallback 和 review policy 既有配置继承语义不变；保存和连接检测仍复用同一 payload 来源。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- Integrations 页的默认元数据、配置展示映射和请求 payload 组装都已进入 feature 层，页面继续收敛为 API 编排、权限和状态写入。
- 下一步建议继续拆分连接检测动作或配置加载/保存数据流；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A102 前端 Integrations 连接检测动作 composable 化

已完成：
- 新增 `repoguard-frontend/src/features/integrations/composables/useIntegrationConnectionTest.ts`，集中承载连接检测的可用性检查、防重复提交、测试中状态、成功/失败提示、异常兜底结果和完成态清理。
- `IntegrationsPage.vue` 移除本地连接检测流程实现，改为调用 `useIntegrationConnectionTest`；页面仅提供每个集成的测试 API、当前卡片存在性检查和检测结果写回函数。
- `features/integrations/index.ts` 导出新增连接检测 composable，保持 Integrations feature 统一访问入口。
- 保持不可测试警告、测试中重复点击拦截、成功/失败提示、异常错误文案、异常时 failed 检测结果、检测时间和检测结果补丁写回语义不变。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- Integrations 页的默认元数据、配置展示映射、payload 组装和连接检测动作都已进入 feature 层，页面继续收敛为加载/保存编排和状态写入。
- 下一步建议继续拆分配置加载/保存数据流；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A103 前端 Integrations 配置加载保存 composable 化

已完成：
- 新增 `repoguard-frontend/src/features/integrations/composables/useIntegrationConfigPersistence.ts`，集中承载集成配置加载、保存、loading/saving、四类配置快照、加载失败提示、保存成功/失败提示和加载/保存后的统一应用配置流程。
- `IntegrationsPage.vue` 移除本地加载/保存流程实现，改为向 composable 注入请求函数、payload getter、权限状态和配置应用回调。
- `features/integrations/index.ts` 导出新增配置持久化 composable，保持 Integrations feature 统一访问入口。
- 保持加载四类配置的并发顺序、保存四类配置的并发顺序、权限/重复保存拦截、加载失败使用本地默认值、保存成功提示、保存失败提示和保存后回填最新配置语义不变。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- Integrations 页的默认元数据、配置映射、payload 组装、连接检测和加载/保存数据流均已沉淀到 feature 层，页面进一步收敛为布局、状态写入和 API 注入。
- 下一步建议评估是否继续抽 `IntegrationCard` 到 feature 目录或梳理表单状态写入 composable；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A104 前端 Integrations 卡片组件归并

已完成：
- 将 `repoguard-frontend/src/components/IntegrationCard.vue` 迁移到 `repoguard-frontend/src/features/integrations/components/IntegrationCard.vue`。
- `features/integrations/index.ts` 导出 `IntegrationCard`，`IntegrationsPage.vue` 改为从 Integrations feature 统一入口引用。
- 保持卡片模板、字段绑定、密码可见性切换、状态展示、诊断展示、测试连接事件和样式类名不变；仅调整组件归属边界。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- Integrations 页的卡片 UI、默认元数据、配置映射、payload 组装、连接检测和加载/保存数据流都已进入 feature 层，页面进一步收敛为页面级布局和状态编排。
- 下一步建议梳理表单状态写入 composable 或转向其它高复杂度页面；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A105 前端 Integrations 表单状态 composable 化

已完成：
- 新增 `repoguard-frontend/src/features/integrations/composables/useIntegrationFormState.ts`，集中承载 Integrations 页面默认卡片克隆、表单状态初始化、密钥可见性状态和集成卡片 patch 写回逻辑。
- `IntegrationsPage.vue` 移除本地 `integrationItems`、`formState`、`visibleSecrets`、`formValues` 和 `applyIntegrationPatch` 实现，改为调用 `useIntegrationFormState`，页面继续收敛为 API 注入、连接检测/加载保存编排和生命周期触发。
- `features/integrations/index.ts` 导出新增表单状态 composable，保持 Integrations feature 统一访问入口。
- 保持默认表单字段、配置加载回填、连接检测结果回写、保存 payload 来源、密钥可见性和卡片展示语义不变；仅调整表单状态写入职责边界。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- Integrations 页的卡片 UI、默认元数据、配置映射、payload 组装、连接检测、加载/保存数据流和表单状态写入均已进入 feature 层，页面进一步收敛为页面级布局、API 注入和少量动作编排。
- 下一步建议评估是否继续拆分 Integrations 的连接测试 action 映射，或转向 `main.css`/其它高复杂度页面继续按小步规则收敛；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A106 前端 Integrations 连接测试 Action 映射下沉

已完成：
- 新增 `repoguard-frontend/src/features/integrations/integrationConnectionTestActions.ts`，集中承载 GitHub、MySQL、RabbitMQ 和 Spring AI 四类连接测试 API 与 payload getter 的映射关系。
- `IntegrationsPage.vue` 移除本地 `testActions` 字面量，改为调用 `buildIntegrationConnectionTestActions` 并注入现有测试 API 与 payload getter。
- `features/integrations/index.ts` 导出新增连接测试 action builder，保持 Integrations feature 统一访问入口。
- 保持连接测试入口、测试中防重、失败兜底、检测结果写回、成功/失败提示和所有 payload 来源不变；仅调整 action 映射职责边界。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- Integrations 页的卡片 UI、默认元数据、配置映射、payload 组装、连接检测流程、连接测试 action 映射、加载/保存数据流和表单状态写入均已进入 feature 层，页面继续收敛为页面级布局、API 注入和生命周期编排。
- 下一步建议评估是否继续抽取 Integrations 的配置应用回调组合，或转向 `main.css`/其它高复杂度页面继续按小步规则收敛；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A107 前端 Integrations 配置应用回调组合下沉

已完成：
- 新增 `repoguard-frontend/src/features/integrations/integrationConfigApplyActions.ts`，集中承载 GitHub、MySQL/RabbitMQ、Spring AI 配置回填和连接检测结果写回对应的 patch 构建与集成 ID 映射。
- `IntegrationsPage.vue` 移除本地 `applyGithubConfig`、`applyServiceConfig`、`applyReviewPolicyConfig`、`applyConnectionTestResult` 实现，改为调用 `buildIntegrationConfigApplyActions` 并注入底层 `applyIntegrationPatch`。
- `features/integrations/index.ts` 导出新增配置应用 action builder，保持 Integrations feature 统一访问入口。
- 保持配置加载回填、保存后回填、连接检测结果写回、诊断展示、表单状态同步和所有 API 契约不变；仅调整配置应用回调的职责边界。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- Integrations 页的卡片 UI、默认元数据、配置映射、payload 组装、连接检测流程、连接测试 action 映射、配置应用回调、加载/保存数据流和表单状态写入均已进入 feature 层，页面基本收敛为页面布局、API 注入、payload getter 和生命周期编排。
- 下一步建议转向 `main.css` 按页面/feature 拆分样式，或继续评估 Integrations payload getter 是否值得组合下沉；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A108 前端 Integrations 样式 feature 化

已完成：
- 新增 `repoguard-frontend/src/features/integrations/integrations.css`，集中承载 Integrations 页面、卡片、配置表单、连接状态、诊断信息和响应式布局样式。
- `repoguard-frontend/src/main.ts` 在全局样式后导入 Integrations feature 样式，保持样式加载顺序稳定。
- `repoguard-frontend/src/styles/main.css` 移除 Integrations 相关选择器和响应式规则，`main.css` 从约 2365 行降至约 2134 行。
- 保持 Integrations 选择器、视觉参数、响应式断点、连接状态颜色、诊断展示和页面结构不变；仅调整样式归属边界。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- Integrations feature 已完成页面逻辑、组件、数据流、配置映射、action 映射和样式的成体系下沉，页面与全局样式职责继续收敛。
- 下一步建议继续按同样方式拆分 `main.css` 中的 NotificationOps 或系统设置相关样式，仍保持选择器、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A109 前端 NotificationOps 全局样式 feature 化

已完成：
- 新增 `repoguard-frontend/src/features/notification-ops/notificationOps.css`，集中承载通知绑定管理、通知运维筛选栏、页脚和响应式布局相关全局样式。
- `repoguard-frontend/src/main.ts` 在 Integrations feature 样式后导入 NotificationOps feature 样式，保持全局样式加载顺序稳定。
- `repoguard-frontend/src/styles/main.css` 移除 NotificationOps/通知绑定相关全局选择器和响应式规则，`main.css` 从约 2134 行降至约 2068 行。
- 保持 NotificationOps 页面 scoped 样式、绑定管理组件结构、选择器、视觉参数和响应式断点不变；仅调整全局样式归属边界。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- NotificationOps feature 的展示组件、数据流、设置流、绑定操作和全局样式边界进一步统一，`main.css` 继续向全局基础布局与通用样式收敛。
- 下一步建议继续拆分 `main.css` 中的系统设置或消息队列健康页相关样式；仍保持选择器、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A110 前端 SystemSettings 样式 feature 化

已完成：
- 新增 `repoguard-frontend/src/features/system-settings/systemSettings.css`，集中承载系统设置页布局、设置卡片、操作按钮、通知配置交接块、提示条、表单间距、底部预览/日志网格和响应式规则。
- `repoguard-frontend/src/main.ts` 在 NotificationOps feature 样式后导入 SystemSettings feature 样式，保持样式加载顺序稳定。
- `repoguard-frontend/src/styles/main.css` 移除系统设置专属选择器和响应式规则，`main.css` 从约 2068 行降至约 2014 行。
- 保留共享的 `switch-row` 全局样式，因为它同时服务 SystemSettings 和 NotificationSettingsPanel；保持系统设置页面结构、选择器、视觉参数和响应式断点不变。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- SystemSettings 页面样式已从全局样式中分离，`main.css` 进一步收敛为基础布局、共享控件和仍待拆分的页面样式。
- 下一步建议继续拆分 `main.css` 中的消息队列健康页或规则配置页相关样式；仍保持选择器、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A111 前端 MessageQueueHealth 样式 feature 化

已完成：
- 新增 `repoguard-frontend/src/features/message-queue/messageQueue.css`，集中承载消息队列健康页布局、RabbitMQ 配置提示、拓扑链路、DLQ 信息、MQ 筛选栏和响应式规则。
- `repoguard-frontend/src/main.ts` 在 Integrations feature 样式后导入 MessageQueue feature 样式，保持样式加载顺序稳定。
- `repoguard-frontend/src/styles/main.css` 移除 `.message-queue-page` 与 `.mq-*` 专属选择器和响应式规则，`main.css` 从约 2014 行降至约 1870 行。
- 保留共享的 `health-list`、`health-item`、`health-footer` 全局样式，因为它们同时服务 Dashboard 和 MessageQueueHealth；保持页面结构、选择器、视觉参数和响应式断点不变。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- MessageQueueHealth 页面专属样式已从全局样式中分离，`main.css` 继续收敛为基础布局、共享控件和少量未拆分页面样式。
- 下一步建议继续拆分 `main.css` 中的规则配置页或用户管理页相关样式；仍保持选择器、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A112 前端 RuleConfig 样式 feature 化

已完成：
- 新增 `repoguard-frontend/src/features/rule-config/ruleConfig.css`，集中承载规则配置页布局、规则表格面板、筛选栏、规则说明卡、规则说明条目、规则表单网格和响应式规则。
- `repoguard-frontend/src/main.ts` 在 NotificationOps feature 样式后导入 RuleConfig feature 样式，保持样式加载顺序稳定。
- `repoguard-frontend/src/styles/main.css` 移除 RuleConfig 专属选择器和响应式规则，`main.css` 从约 1870 行降至约 1820 行。
- 保留 Dashboard 使用的 `rule-legend` 全局样式，避免把图表图例样式误迁到规则配置 feature；保持规则配置页结构、选择器、视觉参数和响应式断点不变。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- RuleConfig 页面专属样式已从全局样式中分离，`main.css` 继续收敛为基础布局、共享控件和仍待拆分的页面样式。
- 下一步建议继续拆分 `main.css` 中的用户管理页或审查任务列表相关样式；仍保持选择器、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A113 前端 UserManagement 样式 feature 化

已完成：
- 新增 `repoguard-frontend/src/features/user-management/userManagement.css`，集中承载用户管理页布局、用户面板、审计面板标题、筛选栏、用户账号单元格、危险计数和响应式规则。
- `repoguard-frontend/src/main.ts` 导入 UserManagement feature 样式，保持样式加载顺序稳定。
- `repoguard-frontend/src/styles/main.css` 移除 UserManagement 专属选择器和响应式规则，`main.css` 从约 1820 行降至约 1753 行。
- 保留共享表格、任务表格、搜索输入和表格操作样式在全局，避免误迁 ReviewTasks、RuleConfig、MQ 共用样式；保持用户管理页结构、选择器、视觉参数和响应式断点不变。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- UserManagement 页面专属样式已从全局样式中分离，`main.css` 继续收敛为基础布局、共享控件和少量未拆分页面样式。
- 下一步建议继续拆分 `main.css` 中的审查任务列表或 ReviewDetail 相关样式；仍保持选择器、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A114 前端 ReviewTasks 样式 feature 化

已完成：
- 新增 `repoguard-frontend/src/features/review-tasks/reviewTasks.css`，集中承载审查任务页容器、PR 单元格、PR 链接、PR 选择弹窗元信息、PR 选项单元格和复制图标样式。
- `repoguard-frontend/src/main.ts` 导入 ReviewTasks feature 样式，保持样式加载顺序稳定。
- `repoguard-frontend/src/styles/main.css` 移除 ReviewTasks 独有选择器，`main.css` 从约 1753 行降至约 1708 行。
- 保留共享的 `task-panel`、`task-table`、`filter-bar`、`table-footer`、`repo-cell`、`failure-summary-cell` 和 `table-actions` 在全局样式中，避免误迁用户管理、规则配置、消息队列健康和通知运维页面共用样式；保持审查任务页结构、选择器、视觉参数和响应式断点不变。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- ReviewTasks 页面独有样式已从全局样式中分离，`main.css` 继续收敛为基础布局、共享控件和少量未拆分页面样式。
- 下一步建议继续拆分 `main.css` 中的 ReviewDetail 页面样式；仍保持选择器、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A115 前端 ReviewDetail 样式 feature 化

已完成：
- 新增 `repoguard-frontend/src/features/review-detail/reviewDetail.css`，集中承载 ReviewDetail 页容器、头部、失败提示、KPI、详情布局、摘要风险画像、finding 列表、GitHub 评论预览/发布历史、人工审核、变更文件、时间线和响应式规则。
- `repoguard-frontend/src/main.ts` 导入 ReviewDetail feature 样式，保持样式加载顺序稳定。
- `repoguard-frontend/src/styles/main.css` 移除 ReviewDetail 独有选择器和响应式规则，`main.css` 从约 1708 行降至约 843 行。
- 保留共享的 `card-title-row` 和 `count-badge` 在全局样式中，避免误迁 MessageQueueHealth 共用标题行和计数徽标样式；保持 ReviewDetail 页面结构、选择器、视觉参数和响应式断点不变。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- ReviewDetail 页面独有样式已从全局样式中分离，`main.css` 基本收敛为基础布局、共享控件和跨页面复用样式。
- 下一步建议继续梳理 `main.css` 中剩余共享样式的归属边界，或转向 `src/types/index.ts` 按领域拆分；仍保持 API 契约、视觉参数和构建门禁不变。

### 2026-06-19 架构优化 A116 前端通知类型领域拆分

已完成：
- 新增 `repoguard-frontend/src/types/notification.ts`，集中承载通知绑定、通知事件、通知投递、通知中心、通知项和通知级别类型。
- `repoguard-frontend/src/types/index.ts` 通过 `export * from "./notification"` 继续统一导出通知类型，保持现有 `@/types` 调用方不变。
- `repoguard-frontend/src/types/index.ts` 移除通知领域类型定义，文件从约 720 行降至约 638 行。
- 保留 `NotificationSettings` 在系统设置领域定义中，避免把系统设置页的通知开关配置误迁到通知运维事件/绑定领域；保持 API DTO 字段、类型名称和导入路径不变。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- 前端类型定义开始按领域拆分，通知运维和通知中心相关 DTO 已从集中式 `index.ts` 分离。
- 下一步建议继续按同样方式拆分 `src/types/index.ts` 中的 MessageQueue 或 Dashboard 类型；仍保持统一导出、API 契约和构建门禁不变。

### 2026-06-19 架构优化 A117 前端 MessageQueue 类型领域拆分

已完成：
- 新增 `repoguard-frontend/src/types/messageQueue.ts`，集中承载 RabbitMQ 当前配置、拓扑、消息队列指标、补偿状态、异常任务、健康概览和重新入队响应类型。
- 新增 `repoguard-frontend/src/types/shared.ts`，承载 MessageQueue、Dashboard 和通用指标共同使用的 `MetricColor` 基础类型，避免领域类型文件反向依赖统一出口。
- `repoguard-frontend/src/types/index.ts` 通过 barrel export 继续统一导出 MessageQueue 和共享类型，保持现有 `@/types` 调用方不变。
- `repoguard-frontend/src/types/index.ts` 移除 MessageQueue 领域类型和本地 `MetricColor` 定义，文件从约 720 行降至约 642 行；保持 API DTO 字段、类型名称和导入路径不变。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过。

阶段结论：
- MessageQueue 类型已形成独立领域边界，共享展示色类型也有了无业务耦合的基础类型归属。
- 下一步建议继续拆分 `src/types/index.ts` 中的 Dashboard 类型；仍保持统一导出、API 契约和构建门禁不变。

### 2026-06-19 架构优化 A118 前端 Dashboard 类型领域拆分

已完成：
- 新增 `repoguard-frontend/src/types/dashboard.ts`，集中承载 Dashboard 指标、趋势点、图表切片、高风险审查、失败规则、系统健康和 LLM 质量统计类型。
- 将跨审查、Dashboard 和规则配置领域使用的 `RiskLevel` 下沉到 `repoguard-frontend/src/types/shared.ts`，避免 Dashboard 领域类型反向依赖统一出口。
- `repoguard-frontend/src/types/index.ts` 通过 barrel export 继续统一导出 Dashboard 和共享类型，保持现有 `@/types` 调用方不变。
- 保留同时被规则配置页使用的 `SimpleMetric` 在统一类型入口中；`types/index.ts` 从约 642 行降至约 557 行，保持 API DTO 字段、类型名称、联合类型和导入路径不变。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过；仅有 `@vueuse/core` 依赖中的既有 Rollup 注释告警。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- Dashboard 类型已形成独立领域边界，跨领域风险等级归入无业务耦合的共享基础类型。
- 下一步建议继续拆分 `src/types/index.ts` 中的 Review 或配置领域类型；仍保持统一导出、API 契约和构建门禁不变。

### 2026-06-19 架构优化 A119 前端 Review 类型领域拆分

已完成：
- 新增 `repoguard-frontend/src/types/review.ts`，集中承载审查任务、详情、状态、人工审核、Finding 反馈、GitHub 评论预览/发布历史、风险画像、LLM 状态、重试和 PR 选择器类型。
- 将同时被 Review API 和通知配置 API 使用的通用分页响应 `PageResponse<T>` 下沉到 `repoguard-frontend/src/types/shared.ts`，避免错误归入单一业务领域。
- `repoguard-frontend/src/types/index.ts` 通过 `export * from "./review"` 继续统一导出 Review 类型，保持现有 `@/types` 调用方不变。
- `types/index.ts` 从约 557 行降至约 202 行；保持 API DTO 字段、字段顺序、类型名称、联合类型和导入路径不变。

验证记录：
- Review 类型及 `PageResponse<T>` 定义唯一性检查通过。
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过；仅有 `@vueuse/core` 依赖中的既有 Rollup 注释告警。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- Review 任务列表、详情及评论回写相关 DTO 已形成完整领域边界，统一类型入口进一步收敛为兼容导出和少量待拆配置类型。
- 下一步建议继续拆分 `src/types/index.ts` 中的规则配置、系统设置或集成配置类型；仍保持统一导出、API 契约和构建门禁不变。

### 2026-06-19 架构优化 A120 前端规则配置类型领域拆分

已完成：
- 新增 `repoguard-frontend/src/types/ruleConfig.ts`，集中承载规则状态、规则指标、规则列表响应、规则配置和状态更新请求类型。
- `ruleConfig.ts` 直接依赖 `types/shared.ts` 中的 `MetricColor` 和 `RiskLevel` 基础类型，避免领域文件反向依赖统一出口。
- `repoguard-frontend/src/types/index.ts` 通过 `export * from "./ruleConfig"` 继续统一导出规则配置类型，保持现有 `@/types` 调用方不变。
- `types/index.ts` 从约 202 行降至约 154 行，并移除对共享基础类型的直接导入；保持 API DTO 字段、类型名称、联合类型和导入路径不变。

验证记录：
- 规则配置类型定义唯一性检查通过。
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过；仅有 `@vueuse/core` 依赖中的既有 Rollup 注释告警。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- 规则配置页面及配置 API 使用的类型已形成独立领域边界，统一类型入口仅剩系统设置和集成配置两组待拆类型。
- 下一步建议继续拆分 `src/types/index.ts` 中的系统设置类型；仍保持统一导出、API 契约和构建门禁不变。

### 2026-06-19 架构优化 A121 前端系统设置类型领域拆分

已完成：
- 新增 `repoguard-frontend/src/types/systemSettings.ts`，集中承载基础设置、审查策略设置、通知开关、安全设置、操作日志和系统设置请求类型。
- 保留 `NotificationSettings` 在系统设置领域；通知运维页面对该类型的复用表达的是系统级通知开关，不与通知事件、绑定和投递 DTO 混合。
- `repoguard-frontend/src/types/index.ts` 通过 `export * from "./systemSettings"` 继续统一导出系统设置类型，保持现有 `@/types` 调用方不变。
- `types/index.ts` 从约 154 行降至约 109 行；保持 API DTO 字段、类型名称、派生请求类型和导入路径不变。

验证记录：
- 系统设置类型定义唯一性检查通过。
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过；仅有 `@vueuse/core` 依赖中的既有 Rollup 注释告警。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- 系统设置页面、通知运维复用及配置 API 使用的设置类型已形成清晰领域边界。
- 下一步建议继续拆分 `src/types/index.ts` 中最后剩余的集成配置类型，使统一入口收敛为纯 barrel export；仍保持 API 契约和构建门禁不变。

### 2026-06-19 架构优化 A122 前端集成配置类型领域拆分

已完成：
- 新增 `repoguard-frontend/src/types/integration.ts`，集中承载集成卡片展示模型、GitHub/MySQL/RabbitMQ 配置请求、审查策略配置和连接测试结果类型。
- 保留 `ConnectionTestResult` 在集成配置领域；通知绑定测试对该类型的复用表达的是跨提供方连接诊断结果，不与通知事件和投递 DTO 混合。
- `repoguard-frontend/src/types/index.ts` 通过 `export * from "./integration"` 继续统一导出集成配置类型，保持现有 `@/types` 调用方不变。
- `types/index.ts` 从约 109 行降至 8 行，现已收敛为纯 barrel export；保持 API DTO 字段、类型名称、联合类型和导入路径不变。

验证记录：
- 集成配置类型定义唯一性及 `types/index.ts` 无本地类型声明检查通过。
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过；仅有 `@vueuse/core` 依赖中的既有 Rollup 注释告警。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- 前端集中式类型文件已按 Dashboard、Integration、MessageQueue、Notification、Review、RuleConfig、SystemSettings 和 Shared 领域完成拆分，统一入口仅承担兼容导出职责。
- 下一步建议重新审计优化方案中的剩余高价值边界，优先选择仍存在职责聚合且可独立验证的模块；不继续进行无收益的细粒度拆分。

### 2026-06-19 架构优化 A123 前端 MessageQueueHealth 状态 composable 拆分

已完成：
- 新增 `repoguard-frontend/src/features/message-queue/composables/useMessageQueueHealth.ts`，集中承载健康数据加载、错误状态、异常任务筛选、仓库选项、自动刷新 timer 和重新入队动作。
- 自动刷新生命周期随 composable 所在组件挂载/卸载统一启动和清理，保留原 30 秒刷新间隔、手动刷新和关闭开关后的 timer 清理语义。
- 重新入队继续保留原状态门禁、确认弹窗、成功/警告反馈、错误提示和完成后重新加载健康数据的行为。
- `MessageQueueHealthPage.vue` 仅保留路由跳转、图标映射、展示文案和模板编排，文件从约 425 行降至约 345 行；API、模板结构、选择器和视觉参数不变。

验证记录：
- `node node_modules/vue-tsc/bin/vue-tsc.js -b` 前端类型检查通过。
- `node node_modules/vite/bin/vite.js build` 前端生产构建通过；共转换 3290 个模块，仅有 `@vueuse/core` 依赖中的既有 Rollup 注释告警。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- MessageQueueHealth 页面已从请求、筛选、timer 和命令状态混合编排推进为“页面展示编排 + feature 状态 composable”边界。
- 下一步建议评估后端 `ReviewTaskQueryServiceImpl` 的详情查询与 DTO 组装职责，优先抽取可独立测试的详情数据加载/组装边界；仍保持 Controller/API 契约和测试门禁不变。

### 2026-06-19 架构优化 A124 后端 ReviewTaskDetailAssembler 详情组装边界抽取

已完成：
- 新增 `repoguard-backend/src/main/java/com/repoguard/agent/review/ReviewTaskDetailAssembler.java`，集中承载 Review 详情 DTO 组装、风险画像/PR 总评构建、LLM 状态、分片审查信息、RabbitMQ 状态和人工审核字段派生。
- `ReviewTaskQueryServiceImpl#getReviewDetail` 保留详情数据加载和查询编排职责，将详情响应对象组装委托给 `ReviewTaskDetailAssembler`，Controller/API 响应结构不变。
- 移除 `ReviewTaskQueryServiceImpl` 内部详情组装、分片摘要解析和详情风险等级派生等私有方法，降低查询服务对展示 DTO 派生规则的直接承担。
- 新增 `ReviewTaskDetailAssemblerTest`，覆盖风险等级派生、LLM 字段归一化、分片审查摘要解析、RabbitMQ 投递/重试计数、人审字段透传和非分片摘要禁用场景。

验证记录：
- `mvn "-Dtest=ReviewTaskDetailAssemblerTest,ReviewTaskQueryServiceImplTest,ReviewServiceImplTest,ReviewControllerTest" test` 通过；当前仓库未发现 `ReviewTaskQueryServiceImplTest`，实际执行 `ReviewTaskDetailAssemblerTest`、`ReviewServiceImplTest`、`ReviewControllerTest` 共 46 个用例，全部成功。
- `mvn "-Dtest=ReviewTaskDetailAssemblerTest,ReviewServiceImplTest,ReviewControllerTest" clean test` 通过；主代码 351 个源文件重新编译，3 个相关测试类共 46 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- Review 详情查询已进一步收敛为“数据加载/查询编排 + 详情 DTO 组装器”边界，详情展示派生规则具备独立单元测试保护。
- 下一步建议继续评估 `ReviewTaskQueryServiceImpl` 中列表查询、状态查询、timeline/finding/changed files 数据加载的归属，优先抽取可独立验证且仍存在重复或职责聚合的边界；不为单纯行数继续拆分。

### 2026-06-19 架构优化 A125 后端 ReviewTaskDetailDataLoader 详情数据加载边界抽取

已完成：
- 新增 `repoguard-backend/src/main/java/com/repoguard/agent/service/impl/ReviewTaskDetailDataLoader.java`，集中承载 Review 详情页所需 changed files、finding、missing test 和 timeline 的加载与 DTO 转换。
- `ReviewTaskQueryServiceImpl#getReviewDetail` 保留任务存在性校验、失败摘要派生和详情编排职责，将详情子数据加载委托给 `ReviewTaskDetailDataLoader`。
- `ReviewTaskQueryServiceImpl` 移除 changed file、finding、missing test DTO 转换私有方法，减少查询服务对详情子资源加载和展示转换规则的直接承担。
- 保留 `ReviewTaskQueryServiceImpl` 旧构造器兼容手工实例化路径，Spring 主构造器继续保持唯一 `@Autowired` 构造器。
- 新增 `ReviewTaskDetailDataLoaderTest`，覆盖 changed files 映射、finding/missing test 分流、feedback 默认状态与时间格式、timeline 状态映射。

验证记录：
- `mvn "-Dtest=ReviewTaskDetailDataLoaderTest,ReviewTaskDetailAssemblerTest,ReviewServiceImplTest,ReviewControllerTest,SpringBeanConstructorSelectionTest" test` 通过，5 个相关测试类共 48 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- Review 详情查询已形成“任务主数据查询 + 详情子数据加载 + 详情 DTO 组装”的三段式边界，数据加载与 DTO 派生规则都有独立测试保护。
- 下一步建议继续评估 `ReviewTaskQueryServiceImpl` 中列表查询和状态查询的 timeline 读取/失败摘要派生逻辑，若继续抽取，应优先选择可复用且能独立验证的 timeline/failure summary 边界。

### 2026-06-19 架构优化 A126 后端 ReviewFailureSummaryResolver 失败摘要边界抽取

已完成：
- 新增 `repoguard-backend/src/main/java/com/repoguard/agent/service/impl/ReviewFailureSummaryResolver.java`，集中承载审查失败时间线标签解析、结构化 category 识别、旧文本错误兜底分类、中文失败原因和处理建议派生。
- `ReviewTaskQueryServiceImpl` 的列表、详情和状态查询统一调用 `ReviewFailureSummaryResolver#resolve`，不再直接维护大段失败分类分支。
- 保留 `ReviewTaskQueryServiceImpl` 旧构造器兼容手工实例化路径，Spring 主构造器继续保持唯一 `@Autowired` 构造器。
- 新增 `ReviewFailureSummaryResolverTest`，覆盖非失败任务空摘要、最新结构化失败 category 优先、旧 GitHub 403 文本分类和未知异常兜底。

验证记录：
- `mvn "-Dtest=ReviewFailureSummaryResolverTest,ReviewTaskDetailDataLoaderTest,ReviewTaskDetailAssemblerTest,ReviewServiceImplTest,ReviewControllerTest,SpringBeanConstructorSelectionTest" test` 通过，6 个相关测试类共 52 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- Review 查询服务中的失败摘要派生规则已形成独立边界，列表、详情和状态查询共享同一套分类逻辑，避免继续在查询服务内堆积展示诊断规则。
- 下一步建议评估 `ReviewTaskQueryServiceImpl` 中 timeline 批量加载和状态查询最新时间线转换逻辑，若继续拆分，应优先抽取 timeline 查询/转换边界；同时避免为单纯行数继续拆分。

### 2026-06-19 架构优化 A127 后端 ReviewTimelineQueryService 时间线查询边界抽取

已完成：
- 新增 `repoguard-backend/src/main/java/com/repoguard/agent/service/impl/ReviewTimelineQueryService.java`，集中承载 Review timeline 的批量查询、单任务查询、详情展示 item 转换、最新时间线提取和 label 提取。
- `ReviewTaskQueryServiceImpl` 的列表查询、状态查询和详情失败摘要派生改为复用 `ReviewTimelineQueryService`，不再直接维护 timeline 查询和展示状态转换逻辑。
- `ReviewTaskDetailDataLoader` 复用 `ReviewTimelineQueryService#loadItemsByTaskId` 加载详情 timeline，避免详情页与状态页各自维护 timeline item 转换规则。
- 保留 `ReviewTaskQueryServiceImpl` 旧构造器兼容手工实例化路径，Spring 主构造器继续保持唯一 `@Autowired` 构造器。
- 新增 `ReviewTimelineQueryServiceTest`，覆盖批量 timeline 分组、单任务 item 转换、最新时间线提取、实体/DTO label 提取和空输入兜底。

验证记录：
- `mvn "-Dtest=ReviewTimelineQueryServiceTest,ReviewFailureSummaryResolverTest,ReviewTaskDetailDataLoaderTest,ReviewTaskDetailAssemblerTest,ReviewServiceImplTest,ReviewControllerTest,SpringBeanConstructorSelectionTest" test` 通过，7 个相关测试类共 55 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- Review 查询链路中的 timeline 读取与展示转换已形成独立边界，列表、详情和状态查询共享同一套 timeline 查询/转换能力。
- `ReviewTaskQueryServiceImpl` 进一步收敛为查询入口和编排服务；下一步建议重新评估剩余职责是否仍有高价值拆分点，优先选择状态响应组装或列表查询条件构建等可独立验证边界，避免继续为行数做低收益拆分。

### 2026-06-19 架构优化 A128 后端 ReviewTaskStatusAssembler 状态响应组装边界抽取

已完成：
- 新增 `repoguard-backend/src/main/java/com/repoguard/agent/service/impl/ReviewTaskStatusAssembler.java`，集中承载审查状态接口响应 DTO 组装和任务更新时间派生规则。
- `ReviewTaskQueryServiceImpl#getReviewStatus` 保留任务查询、timeline 查询和失败摘要派生编排职责，将 `ReviewTaskStatusResponse` 构建委托给 `ReviewTaskStatusAssembler`。
- 保留 `ReviewTaskQueryServiceImpl` 旧构造器兼容手工实例化路径，Spring 主构造器继续保持唯一 `@Autowired` 构造器。
- 新增 `ReviewTaskStatusAssemblerTest`，覆盖状态响应字段透传、latest timeline 透传、人审字段透传，以及 `finishedAt -> startedAt -> createdAt` 的更新时间优先级。

验证记录：
- `mvn "-Dtest=ReviewTaskStatusAssemblerTest,ReviewTimelineQueryServiceTest,ReviewFailureSummaryResolverTest,ReviewTaskDetailDataLoaderTest,ReviewTaskDetailAssemblerTest,ReviewServiceImplTest,ReviewControllerTest,SpringBeanConstructorSelectionTest" test` 通过，8 个相关测试类共 57 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- Review 状态查询已从“查询 + 时间线转换 + 失败摘要 + 响应 DTO 组装”继续收敛为“查询编排 + 专用状态响应组装器”，状态响应更新时间规则具备独立单元测试保护。
- 下一步建议评估 `ReviewTaskQueryServiceImpl` 中列表查询条件构建是否还有独立抽取价值；若继续拆分，应优先确保 Wrapper 条件构建具备可靠测试方式，避免为了行数拆出难验证逻辑。

### 2026-06-19 架构优化 A129 后端 ReviewTaskListItemAssembler 列表项组装边界抽取

已完成：
- 新增 `repoguard-backend/src/main/java/com/repoguard/agent/service/impl/ReviewTaskListItemAssembler.java`，集中承载审查任务列表项 DTO 组装、状态/风险/来源/LLM 状态小写归一化、任务耗时格式化、人审状态默认值和人审时间格式化。
- `ReviewTaskQueryServiceImpl` 的列表、详情和状态查询统一调用 `ReviewTaskListItemAssembler#assemble`，不再直接维护 `ReviewTaskListItem` 展示派生规则。
- 保留 `ReviewTaskQueryServiceImpl` 旧构造器兼容手工实例化路径，Spring 主构造器继续保持唯一 `@Autowired` 构造器。
- 新增 `ReviewTaskListItemAssemblerTest`，覆盖字段透传、大小写归一化、source/triggerSource 默认值、duration 格式化、失败摘要透传、人审状态默认值和人审时间格式化。

验证记录：
- `mvn "-Dtest=ReviewTaskListItemAssemblerTest,ReviewTaskStatusAssemblerTest,ReviewTimelineQueryServiceTest,ReviewFailureSummaryResolverTest,ReviewTaskDetailDataLoaderTest,ReviewTaskDetailAssemblerTest,ReviewServiceImplTest,ReviewControllerTest,SpringBeanConstructorSelectionTest" test` 通过，9 个相关测试类共 59 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- Review 查询链路中的列表项展示派生规则已形成独立边界，列表、详情和状态查询共享同一套 `ReviewTaskListItem` 组装逻辑。
- `ReviewTaskQueryServiceImpl` 当前已基本收敛为任务查询、分页条件构建和子服务编排；下一步如继续拆分，应优先评估列表查询条件构建是否具备足够收益和可靠测试手段。

### 2026-06-19 架构优化 A130 后端 ReviewTaskListQueryBuilder 列表查询条件边界抽取

已完成：
- 新增 `repoguard-backend/src/main/java/com/repoguard/agent/service/impl/ReviewTaskListQueryBuilder.java`，集中承载审查任务列表查询条件构建、过滤参数 trim、状态/风险/来源字段大写归一化、keyword 数字 PR 编号识别。
- `ReviewTaskQueryServiceImpl#listReviews` 改为委托 `ReviewTaskListQueryBuilder#build` 构建 MyBatis-Plus 查询 Wrapper，查询服务不再直接维护列表过滤条件细节。
- 为避免测试绑定 MyBatis-Plus 内部参数存储实现，`ReviewTaskListQueryBuilder` 将参数归一化结果沉淀为包内可测的 `ReviewTaskListQueryCriteria`，Wrapper 构建复用该归一化结果。
- 保留 `ReviewTaskQueryServiceImpl` 旧构造器兼容手工实例化路径，Spring 主构造器继续保持唯一 `@Autowired` 构造器。
- 新增 `ReviewTaskListQueryBuilderTest`，覆盖过滤参数 trim、大写归一化、数字 keyword 到 PR 编号识别、空白过滤跳过和文本 keyword 不生成 PR 编号条件。

验证记录：
- `mvn "-Dtest=ReviewTaskListQueryBuilderTest,ReviewTaskListItemAssemblerTest,ReviewTaskStatusAssemblerTest,ReviewTimelineQueryServiceTest,ReviewFailureSummaryResolverTest,ReviewTaskDetailDataLoaderTest,ReviewTaskDetailAssemblerTest,ReviewServiceImplTest,ReviewControllerTest,SpringBeanConstructorSelectionTest" test` 通过，10 个相关测试类共 61 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- `ReviewTaskQueryServiceImpl` 已进一步收敛为任务查询入口和子服务编排，列表查询条件、列表项组装、状态响应组装、timeline 查询、失败摘要和详情组装均已形成独立边界。
- 后续不建议继续围绕 `ReviewTaskQueryServiceImpl` 为行数做细粒度拆分；下一步应重新审计当前优化方案中的其他高价值聚合点，例如 GitHub 评论预览/发布内部细节、通知运维链路或前端剩余页面状态逻辑。

### 2026-06-19 架构优化 A131 GitHub 评论预览复用 ReviewTaskListItemAssembler

已完成：
- `GithubCommentPreviewServiceImpl` 不再维护本地 `ReviewTaskListItem` 组装逻辑，改为复用 `ReviewTaskListItemAssembler`。
- 移除 GitHub 评论预览服务中重复的 source 默认值、duration 格式化、人审状态默认值和任务字段小写归一化规则，避免与 Review 查询详情/列表/状态链路维护两套展示派生逻辑。
- 保留原有兼容构造器，新增 Spring 主构造器注入 `ReviewTaskListItemAssembler`，不改变 `GithubCommentPreviewService` 对外接口和评论预览响应结构。
- 将 `GithubCommentPreviewServiceImpl` 纳入 `SpringBeanConstructorSelectionTest`，约束多构造器 Bean 保持唯一 `@Autowired` 构造器。

验证记录：
- `mvn "-Dtest=GithubCommentPreviewServiceImplTest,ReviewTaskListItemAssemblerTest,ReviewTaskListQueryBuilderTest,ReviewServiceImplTest,ReviewControllerTest,SpringBeanConstructorSelectionTest" test` 通过，6 个相关测试类共 54 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- GitHub 评论预览和 Review 查询链路已共享同一套任务列表项派生规则，减少了 PR 总评/风险画像输入模型的重复维护点。
- 下一步建议继续审计 `GithubCommentPreviewServiceImpl` 中 changed files、findings、missing tests 加载与 DTO 转换逻辑，优先抽取可复用的数据加载/组装边界；仍保持评论预览 API 契约不变。

### 2026-06-19 架构优化 A132 GitHub 评论预览数据加载边界抽取

已完成：
- 新增 `repoguard-backend/src/main/java/com/repoguard/agent/service/impl/GithubCommentPreviewDataLoader.java`，集中承载 GitHub 评论预览所需 changed files、actionable findings、finding DTO、missing tests 和 changed file DTO 的加载与转换。
- `GithubCommentPreviewServiceImpl#getPreview` 保留任务校验、风险画像/PR 总评构建、发布状态查询和预览 item 编排职责，将预览基础数据加载委托给 `GithubCommentPreviewDataLoader`。
- 移除 `GithubCommentPreviewServiceImpl` 中 changed file、finding、missing test DTO 转换私有方法，减少预览服务对底层实体加载和 DTO 映射规则的直接承担。
- 新增 `GithubCommentPreviewDataLoaderTest`，覆盖 changed files 按 ID 排序、path map 构建、actionable finding 分离、missing test 分离、feedback 默认状态和时间格式化。

验证记录：
- `mvn "-Dtest=GithubCommentPreviewDataLoaderTest,GithubCommentPreviewServiceImplTest,ReviewTaskListItemAssemblerTest,ReviewServiceImplTest,ReviewControllerTest,SpringBeanConstructorSelectionTest" test` 通过，6 个相关测试类共 53 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- GitHub 评论预览已形成“任务校验/编排 + 预览基础数据加载 + 预览 item 构建”的更清晰边界。
- 下一步建议继续审计 GitHub 评论预览中的发布记录查询逻辑，优先抽取 finding 发布记录和 PR 总评发布记录加载边界；仍保持评论预览响应结构不变。

### 2026-06-19 架构优化 A133 GitHub 评论预览发布记录加载边界抽取

已完成：
- 新增 `repoguard-backend/src/main/java/com/repoguard/agent/service/impl/GithubCommentPreviewPublicationLoader.java`，集中承载 GitHub 评论预览中的 finding 发布记录查询和 PR 总评发布记录查询。
- `GithubCommentPreviewServiceImpl#getPreview` 改为通过 `GithubCommentPreviewPublicationLoader#load` 获取 `publicationByFindingId` 和 `prSummaryPublication`，不再直接维护发布记录查询 Wrapper。
- 移除 `GithubCommentPreviewServiceImpl` 对 `GithubCommentPublicationMapper` 的直接字段依赖，预览服务进一步收敛为任务校验、风险/总评构建、预览 item 编排和响应组装。
- 新增 `GithubCommentPreviewPublicationLoaderTest`，覆盖 finding 发布记录 map 构建、重复 findingId 取首条、PR 总评发布记录加载，以及无 finding 时跳过明细查询。

验证记录：
- `mvn "-Dtest=GithubCommentPreviewPublicationLoaderTest,GithubCommentPreviewDataLoaderTest,GithubCommentPreviewServiceImplTest,ReviewTaskListItemAssemblerTest,ReviewServiceImplTest,ReviewControllerTest,SpringBeanConstructorSelectionTest" test` 通过，7 个相关测试类共 55 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- GitHub 评论预览中的基础数据加载和发布记录加载已分别形成独立边界，预览服务本体更接近应用编排角色。
- 下一步建议评估 `GithubCommentPreviewServiceImpl` 中风险画像/PR 总评构建与 preview item 编排是否还需要进一步抽取；若收益不明显，应转向 GitHub 评论发布链路或通知运维链路的高价值边界。

### 2026-06-19 架构优化 A134 GitHub 评论预览响应组装边界抽取

已完成：
- 新增 `repoguard-backend/src/main/java/com/repoguard/agent/service/impl/GithubCommentPreviewResponseAssembler.java`，集中承载 GitHub 评论预览 item 编排、PR 总评 item 构建、finding item 构建、commentable/published/blocked 计数和 writeback check 组装。
- `GithubCommentPreviewServiceImpl#getPreview` 保留任务校验、基础数据加载、风险画像/PR 总评构建和发布记录加载职责，将最终 `GithubCommentPreviewResponse` 组装委托给新 assembler。
- 预览响应结构、item 顺序和 GitHub 写回检查语义保持不变。
- 新增 `GithubCommentPreviewResponseAssemblerTest`，覆盖 PR 总评 item、finding item、已发布 finding、非 actionable finding、commentable/published/blocked 计数和 writeback check 状态。

验证记录：
- `mvn "-Dtest=GithubCommentPreviewResponseAssemblerTest,GithubCommentPreviewPublicationLoaderTest,GithubCommentPreviewDataLoaderTest,GithubCommentPreviewServiceImplTest,ReviewTaskListItemAssemblerTest,ReviewServiceImplTest,ReviewControllerTest,SpringBeanConstructorSelectionTest" test` 通过，8 个相关测试类共 56 个用例全部成功。
- `git diff --check` 通过；仅有工作区 LF/CRLF 转换提示，无空白错误。

阶段结论：
- GitHub 评论预览链路已形成“任务校验/主流程编排 + 基础数据加载 + 发布记录加载 + 响应组装”的清晰边界。
- `GithubCommentPreviewServiceImpl` 当前已接近稳定应用服务，后续不建议继续围绕预览服务为行数拆分；下一步建议转向 GitHub 评论发布链路或通知运维链路的高价值边界。
